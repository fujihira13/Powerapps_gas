# 実装・検証計画

更新日：2026-09-26

## 今回の完了範囲

目的は、承認済み4画面と件別Excel転記をポートフォリオとして説明でき、架空ログで実際に操作できる状態にすること。詳細な進捗と実施証拠は[docs/TASKS.md](TASKS.md)、再現方法と合格条件は[docs/CHECKS.md](CHECKS.md)を正式記録先とする。

実施状況：C08の短い正常ログ1件の保存・読戻しは2026-09-26に合格した。停止件の表示修正もCanvas MCPで6 YAMLのコンパイル検査に合格し、Studio保存後の再同期内容は候補と一致した。非公開プレビューではrun101の照合・判断が「—」となり、該当操作が無効であること、正常D/Gの表示が維持されることを確認した。READMEの操作案内も実画面に合わせて整備済み。アプリはDeveloper環境の非公開状態である。残る必須ゲートはREADMEの手順を使ったユーザー受入とT-012への記録。run8372の新規通しデモは任意であり、追加のAIテストは予定しない。

今回の優先順は次のとおり。

1. C08の保存表示候補をStudioに保存し、架空ログ1件で保存結果とDataverseの行・添付を読み戻して確認する。**完了**。保存のみ行い、転記開始はしていない。
2. 既に確認済みの正常な複数件転記・Excel実内容と、担当者確認・レビュー往復の証拠を再利用し、同じ正常試験を繰り返さない。
3. 画面操作、期待結果、既知の停止ケース、実装上の限界をREADMEにまとめる。**完了**。
4. ユーザー受入は、READMEの手順でアプリを操作し、表示と説明が分かることを確認する。**未実施（唯一の必須残タスク）**。必要なら利用者がrun8372で任意の通しデモを行う。

## 採用範囲と明示的な除外

- 対象：Developer環境の非公開Power Apps、Dataverseの件・転記先表、接続済みの件別Excel転記フロー、架空ログ、ポートフォリオ説明資料。
- 維持する安全動作：保存・開始・Excel書込の結果が確定しないときは「要確認／未確認」で止め、成功や失敗を推測しない。既存の結果不明件は再実行しない。
- 今回は実装しない：確定保存失敗後の同一件IDでの再保存、Excel失敗件の安全な再実行、保存後に取り消した件の再開・ログ差替え。これらは運用範囲から外し、再試行や再開のボタンを提出版に含めない。
- 対象外：一般公開、実務データ、購入・課金設定、別人アカウントの権限制御、通知、長文分割、追加の障害注入試験。

## 完了判定

提出準備完了は、C08のStudio保存と1件の保存読戻し、確認済みの正常転記・レビュー証跡、停止件表示修正のプレビュー確認、実物と一致するREADME手順が揃い、ユーザーがその手順を理解できた状態とする。これらの確認済みゲートは揃っている。ユーザー受入の記録だけが未実施で、受入後にT-012へ記録する。run8372の新規通しデモは任意で、完了判定の追加テストにはしない。アプリは非公開のままとし、公開は含めない。

文書だけを更新する場合は、差分・相互参照・記載した実在パスを確認する。アプリやフローの変更時は[docs/CHECKS.md](CHECKS.md)の必須ゲートだけを実行し、成功済み確認を理由なく繰り返さない。
