# 検証手順（草案）

状態：**計画のみ。2026-09-25時点でアプリ・フロー・テスト環境は未作成、全項目未実施。** 採用方式と対象端末がT-001～T-006で決まった後、T-007で具体的な操作・期待画面・証拠の保存先を確定する。

## 実装前の起動確認

実行できるアプリができた時点で、新しい変更の前に、PCブラウザで4画面の表示、既存の架空1件の読出し、一覧から件別画面への移動を短く試す。失敗すれば原因と影響を記録し、承認範囲内で修復する。現在はアプリがないため実施不能。

## 条件別の確認案

| 条件 | 操作と期待結果の要点 | 証拠 |
| --- | --- | --- |
| C01 | 異なる環境・サーバーの正常ログを複数開始。1件1冊のExcelを開き直し、全文・識別情報と実反映状態を照合。 | SCR-02、件別Excelの実内容、件IDと版。 |
| C02 | 正常件と日付・本文・識別情報異常を混在。異常件のみ理由付き停止、正常件は継続。 | 件別状態、理由、作成されたExcelの対応。 |
| C03 | Excel作成／書込失敗と結果不明を再現。実反映済みと誤表示せず、自動重複転記しない。 | 画面状態、処理履歴、Excelの実内容。 |
| C04 | 同一件を再登録し、安全条件を満たした失敗件だけ再実行。重複と過去証跡の消失なし。 | 件ID、再実行前後の状態とExcel一覧。 |
| C05 | 担当者役で転記照合と処理結果判断・コメントを別々に保存。 | SCR-03の記録、再表示後の値。 |
| C06 | 1アカウントで依頼、役割切替、確認済み、理由付き差し戻し、修正後の再依頼。 | SCR-02～SCR-04の件別状態と履歴。 |
| C07 | 作者の端末で相手が架空データを操作し、Excelと説明を確認。 | 操作結果、受入記録、限界の説明。 |
| C08 | ログ保存失敗を再現し「保存できませんでした」と表示。その件の転記なし。 | SCR-01、処理記録、Excel未作成の確認。 |
| C09 | 開始前に修正・取消。取消件の処理とExcel作成なし。開始後に進行中取消なし。 | SCR-01～SCR-02、件別状態、Excel一覧。 |

## 実行時の記録

各条件について合格／不合格／未確認／対象外、対象版、日時、OS・ブラウザ、担当、実際の操作、期待結果と実結果、証拠の参照先をTASKS.mdへ残す。未確認を合格へ置き換えない。保存・計算・入力制限・状態遷移は、実装方式が許す場合、期待結果のテストを先に作って失敗を確認する。実機でしか確かめられない画面表示とExcel実反映は手動確認を必須とする。
