# 確認方法

状態：**下書き（2026-09-26）**。T-001～T-003は検証用の別表・別フローで入力、短い架空ログA・Bの件別Excel転記、同一件Cの重複時の既存Excel保護を実機確認済み。完成版候補はDeveloper環境に件表・転記先表、対応9件、OneDriveの架空ログ9件・ひな形1冊、4画面アプリ、件別転記フローを準備した。完成版の旧BはExcel実体とフロー即時読戻しが食い違い`結果不明`で停止した。読取り専用再照会を既存フローへ反映し、新規受付で旧入力を消すCanvas修正も保存した。Chromeの正式プレビューで架空件C・F・新A・D・Gを保存・開始し、それぞれDataverseの件状態`転記済み`／`全文一致`、件別Excelの9項目とログ全文、進捗画面の`処理完了`／`反映確認済み`を確認。D・Gの異なる環境の正常2件は1回の開始操作で各1冊となり、C01を対象版で合格とした。C02更新版でも異常3件の個別停止・Excel未作成と正常A/A・run102の単独回帰をExcel実体まで確認したが、更新後版の異常＋正常混在・添付読取不能・長文等は未確認。C03はローカル候補の再試行抑止・結果不明分岐を静的検査し、変異テストと独立レビューに合格したが、稼働中フローは未変更でC03実機失敗試験は未実施。C04の安全な再実行は未実装。Cの担当者・レビュー最小一巡と、C10の再起動部分試験も確認した。旧Bは保全して再実行していない。実機失敗からの安全な再実行、異常判断、C10の残る分岐は未完了。仕様と規律は`docs/REQUIREMENTS.md`、画面仕様は`docs/ui/`、実施結果は`docs/TASKS.md`を正式な記録先とする。

## 検証の方針

- この文書を正式な検証方法の記録先とする。自動テスト、型チェック、Lint、ビルド、実行場所、前提条件、対象環境、成功の判定方法を記載する。
- 未構築時は未設定とする。構築後に実在する設定と成功・失敗の動作を確認して更新する。存在しないコマンドを推測して案内しない。
- 変更に応じて必要なテスト、型チェック・Lint・ビルドを選び、実行方法と対象を記録する。必須の確認は実施し、該当しない確認は理由を残す。合格後に同じ確認を理由なく繰り返さない。
- モックのUI調整中は、変更した画面と影響する既存画面の表示・文言・主要操作を短い間隔で確認する。毎回すべての機能テストを走らせる前提にはしない。本処理や保存を結合した後は該当する機能・データ・回帰テストを実施し、公開候補では工程11の全体確認を行う。テストを省いたことを合格と記録しない。
- 文書のみの変更では内容、参照、差分を確認する。コード用テストの成功を文書の網羅性の証拠にしない。
- 実施できない検証は理由・未確認範囲・影響・担当・次の行動を報告する。自動テスト合格と実機合格を混同しない。
- 新規の失敗と既存の失敗を区別し、実行環境・対象版・再現条件を記録する。

## 実行設定と前提条件

| 項目 | 現在 | 設定時に確認すること |
| --- | --- | --- |
| アプリ・フロー・保存先 | Developer環境の非公開アプリ、T-001検証専用表と検証フローID`7f1b320f-a8b0-1884-2957-1a9dffb91d02`、T-002検証用Excel A・B・C各1冊。完成版候補には別表`cr6cb_evidencecase`・`cr6cb_evidencedestination`、OneDriveの検証フォルダー、4画面アプリとフロー`架空ログ証跡_件別Excel転記`を接続・保存済み。完成版の旧Bは件状態`結果不明`で保全。新規C・F・A・D・Gは件状態`転記済み`／`全文一致`と件別Excelの実内容一致を確認し、D・Gの1回の同時開始でC01を対象版で合格とした。C02更新版では異常3種の個別停止と正常A/A・run102単独回帰も確認済み。C03の静的候補検査は合格したが実機失敗経路は未確認。C04独立監査では現行列・フローに安全な再実行がなく未実装と確認。試行トークン＋条件付き更新は未検証の候補。 | C02の添付読取不能・異常と正常の混在・長文、C03実機失敗、C04候補のAPI適合性と安全な再実行、C08～C09の保存・取消分岐、レビューの残る分岐を条件別に確認する。Bは再実行・上書きしない。 |
| 実行場所・対象環境 | WindowsのChromeでStudioを操作。Developer環境ID `c1719a14-6cac-eb5b-b598-2254af1c64b7`、アプリID `3186a908-ac5b-48cc-90f5-d50e07282ee7`。OS・ブラウザの正確な版は未記録。 | 実施した端末、環境、対象版を記録。 |
| 自動テスト | Canvasの自動操作テストは未設定・未実施。2026-09-26にリポジトリルートから`python -B -m unittest discover -s outputs/flow-implementation -p test_flow_definition.py`を実行し24件合格。`python -B -m unittest discover -s outputs/flow-implementation -p test_prepare_workflow_patch.py`は4件合格。対象Pythonの`py_compile`と`git diff --check`も成功。C03の変異テストは自動再試行、結果不明ハンドラーの変更、重複コピー、転記先確認の失敗側への処理一式移動を拒否し、正常候補の受入も検査する。これはローカル静的検査であり、実サービスの動作合格ではない。CLI 2.12.2の`pac test --help`には`run`が表示されたが、Canvasのテスト計画ファイルと実行結果はない。[現行のCLI資料](https://learn.microsoft.com/en-us/power-platform/developer/cli/reference/test)はTest Engineを非推奨とし、Power Platform Playwrightのサンプルを案内する。 | 採用するCanvas実機試験の手段、対象、失敗・成功判定を確認。ブラウザを使う試験は実行前にユーザーへ報告。 |
| 型チェック | 対象・コマンド未設定、未実施 | 実装に対象があれば方法を記録。なければ理由付きで対象外。 |
| Lint | 対象・コマンド未設定、未実施 | 実装に対象があれば方法を記録。なければ理由付きで対象外。 |
| ビルド／公開前検査 | Canvas Authoring MCPの`compile_canvas`で完成版候補の6 YAMLが`Validation PASSED`。Ctrl+S後に`sync_canvas`で6 YAMLを読戻し、意図した6ファイルとSHA-256一致、フローAPI1件、公式APIの更新時刻23:44:13 JSTを確認。これは下書き保存の証拠で、実行・公開の合格ではない。CLIの`pac org who`と`pac canvas list`は再認証後に成功。`pac org fetch`も権限のある実行で成功し、通常サンドボックスでは認証キャッシュのロックファイルアクセス拒否。公開前検査は未実施。 | 実画面・実データ・フロー・Excelを対象版と照合する。公開は別承認。 |
| 失敗再現用データ | `samples/t001/mock-log-a.txt`と`mock-log-b.txt`を作成。入力不正は整数列の上限`2147483647`を超える`2147483648`で再現済み。T-002のローカル検証用ひな形は`outputs/t002-20260925/evidence-template.xlsx`。T-006の完成版候補は`outputs/t006-20260925/evidence-template.xlsx`、架空ログは`samples/t006/`。通信・Dataverse側の失敗、結果不明の再現方法は未設定。 | 架空ログ・架空Excelのみを準備し、実務データ・秘密値を含めない。 |

実際のAPIやクラウド保存先に接続する試験は`docs/WORKFLOW.md`の「外部操作を行う場合」に従い、接続先・操作・送信データ・費用・変更の有無を示し、AIまたはユーザーのどちらが実行するか決めてから行う。

### T-002の検証用Excelとクラウド試験

- ローカルひな形`outputs/t002-20260925/evidence-template.xlsx`は`python outputs/t002-20260925/build-template.py`で再作成できる。1シート「証跡」、表`Evidence`の列`CaseId`、`LogFileName`、`Environment`、`Server`、`RunNumber`、`TargetDate`、`LogText`を確認する。表の範囲は`A4:G5`、5行目は`CaseId=__TEMPLATE__`の仮行。元のArtifact Toolによる見出しのみの表はExcel Onlineで修復され表が削除された。修正版はExcel Onlineで修復警告なしに開き、フローの「行の更新」で仮行が実データに置き換わることを確認済み。表定義のローカル検査だけでクラウド適合と判定しない。
- クラウドでのT-002確認方法は、架空ログA・Bを使って異なる2冊を作成し、それぞれの表を開き直す。各冊の件ID、元ファイル名、環境・サーバー・実行回・対象処理日と本文全文を元の架空ファイルと照合し、他件の行がないことを確かめる。作成APIの応答のみでは合格としない。対象フォルダーは業務用OneDrive「ドキュメント」の`架空ログ証跡アプリ検証-T002-20260925`。検証フローIDは`7f1b320f-a8b0-1884-2957-1a9dffb91d02`。2026-09-25にA・Bの各1冊をSharePointプラグインで開き直し、各1行の件ID・元ファイル名・4項目・対象処理日・本文全文と別件混入なしを確認した。独立レビュー担当も指定されたA・Bの2冊だけを読み取り、元の架空ログとの末尾改行までの一致と別件混入なしを確認した。A・Bの再試験はPower Automate実行履歴でも成功と表示された。CLIの`pac power-automate list-flow-runs --workflow-id 7f1b320f-a8b0-1884-2957-1a9dffb91d02 --json`は`null`を返し、実行結果の判定には使えない。
- 初回A（実行ID`08584113081580581615531092475CU21`）は旧ひな形の表がExcel Onlineで削除され、行追加が`BadGateway`になった。実行履歴は「テストがキャンセルされました」、旧Excelに本文行がないことを確認。初回結果が空と分かってから、Aの再試験1回だけコピーの上書きを許可した。再試験後は「上書きする：いいえ」へ戻して保存済み。Excel行更新の再試行ポリシーは「なし」。結果不明時に自動で再実行しない条件と、複製済みブックの実内容確認をT-003へ結び付ける。旧AブックのURLは上書きで無効になり、現在のAは新しいアイテムID`01VTXCECCPBPGUDEQPLNDYPJXAV7RGAMAF`。この一時的な上書き試験は過去の正常な証跡保護の合格根拠にしない。
- 1セル上限を超える長文、書込結果不明と再実行の扱いはT-003・T-004の検証と設計へ渡す。短いログ2件での成功だけでこれらを合格にしない。

### T-006の完成版Excelひな形候補とクラウド配置

- `outputs/t006-20260925/evidence-template.xlsx`は、バンドルされたPythonで`outputs/t006-20260925/build-template.py`を実行して作成した。表`Evidence`の範囲`A4:I5`、9列、`CaseId=__TEMPLATE__`の仮行をopenpyxlで開き直して確認済み。Artifact Toolで同じファイルを読み取り、表1件と9列の表示を確認し、プレビューも目視で確認した。T-002の空表Artifact Tool出力がExcel Onlineで修復された実績があるため、クラウド向けの表はT-002で動いたopenpyxlの作り方を継承した。
- 2026-09-25にSharePointプラグインで業務用OneDrive「ドキュメント」の`架空ログ証跡アプリ完成版検証-T006-20260925`（フォルダーID`01VTXCECE5BP476QXS7RFIAAAMC5QNGY6X`）へひな形1冊と架空ログ9件を上書きなしで配置し、再一覧で10ファイルを確認した。ひな形のアイテムIDは`01VTXCECFCHLCOP5KJYFG3SOSB36BFWFWD`。プラグインで取得した文字抽出には9列の見出しと`__TEMPLATE__`行がある。配置・読取は確認済みだが、Excel Onlineの開封・修復有無は未確認。
- 完成版ではコピーしたブックの仮行を更新するフロー定義を作成・有効化した。T-006ひな形のExcel Onlineでの修復有無、行更新、30,000 UTF-16単位を超える本文の分割行追加と全文復元は未実施・未確認。クラウド起動コマンドは未設定。T-009・T-011で実物を確認する。
- 完成版候補の件表`cr6cb_evidencecase`はメモ添付を有効にし、環境・サーバー各100文字、実行回1以上の整数、対象処理日`DateOnly`動作の列を作成した。対応表`cr6cb_evidencedestination`は環境・サーバー、ドライブID、フォルダーID、有効フラグを作成した。Dataverse CLIから列を読み戻し、対象処理日の`Format=DateOnly`・`Behavior=DateOnly`・`Required=ApplicationRequired`を確認。件表4項目と対応表2項目の代替キーは14:04 JSTに両方`Active`。対応表の同一組合せ2件目はAPIのキー違反で拒否され、その時点の件数8を維持。その後に架空Hを1件追加して9件を読戻し確認。2026-09-25のT-007実API試験では、件表の正常POSTは201、同じ4項目の重複POSTは412で拒否されたが、環境列欠落POSTも201で受理され、最終読戻しは2行。必須値拒否の実証は失敗し、残りの欠落値POST・条件付き更新は停止して未実施。現行の未保護表へ欠落値POSTを追加しない。新しいサーバー側検証方式は未採用で、追加試験・展開には別途承認が必要。`ApplicationRequired`はAPI経由の空値拒否の証拠ではない。
- 対応表の9件は架空環境A～H・架空環境長文と各架空サーバーの組合せ。すべて有効で、業務用OneDrive「ドキュメント」の同じT-006検証フォルダーIDを指すことをCLIで読み戻した。`missing-identifier.txt`の本文はサーバー行を欠くが、画面では登録済みのH組合せを選び、開始前の転記先確認を通してから本文の識別情報欠落を検査する。実際のアプリ・フローでの確認は未実施。

### T-007の過去の実証ゲート（空値拒否の失敗で途中停止・今回の実装ゲートから除外）

ローカルの要求データは`python outputs/t007-20260925/build-case-test-payloads.py`で生成し、8件のJSONを静的確認済み。このコマンドはクラウドへ送信しない。2026-09-25に、最大8件の作成要求・読取・条件付き更新CLI試験の実行範囲が承認された。実API試験は事前GET・正常1件・重複1件・環境列欠落1件まで実施し、環境列を欠く要求がHTTP 201で保存されたため、その時点で停止した。最終読戻しは2行。詳細と行IDは`outputs/t007-20260925/api-test-run-20260925.md`を参照。**残りの欠落値POSTは現行の未保護表へ送らない。新方式を使う実API試験とプラグイン等のサーバー側検証の展開は別途承認を得てから行う。**フロー作成・有効化・起動、Excel Onlineでの開封・行操作、C10の追加実証もこの承認範囲に含めず、実施前に別途範囲を確認する。

| 対象 | 操作と判定方法 | 現在 |
| --- | --- | --- |
| 件表の業務キーと日付（CLI） | 架空データで正常1、同じ4項目の重複1、4項目を各1つ空にした要求4、別の同一4項目の近接要求2の計最大8回のDataverse CLI作成要求を行い、想定の保存2行だけが残るかIDと列値をCLIで読戻す。拒否されない場合は一覧を記録し、フローを起動せずに止める。`DateOnly`列もAPI読戻し値だけを記録する。 | 事前GET HTTP 200・0行。正常要求はHTTP 201でID `fed4e447-b1b8-f111-b377-7ced8d3141aa`、重複はHTTP 412で拒否、環境列欠落要求は予想外にHTTP 201でID `3086ba97-b1b8-f111-b377-7ced8d3141aa`。最終GET HTTP 200・2行（正常行と環境値が空の行）。**空値拒否の個別実証は失敗し停止**。残り5件の作成要求は未送信とし、現行の未保護表への追加の欠落値POSTも行わない。新方式を使う実API試験は別途承認後に行う。詳細は`outputs/t007-20260925/api-test-run-20260925.md`。画面・フローとの日付照合、添付・Excel確認は今回の承認範囲外。 |
| 件表の条件付き更新 | 新規の架空試験行を読んで版を控え、別更新の後に古い版を`If-Match`で指定して更新を試す。古い版の変更が拒否され、元の値が維持されることを読戻す。Canvas・フローから開始受付、修正・取消、古い試行の遅延応答でも同じ拒否ができるか別に確認する。 | 2表の`IsOptimisticConcurrencyEnabled=True`、Dataverse CLIの`api request --header`対応のみ確認。環境列欠落の拒否条件失敗でT-007試験は停止し、条件付き更新は未送信。Canvas・フローからの試験も未実施。停止後の再開範囲は別途確認する。 |
| ソリューション内フロー | 既存の接続参照で1件作成し、定義・状態・画面用resource IDを読戻す。有効化、アプリからの起動、結果Excelの実内容を別に判定する。 | 2026-09-25にCLIでフロー `ef881fbc-dcb8-f111-b377-7ced8d3141aa` を1件作成・有効化。読戻し修正版の`clientdata`もCLIで完全一致を確認。Canvasへ接続し、架空B・C各1回をアプリから起動。Cは件状態`転記済み`／`全文一致`、Excel実体1冊・1行の9項目・全文一致まで確認。Bは件状態`結果不明`のまま保全。異常・長文・複数件・再実行は未実施。 |
| 完成版Excelひな形 | 元のT-006ひな形を変更せずにコピーを作り、Excel Online (Business)で仮行更新・分割行追加・読戻しを行う。件ID・4項目・元ファイル名・分割番号と全文を元ログと比較し、警告・修復・遅延・重複を調べる。 | 短い架空件B・Cで各1冊の結果Excelを作り、SharePointプラグインの読戻しで各1行の識別情報と全文一致を確認。Cは件状態と画面表示も一致。BはExcel実体が正しくても件状態`結果不明`。長文分割、複数件同時、失敗・再実行の試験は未実施。 |

2026-09-25に最大8件の作成要求・読取・条件付き更新CLI試験の実行範囲が承認された。実API試験は事前GET 200・0行、正常POST 201、同じ4項目の重複POST 412、環境列欠落POST 201で停止した。最終GETは200・2行で、2行が残存する。必須値拒否の実証は失敗し、残り5件のPOSTと条件付き更新は未送信。現行の未保護表に追加の欠落値POSTを送らない。新しいサーバー側検証方式は未採用で、新方式のAPI試験・プラグイン実装・登録・展開、新しい専用クラウド検証表の作成には別途承認が必要。個別試験の失敗はWORKFLOWの正式評価回数に加算しない。フロー・結果Excelの新規作成とExcel Online操作は**このT-007試験の当初承認範囲外**。後続で完成版フロー1件と架空ログ2件の作成・転記は別途承認され、上表のフロー作成と架空件A・添付1件の保存まで実施した。別操作は`docs/WORKFLOW.md`の「外部操作を行う場合」に従い、対象・架空データ・費用・変更を示して実行範囲を確認する。資料・メタデータ確認、ローカル模擬試験、クラウド実機成功を分けて記録する。

### T-009：件別転記フローの確認

- ローカル定義と構造の検査はリポジトリルートで`python -m unittest discover -s outputs/flow-implementation -p test_flow_definition.py`、`python outputs/flow-implementation/build_flow_definition.py`、`python outputs/flow-implementation/case_transfer.py --self-test`を実行する。2026-09-25に単体検査12件と自己試験が合格した。これらはクラウド動作の証拠ではない。
- 実際の作成では、存在する`@microsoft/dataverse` CLIの`api request --target dataverse --environment https://org3d6ad684.crm7.dynamics.com/ --path /api/data/v9.2/workflows --method POST --body-file outputs/flow-implementation/workflow-create-body.json`相当を使った。同名0件を先に読み、送信後に`workflows`のID・状態・`clientdata`を読戻した。CLIは現在`C:\Users\misum\AppData\Local\npm-cache\_npx\a64cee6843b5ff58\node_modules\@microsoft\dataverse\bin\dataverse.js`に存在するが、キャッシュ内パスは将来も同じとは限らない。フローの再作成コマンドとして安易に再実行しない。
- `pac power-automate list-cloud-flows --environment c1719a14-6cac-eb5b-b598-2254af1c64b7 --workflow-id ef881fbc-dcb8-f111-b377-7ced8d3141aa --json`でresource IDとPublishedを確認した。`pac power-automate list-flow-actions`は83行を返し、重複を除いた操作種別6件とトリガー1行を確認した。83行を83アクションとは数えない。
- 架空件A `1e47d146-ddb8-f111-b377-7ced8d3141aa`に`normal-a.txt`の166バイトを注釈 `9e59c959-ddb8-f111-b377-7ced8d3141aa`として保存し、親件ID・ファイル名・添付の有無をCLIで読戻した。本文全文の再読戻し、フロー起動、Excel作成・行更新・実内容は未実施。Bはまだ登録していない。
- 起動後は同一件IDの結果Excelが1冊だけできること、`Evidence`表の`CaseId`・元ファイル名・4項目・本文が架空ログAと一致すること、保存済み状態とExcel実内容が一致することを別々に確かめる。フローの実行履歴だけでは合格にしない。長文分割・同時起動ロック・安全な再実行、`excelurl`の記録は現定義に未実装であり、C01～C04の全体合格は未確認。
- `pac power-automate`には起動コマンドがなく、Canvas Authoring MCPの安全な空フォルダー同期は`Nothing written`、Chromeブラウザ操作はauto-reviewがChrome限定条件を確認できないとして2回拒否した。Codex内ブラウザ・別の画面操作へ切り替えていない。実行不能の影響は、クラウド処理・Excel実内容とアプリ結線の検証が未確認のまま残ること。担当は統括、次の行動は許可されたChrome接続の復旧かユーザー本人による架空件Aの手動起動と実行結果の共有。
- ユーザー共有のChrome画像では、作成したフローの編集画面と先頭4ブロック、3接続の「接続されています」を確認した。保存済みの全定義や実行成功の証拠ではない。Canvas MCPの再接続は成功したがデータソース一覧は0件。`pac canvas download`は接続後に`System.InvalidOperationException`で終了し、クラウドアプリの保存済み状態は読戻せていない。
- 正確なPower Apps Studio編集URLが共有された後、Canvas MCPのデータソース一覧は3件となり、`sync_canvas`で編集セッションから2画面・4 YAMLファイルを読み戻した。承認済み4画面版との差は、欠けているScreen3・Screen4とScreen2の2行、Screen1のプロパティ順序だけ。同期済みファイルを基準にScreen2の2行とScreen3・Screen4をローカルで準備し、App・Screen1・`_EditorState`は保持した。`compile_canvas`はauto-reviewによる外部送信範囲の承認不足で実行前に拒否され、検査結果・サーバー反映・Studio保存は未確認。承認を得た場合に同じ6 YAMLファイルを検査する。
- その後、ユーザーの明示許可を受けて同じ6 YAMLファイルの`compile_canvas`を実行し、`Validation PASSED`。新規ディレクトリへの`sync_canvas`読戻しは6ファイル・4画面。App・Screen1・Screen2は完全一致、Screen3・Screen4にはPower Apps側の正規化差分がある。これは編集セッションの反映確認であり、Studio保存・開き直し・実画面の操作確認ではない。保存完了後に対象4画面の表示と主要操作を確認する。
- フローがStudioの「アプリ内」とMCPの`list_apis`双方に現れ、`describe_api`は`Run(text: String caseId) -> Boolean`を返した。Screen1の開始式とApp.StartScreen更新後、ForAll内Setのエラー11件、転記先照会の委任警告4件を修正して6ファイルで`Validation PASSED`。独立レビューが指摘した開始中・結果不明時の操作制限も修正し、再コンパイルは`Validation PASSED`。新規ディレクトリへの読戻しで変更したScreen1はバイト一致、MCPのAPI一覧にフロー1件。Studio保存・短い架空ログBでの実行・Excel実内容は未実施。

### T-007 Phase B：同期プラグイン候補のオフラインTDD

2026-09-25にユーザーが承認した範囲は`dataverse-plugins/`内のローカル候補コードとTDDまで。専用クラウド表の作成、Dataverseへの登録・展開、新方式のAPI試験は承認されていない。候補はPAC CLI 2.12.2付属の実`Microsoft.Xrm.Sdk.dll`を参照し、.NET Framework 4.8・fake execution context・合成テーブル名でオフライン実行する。NuGetソースは`dataverse-plugins/NuGet.Config`で無効化している。リポジトリルートからの実行コマンドは`.\dataverse-plugins\run-local-tests.ps1`。

- 初回red：5 pass／6 fail。実装前の欠落・null・空値・Update消去等の拒否期待を実行で確認。証拠：`dataverse-plugins/evidence/tdd-red.txt`。green：11 pass／0 fail。証拠：`dataverse-plugins/evidence/tdd-green.txt`。
- 独立レビューでHigh指摘2件（実行回0／-1を許す、既存件表名をコードに固定）を受け、どちらも赤テストで再現して修正。再現証拠：`tdd-review-fix-table-red.txt`、`tdd-review-fix-red.txt`。最終greenは16 pass／0 fail、ビルド0警告・0エラー：`dataverse-plugins/evidence/tdd-review-fix-green.txt`。独立再レビューでHigh 2件の解消を確認した。
- この結果が示すのはローカル候補の入力検査だけ。重複タプルケースは値検査を通ることの確認に限られ、代替キーの重複拒否・Active状態は示さない。net462の正式アセンブリビルド、実際のテーブルへの登録・実動作、権限・費用、全書込経路での迂回不能は未確認。TR-03適合や新方式採用の判定に使わず、クラウド操作には対象ごとの別承認を得る。

### T-008Mの4画面先行モック

- 対象は`outputs/ui-mock/index.html`のローカル操作モック。構文はHTML内の`<script>`を取り出し、`([regex]::Match((Get-Content 'outputs/ui-mock/index.html' -Raw -Encoding UTF8), '(?s)<script>(.*?)</script>')).Groups[1].Value | node --check -`で確認する。2026-09-25に実行して終了コード0。4画面のCanvas YAMLは作成していないため、`compile_canvas`は**対象外**。ローカルHTMLの構文成功をPower Apps本体のコンパイル成功と扱わない。
- SCR-01では架空件の選択、件ごとの4項目、修正・取消、0件時の開始不可、開始操作後のSCR-02遷移を確かめる。SCR-02では件別の保存・処理・Excel実反映・担当者確認・レビューを別表示し、件の詳細、レビュー、受付へ戻る導線を確かめる。処理中の件が残る状態でも「新しい件を受付」からSCR-01へ移動でき、既存件の状態・データが変わらないことを実機で確認する。SCR-03では元ログとExcelを見比べる場面、二種類の担当者記録、依頼・再実行可否の表示を確かめる。SCR-04では実演上の役割切替、確認済み・理由付き差し戻し、一覧への戻りを確かめる。各画面の正確な項目・文言・操作条件は`docs/ui/screens/`の該当資料を参照する。
- 正常、異常・結果不明、差し戻し後の再依頼を架空状態で一巡する。ボタンから到達でき、選択した同じ件の情報が別画面でも対応し、戻った後に表示が矛盾しなければ**モックの遷移確認**とする。セッション中の表示をDataverse保存、Excel実反映、フロー処理、権限制御の合格としない。
- 2026-09-25に`python 'outputs/ui-mock/check_mock.py'`を実行して終了コード0。Windows上のChromium 145.0.7632.6で1440×900の4画面と390×844の受付を表示し、初期0件で開始不可、取消件を同じ件IDでログ差し替え、架空4件の開始、転記先未登録、安全な停止、Excel確認前後、担当者記録、依頼、差し戻し、再依頼、確認済み、結果不明の再実行不可、受付への戻り、処理済み件を再処理せず進捗へ戻れること、狭幅での横はみ出しなしを確認した。画面画像は`outputs/ui-mock/review-01-intake.png`～`review-05-narrow.png`。モックの表示・操作確認であり、実サービスの試験結果ではない。ユーザーは画面の方向と仮遷移を受け入れたが、本人が全操作を完了したかは**未確認**。完成版アプリの受入は**未実施**。ブラウザ／コンピュータユースをAIが使う場合は事前報告する。
- この段階は見た目と一時的な画面遷移のモックで、保存先へ状態を書かず業務判定を実装しないためTDDの対象外。代替としてCLI構文確認、仕様との静的照合、クリック操作と実表示の確認を行う。T-008～T-010で本処理を結ぶ際の保存・判定・重複防止・レビュー依頼条件はTDD対象のままにする。

### T-008～T-010の完成版画面（段階別の確認記録）

- 対象は`log-evidence-app-complete/`の`App.pa.yaml`、`_EditorState.pa.yaml`、`Screen1.pa.yaml`～`Screen4.pa.yaml`。Canvas Authoring MCPの`compile_canvas`をこのディレクトリへ実行し、2026-09-25に最新6ファイルで`Validation PASSED`を確認した。直前の`sync_canvas`ではサーバー側4ファイル・2画面、直後の別ディレクトリへの再同期では6ファイル・4画面を確認。再同期で一部のプロパティ順序・既定値・添付欄の自動プロパティに正規化差分が出たため、バイト一致を反映の条件にしない。後続のStudio保存・画面実動作は下記の個別結果を参照する。コンパイル成功だけでStudio表示・永続保存・実動作を合格にしない。ツール呼出しの再現は有効な編集セッションへの`connect`とディレクトリ指定が前提で、一般のシェルコマンドとしては未設定。
- ローカル静的検査だけでは、Dataverseの保存・状態更新、実画面の表示・遷移、添付の参照、再起動後の復元、フロー起動、Excel実反映、レビュー更新競合を合格としない。クラウドアプリへの保存、Studio非公開プレビュー、架空件による一部の操作は後続で確認した。各条件の範囲と未確認分岐は下記および`docs/TASKS.md`を参照する。
- 保存・レビュー依頼条件など自動判定可能な処理には、実物へ反映できる経路が決まった後、条件から先に失敗ケースを確かめる。現在のYAML静的コンパイルはTDDのred/greenに相当しない。実画面では4画面の文言・視認性・主要操作と影響画面を確認し、架空件の保存・読戻し・再起動・レビュー状態を別に検証する。実施できない間は未確認と記録する。

#### T-010：C05・C06の既存C 1件による最小実機一巡（初回評価不合格・修正後一巡合格）

- 根拠：`docs/REQUIREMENTS.md`のREQ-05・REQ-06、完成条件C05・C06、技術上の規律TR-05（草案）、および[`件ごとの確認`](ui/screens/件ごとの確認.md)・[`レビュー`](ui/screens/レビュー.md)。この手順は承認済みの画面操作を検証するもので、仕様追加ではない。
- 対象は既存の架空件C 1件だけ：件ID`7eace997-2fb9-f111-b377-7ced8d3141aa`、`架空環境B`／`架空サーバーB`／実行回`2`／対象処理日`2026-09-25`、元ログ`samples/t006/normal-c-readback.txt`。事前の読み取りで対象行が1件、処理状態`転記済み`、Excel照合`全文一致`、ログ添付と対応する結果Excelが存在することを確認する。レビュー状態が空欄または`未依頼`で、担当者3記録が未記録の場合だけ続行する。いずれかが異なる、既存のレビュー記録がある、または行・添付・Excelを読み取れない場合は停止し、記録を消去・上書きしない。
- 開始前にDataverseの件ID・4項目・処理状態・Excel状態・元ログ添付のIDと全文を読み取り、対応するExcelのアイテムID・Evidence表9列・全文を基準として記録する。各段階の後に同じC行を読み戻し、下表で指定するレビュー欄だけが意図どおり変わったことを確認する。件ID・4項目・処理状態・Excel状態／URL、元ログ添付と本文、Excelアイテムと表の内容に差があればそこで停止する。
- 2026-09-26初回正式評価（不合格1回目）：CLI事前読取でC一行・4項目・`転記済み`／`全文一致`・annotation添付1件と結果Excel1冊の内容一致・担当者欄空・レビュー未依頼を確認。ChromeプレビューのScreen3で元ログが「添付されているものはありません」、証跡Excelリンクが無効だったため、担当者記録の書込前に停止した。C05・C06は未達、実機一巡未実施、Cレビュー欄への書込ゼロ。別stageのScreen3修正候補はCanvas MCP compile `Validation PASSED`・独立静的レビュー済みだが、Studioに灰色の「保存しています...」が残り、永続保存・実表示は未確認。`cr6cb_excelurl`は空欄で、標準コネクタによる安全な保存方法も未確認。
- 同日、元ログ表示の修正版`outputs/canvas-t010-attachment-display-final-stage-20260926/`はCanvas Authoring MCPの`compile_canvas`で`Validation PASSED`。非公開StudioプレビューでCの`normal-c-readback.txt`を表示し、Studioタイトルは「保存済み (公開取り消し済み)」だった。元ログの画面上の参照は修正済み。証跡Excelリンクは`cr6cb_excelurl`空欄のため未確認で、レビュー欄の書込と一巡は未実施。添付ファイルを開いた画面での本文確認も未実施。SharePointプラグインの読取ではCの結果Excelの直接ファイルURLから同じ9列・全文を取得できたが、Chromeでの直接URLの閲覧はクライアント側の`ERR_BLOCKED_BY_CLIENT`で未確認。プラグインの読取成功をブラウザ閲覧成功と扱わない。
- 追加承認後、Excel URL記録版フロー候補のローカルテスト16件・静的検査に合格。既存フローのID・有効状態・旧定義の完全一致を条件に`clientdata`だけをPATCHしHTTP 204、再読取で新定義の文字列完全一致・有効状態を確認。Cの`cr6cb_excelurl`は、既存ExcelのSharePoint直接URLと一致する1列だけをETag条件付きで更新。Dataverse読戻しでCの4項目・処理状態・Excel照合・レビュー欄は不変、SharePointのC ExcelアイテムID・eTag・サイズも不変。非公開StudioプレビューでCの「証跡Excel」は有効になり、クリックでC件ID名のExcel 8256バイトがChromeへダウンロードされた。フローの新定義を新規件で実行した結果は未実施であり、ローカルテスト合格とクラウド実行成功を混同しない。
- 2026-09-26修正後の最小一巡：Chrome非公開StudioプレビューとDeveloper環境の既存C 1行を使用。ダウンロードしたExcelは9列、C件ID・4項目・元ログ80文字が一致。画面から担当者の`確認保留`／`判断保留`／コメントを保存→依頼→理由付き差し戻し→`一致`／`正常`／コメントに修正→再依頼→確認済みを各1回操作した。各書込直後にDataverse CLIで同じC行を読み戻し、予定した欄と状態遷移が一致、件ID・4項目・処理状態`転記済み`・Excel照合`全文一致`・Excel URLが不変と確認。再依頼前の差し戻し理由表示は保存先と一致し、確認済み保存後の理由欄は`null`。確認済み後はScreen3の担当者入力・保存・依頼・再実行、Screen4の役割切替・結果入力・保存が画面上で無効。SharePointプラグインの再読取ではCのExcelアイテムID、eTag、8256バイト、QuickXorHashが基準と一致した。この1件の手順は合格。実権限制御、別人レビュー、異常判断、空コメント拒否、古い依頼版、競合、フロー新定義での新規件実行は未確認。
- URL記録版フロー候補の独立静的レビューで、偽SharePointホスト、コピー先パスの不一致、読戻し条件・順序の改変を検査する必要を確認。`outputs/flow-implementation/build_flow_definition.py`と`test_flow_definition.py`を修正し、`python -m unittest discover -s outputs/flow-implementation -p test_flow_definition.py`の全18件と`py_compile`に合格した。既存クラウド更新bodyの`clientdata`と修正後の既定生成候補は完全一致、新Validatorでもエラーなし。クラウド定義の再PATCHや新規件のフロー実行は未実施。

- 一巡の実施は承認済み。Dataverseに担当者記録・依頼・レビュー結果を書き込む手順のため、実行時は`docs/WORKFLOW.md`に従い接続先・書込データ・費用やデータ変更の有無・実行者を記録する。Screen3修正候補の永続保存・元ログ表示、および証跡Excel URLの安全な保存が確認できるまでCのレビュー欄へ書き込まない。旧Bや他の件は開かず、変更せず、フローを起動せず、Excelを作成・更新しない。

| 順 | 画面操作 | 直後に照合する結果 |
| --- | --- | --- |
| 1 | 進捗一覧からCの「件を確認」。元ログとExcelを開いて照合し、「転記内容の照合」を`確認保留`、「処理結果の判断」を`判断保留`にする。コメントを入力し「確認を記録」。 | Cの`cr6cb_transfercheck`、`cr6cb_outcomejudgment`、`cr6cb_reviewcomment`だけが入力と一致して保存される。レビューは未依頼のまま。 |
| 2 | 「レビューを依頼」を1回押す。 | Cの`cr6cb_reviewstatus`が`依頼中`。一覧にも同状態が表示される。 |
| 3 | 一覧の「レビューを確認」からレビュー画面を開き、「レビュアー役に切り替える」。 | 画面に実演上の役割`レビュアー役`を表示。別アカウントや実権限の確認とは扱わない。 |
| 4 | 結果を`差し戻し`にし、架空の差し戻し理由を入力して「レビュー結果を記録」。 | Cのレビュー状態が`差し戻し`、差し戻し理由が入力値と一致。画面は一覧へ戻り、実演役割は担当者役。 |
| 5 | Cの「件を確認」へ戻る。ログとExcelを再照合して照合を`一致`、ログに記載された結果を`正常`、コメントを照合内容に合わせて更新し、「確認を記録」。 | Cの担当者3記録が更新され、レビュー状態は`差し戻し`のまま。ログ・4項目・処理状態・Excelは基準時点と不変。 |
| 6 | 「再依頼」を1回押す。 | Cのレビュー状態が`再依頼`となり、Screen2（進捗一覧）へ戻る。 |
| 7 | 一覧の「レビューを確認」からScreen4を開き、差し戻し理由と担当者3記録が画面表示と保存先で一致することを確認する。再度「レビュアー役に切り替える」。結果を`確認済み`にして「レビュー結果を記録」。 | 確認済みを記録する前に、差し戻し理由と担当者3記録のScreen4表示が保存先の値と一致。記録後はCのレビュー状態が`確認済み`となり、一覧に反映され、実演役割は担当者役へ戻る。 |
| 8 | 一覧からCの「件を確認」を開き、担当者入力・依頼操作の可否を確認する。続けて「レビューを確認」を開き、レビュアー役切替・結果入力・記録操作の可否を確認。DataverseとExcelを基準値と再照合する。 | Screen3の担当者入力・依頼操作とScreen4のレビュアー切替・結果記録が無効。件ID・4項目・元ログ添付・処理状態・Excel状態／URL・Excelアイテムと実内容は不変。 |

- どの段階でも保存が失敗・不明、期待状態との不一致、別件の表示、基準データの差異があれば、再保存・再依頼・フロー起動をせず停止して実際の状態を記録する。各書込は画面表示だけで合格とせず、Dataverse読戻しで確認する。
- この一巡はC05・C06の主要な表示・保存・差し戻し・再依頼・確認済み後の操作制限を確認する最小手順。判断保留はコメントありの1ケースを通すが、判断「異常」の記録、および判断「異常」「判断保留」でコメントが空欄の場合の確認記録・レビュー依頼拒否の分岐はこの手順に含まず、未確認のまま残す。古い依頼の応答拒否、同時更新、実権限制御、別人レビューも未確認のまま残す。完了・公開の判定には使わない。

#### T-011：C10の再起動部分試験

- 2026-09-26、Chromeの非公開Studioプレビューで保存済みC・停止中Bを進捗一覧に表示。受付で架空Fログを未保存のまま選び、環境・サーバー・実行回・日付を入力してからプレビューを閉じ、Studio編集タブを再読込した。再プレビューでは進捗一覧が初期画面となり、受付へ移ると選択待ち0件、添付なし、実行回・日付は空欄。保存済み件の復元と未保存入力の破棄はこの条件で合格。進行中の件がない場合の受付初期表示は未確認。保存・処理開始は押していないため、この部分試験でFの件行やExcelを作ったとは扱わない。

#### T-011：URL記録版フローの新規件試験

- 2026-09-26、Developer環境の非公開Studioプレビューで`samples/t006/judgment-hold.txt`（架空環境F／架空サーバーF／実行回1／2026-09-25）を1件保存し、保存先に件1行・添付1件と元ファイルの262バイト・SHA-256一致を確認してから「処理開始」を1回押した。件IDは`0165ef02-5ab9-f111-b377-7ced8d3141aa`。
- 読戻し結果：Dataverseは`転記済み`／`全文一致`、Excel URLあり。T-006フォルダーに同件ID名のExcelは1冊だけで、Evidence表9列・対象行1行の識別情報・ログ全文は一致。日付セル`2026-09-25 00:00:00`は対象日と一致。Excelの直接URLとDataverse URLが一致し、Screen2は`処理完了`／`反映確認済み`、Screen3の「証跡Excel」リンクは有効。フローの追加実行・別件の変更は未実施。
- 判定：F 1件の保存・転記・Excel実内容・リンクは合格。C01の同時複数件、失敗・結果不明・長文・異常の分岐、C10の進行中件がない初期表示はこの試験の対象外で未確認。クラウド操作のコマンドは自動再現手順として未設定。実画面とDataverse・OneDrive読戻しを証拠とする。

#### T-011：正常件と日付不一致件の同時開始

- 2026-09-26、非公開Studioプレビューで`normal-a-run2.txt`（環境A／サーバーA／実行回2／指定日2026-09-25）と`date-mismatch.txt`（環境C／サーバーC／実行回1／指定日2026-09-25、本文日付2026-09-24）を各1回保存した。事前読取で同じ4項目は各0行、転記先は各1件。保存後はDataverseが各1行・annotation各1件で、ファイル名・長さ・SHA-256が元ログと一致。件IDは順に`9f0b7d1e-5db9-f111-b377-7ced8d3141aa`と`308ded3a-5db9-f111-b377-7ced8d3141aa`。
- 「処理開始」を1回押した結果、正常Aは`転記済み`／`全文一致`でExcel1冊・Evidence 9列1行の識別情報と本文全文が一致。件表URLと実ファイルURLが一致し、画面は`処理完了`／`反映確認済み`とExcelリンク有効。日付不一致Cは`停止`／Excel確認`未確認`で結果Excel0冊、進捗一覧に理由を表示。理由文は「ログの日付・識別情報が件と一致しないか、1行上限を超えています。」と複数原因を列挙しており、実際の日付不一致は元ログと指定日の照合で特定した。
- 判定：C02の日付不一致1件と正常1件の分離は合格。他の異常入力、C01の複数正常件、失敗・再実行は未確認。停止理由の具体性は実演前に見直す。クラウド操作の自動再現コマンドは未設定、実画面とDataverse・OneDrive読戻しを証拠とする。

#### T-011：異なる環境の正常2件を同時開始

- 2026-09-26、`normal-d-run2.txt`（D／D／実行回2／2026-09-25）と`normal-g-run2.txt`（G／G／実行回2／同日）を非公開Studioプレビューの同じ受付で各1回保存し、保存先の行各1件と添付各1件が元ログの各136バイト・SHA-256と一致することを確認。件IDはD=`d08f0611-5fb9-f111-b377-7ced8d3141aa`、G=`96709826-5fb9-f111-b377-7ced8d3141aa`。事前に両4項目の重複0件と有効転記先各1件を読取り確認。
- 処理開始を1回押した後、両件はDataverseで`転記済み`／`全文一致`。T-006フォルダーには件ID別のExcelが各1冊・Evidence表各9列1行。件ID・ログ名・4項目・分割情報・ログ全文がそれぞれ元ファイルに一致し、相互取り違えなし。進捗画面の2件はいずれも`処理完了`／`反映確認済み`。両元ログの詳細行は実際にはともに`詳細: 架空環境。`であり、その実内容との一致を確認した。
- 判定：C01の異なる環境・サーバーの正常複数件を同時に処理し、1件1冊・全文・実反映を確認する条件は、この非公開版で合格。失敗・異常・再実行の条件へ転用しない。クラウド操作の自動再現コマンドは未設定。

#### T-011：C02停止理由具体化版の反映と実機確認

- 対象：`outputs/flow-implementation/build_flow_definition.py`、`test_flow_definition.py`、`flow-definition.excelurl-c02-local-candidate.json`。入力異常ごとの理由、実在しない暦日の拒否、添付本文の読取失敗・タイムアウト時のExcelコピー前の停止記録を確認する。成功時のExcel URL記録・読戻し条件・接続参照も維持する。
- ローカル確認：`python -m unittest discover -s outputs/flow-implementation -p test_flow_definition.py`で21件成功、`python -m py_compile outputs/flow-implementation/build_flow_definition.py outputs/flow-implementation/test_flow_definition.py`成功、生成候補の静的検査はエラー0件。GPT-6 Luna／maxの独立レビューでも読取失敗経路からコピーへ進まない構造を確認した。
- 稼働中フローとの事前照合用`outputs/flow-implementation/prepare_workflow_patch.py`はオフライン専用で、ネットワーク通信をしない。保存済みGET応答の対象ID・名前・有効状態・ETagと候補SHA-256を検査し、許可したC02関連差分以外があればPATCH用ファイルを作らず停止する。`python outputs/flow-implementation/test_prepare_workflow_patch.py`は合成データ4件で成功（許可差分、既存アクション改変の拒否、Excel URL差分の拒否、ETag・状態の拒否）。
- 2026-09-26の実反映確認：ユーザー承認後、Developer環境の既存フロー`ef881fbc-dcb8-f111-b377-7ced8d3141aa`へCLIから条件付きPATCHを行い、HTTP 204を確認。PATCH前後の保存済み定義を比較し、C02候補以外に差分がないことを確認した。実行したオフライン比較・事後読戻し検査は次のとおり。
  - `python outputs/flow-implementation/prepare_workflow_patch.py --prepare --live-response outputs/t011-20260926/live-workflow-before-c02.json --output-dir outputs/t011-20260926/c02-preflight-20260926`
  - `python outputs/flow-implementation/prepare_workflow_patch.py --verify-readback --readback-response outputs/t011-20260926/c02-preflight-20260926/live-workflow-after-c02.json`
  - 事後読戻し検査は候補定義との一致を確認し、フローがactiveであることもGET応答で確認した。候補SHA-256は`917d7ec981b5490da494d8f5b3f14b58cf6b2ad723870360634d96ab60bbfe1b`。GET応答・ETag・PATCH準備物は`outputs/t011-20260926/`以下に保存し、gitignore対象としている。
- 判定：ローカル検査と稼働中定義への条件付き反映・定義読戻しに加え、更新後フローで3異常ログの個別停止理由・Excel未作成と、正常A/A・run102単独回帰のDataverse・Excel実体一致を確認した。異常＋正常の混在、添付本文読取不能、長文等は未確認のためC02全体は未判定。実機の自動起動コマンドは未設定。

#### T-011：C02更新版の異常ログ3件（1件ずつ）

- 前提確認：2026-09-26、非公開Studioプレビューで開始直前にPAC FetchXMLを照会し、3キーとも重複0件、有効転記先はG/G・D/D・H/H各1件。各件の対象処理日は2026-09-25。ログを1件ずつ保存・開始し、同時投入はしていない。
- `samples/t006/invalid-date.txt`（G/G／実行回1、本文`処理日: 2026-02-30`）：件ID`757e9bb1-73b9-f111-b377-7ced8d3141aa`、添付1件181バイト。期待理由「ログの処理日は暦上存在しない日付です。」。実結果は状態`停止`／Excel`未確認`、理由一致。画面でも停止理由を確認。
- `samples/t006/missing-date.txt`（D/D／実行回1、冒頭日付行なし）：件ID`6f972671-74b9-f111-b377-7ced8d3141aa`、添付1件156バイト。期待理由「ログの冒頭に処理日行（処理日: YYYY-MM-DD）がありません。」。実結果は状態`停止`／Excel`未確認`、理由一致。画面でも停止理由を確認。
- `samples/t006/missing-identifier.txt`（H/H／実行回1、サーバー行なし）：件ID`5a997206-75b9-f111-b377-7ced8d3141aa`、添付1件129バイト。期待理由「ログにサーバーの識別情報がありません。」。実結果は状態`停止`／Excel`未確認`、理由はCLI読戻しで一致。画面は処理中の瞬間のみで、最終理由の表示は未確認。
- Excel未作成の確認：各開始前後にSharePointプラグインでT-006検証フォルダーの項目を読取り、全期間16項目のままで増減なし。3件それぞれの件ID名Excelは0冊。Dataverseの`Excel未確認`だけでは未作成の根拠にせず、フォルダー一覧の読戻しと組み合わせる。フロー履歴の個別アクションは未確認。
- 併せて停止済み`normal-demo-run101`のScreen3をプレビューで読取り、転記照合・判断・コメント欄と「確認を記録」ボタンがdisabledであることを確認。既存の担当者値は変更・消去していない。これは停止件ゲートの画面確認に限り、C05全体の回帰合格ではない。
- 判定（3異常試験を終えた時点）：上記3件は更新後フローで個別の異常理由を記録して停止し、対応Excelを作らない条件を確認。旧B、`normal-demo-run101`、その他既存件は再実行せず、公開もしていない。当時は添付本文の読取不能、更新後の正常経路回帰、および異常件と独立した正常件の混在は未確認だった。正常回帰は次項で後続確認した。先行する日付不一致＋正常件の混在結果は更新前フローの試験であり、更新版の混在合格には転用しない。

#### T-011：C02更新版の正常単独回帰

- 前提確認：2026-09-26、Developer環境の非公開アプリで実施。開始前のPAC FetchXMLでA/A・実行回102・対象日2026-09-26の重複0件、有効な転記先1件を確認。フローはC02更新版でactive。
- 操作：ChromeのStudioプレビューで`samples/t006/normal-demo-20260926-run102.txt`を添付し、1回保存・1回開始した。開始直後は進捗一覧で`処理中`だったが、受付へ移動して一覧へ戻る再読込み後に`ログ保存：保存済み`／`転記処理：処理完了`／`Excel実反映：反映確認済み`へ更新された。担当者欄・レビューは未記録／未依頼。旧Bと`normal-demo-run101`は再実行せず、公開・削除も行っていない。
- Dataverse読戻し：件ID`25ba005c-76b9-f111-b377-7ced8d3141aa`は`転記済み`／`全文一致`でExcel URLあり。annotation ID`26ba005c-76b9-f111-b377-7ced8d3141aa`、添付1件・177バイト。
- Excel実体：SharePointプラグインでT-006検証フォルダーが開始前16項目から開始後17項目へ増加し、件ID名のブック1冊（8265バイト）を確認。Evidence表を取得して9列・1行を照合し、件ID・ログ名・A/A・実行回102・対象日・ログ全文が元ログと一致した。
- 判定：C02更新版の正常単独回帰は合格。これは異常件との同時処理を検証していない。更新版での異常＋正常混在、添付本文読取不能、長文等は未確認なのでC02全体は未判定。

#### T-011：C03・C04の失敗経路ガード（ローカル、実機未確認）

- 対象：`outputs/flow-implementation/build_flow_definition.py`、`test_flow_definition.py`、`flow-definition.excelurl-c02-local-candidate.json`。Power Automateの失敗経路をローカルで検査し、稼働中フローの実機挙動とは分けて評価する。
- 候補の規律：`CopyDriveFileByPath`とExcel `PatchItem`の自動再試行は`retryPolicy=none`。コピー失敗／タイムアウトと、コピー後のExcel書込・読戻し失敗／タイムアウトは、成功扱いにせず`UpdateOnlyRecord`で`結果不明`と再実行保留理由を記録する構造を検査する。
- TDD・ローカル検査：自動再試行の追加、Excel書込の再試行設定欠落、コピー失敗ハンドラーからTimedOutを除外、コピー後失敗を`転記済み`へ変更、二つ目のコピー追加、転記先確認の失敗側へ処理一式を移動する変異を拒否するテストを実施。最後の枝移動は独立レビューで見つかった静的検証器のfalse negativeを修正した後、同等の変異を再実行し、4アクションそれぞれに成功側配置を要求するエラーが出ることを確認した。現行候補が検査を通るpositive controlも維持する。
- 検証結果：`test_flow_definition.py`全24件、`test_prepare_workflow_patch.py`の事前照合テスト4件、対象Pythonファイルの`py_compile`、文書・コードの`git diff --check`が成功。独立再レビューでも枝移動の変異が拒否されることとテストが枝配置理由を直接assertすることを確認した。これはローカルの静的候補検査の合格であり、実機C03の合格ではない。
- クラウド状態と未確認：稼働中フローの定義・アプリ・件データ・Excelは今回変更しておらず、フローも起動していない。実際のExcel副作用、結果不明状態のDataverse更新そのものが失敗した場合、サービス側の実効再試行、C03結果不明経路は未確認。C04の安全な再実行は未実装で、旧Bは結果不明のため再実行しない。
- C04独立監査：現行の件表列・稼働中フローでは安全な再実行は未実装。旧Bは結果Excelが存在するのに件状態`結果不明`であり、再実行・上書きをしない。試行トークンと条件付き更新は候補に留まり、Dataverse APIでの条件付き書込・競合拒否・旧試行応答の排除は未確認。次のゲートは、候補を現行列・APIで非破壊に検討し、書込試験が必要な場合は別途、範囲・副作用・復旧方法を決めて承認を得ること。
- 次の実機候補：隔離した架空件1件で、決定的なExcel書込失敗を1回だけ再現する。失敗注入の仕方・副作用確認・復旧手順と操作範囲を先に定め、個別承認を得るまでは実施しない。旧Bには触れない。

#### T-011：C08保存確認のローカル候補（未完了）

- 対象：基準YAML `outputs/canvas-live-readback-20260926-demo-audit/` とローカル候補 `outputs/canvas-c08-save-readback-candidate-20260926/`。従来版の`Form1.OnSuccess`が関連添付の読戻しなしに保存済み表示へ進んだ所見は、候補作成前の基準監査として記録する。
- 候補で確認する規律：件ごとに結果不明を隔離する。保存済み件と入力不正は送信前に拒否する。保存済み表示へ進む前にDataverse行、関連添付件数1、状態を読み戻す。
- ローカル検査結果：`python -B outputs/canvas-c08-save-readback-candidate-20260926/tests/validate_candidate.py`は最終小修正後にPASS。GPT-6 Luna／maxの独立レビューで指摘された重大分岐は修正済み。実行回カードのUpdateへIfError保護を追加した後の独立再レビューも完了し、新たな問題は見つからなかった。Canvas compile、Canvas Authoring MCPへの送信、Studio保存、実機試験は未実施。
- 未実装・未確認：添付のファイル名・内容一致、安定IDを使った同一件再保存、再起動後の結果不明復元。これらを含むC08全体は未完了。TDDのred→green証拠は未記録。ローカル構造検査と独立レビューを実施済みだが、クラウド保存の正しさを示す証拠にはしない。
- 次の確認：Canvas compileと基準版との差分を確認する。許可された手順でMCP送信・Studio保存へ進める前にその範囲を確認する。実機では保存成功・失敗・結果不明それぞれで行／添付／状態の読戻しと、上記の未実装分岐を別々に検査する。C02の実機結果は変更しない。

#### T-012：本人による正常経路の受入（未実施）

- `samples/t006/normal-demo-run101.txt` の本文は処理日`2026-09-25`、架空環境A／架空サーバーA／実行回101。2026-09-26指定で試した件は日付不一致による`停止`であり、正常経路の合格証拠にしない。停止した件を再度開始しない。画面の`転記照合：一致`・`処理結果の判断：正常`は`Excel実反映：未確認`を覆さない。これらの担当者欄がいつ保存されたかは未確認。
- 正常経路候補`samples/t006/normal-demo-20260926-run102.txt`は、C02更新版の正常単独回帰として前項のとおりAIが1回保存・開始し、件ID`25ba005c-76b9-f111-b377-7ced8d3141aa`のExcel実体まで一致を確認済み。同じ4項目の件は既に存在するため、重複登録を避ける目的でこのキーを再保存・再開始しない。これはAI実施の機能確認であり、本人による受入・操作確認は未実施。本人受入に別キーを使う場合は、開始前に重複と転記先を改めて確認する。
- 正常経路の合格は、件状態`転記済み`、Excel照合`全文一致`、件別Excel1冊の9項目とログ全文の実内容一致、画面のリンクと表示を同じ件IDで確認した場合に限る。画面文言だけ、または担当者欄の`一致`だけでは合格にしない。実API試験と本人の操作は`docs/WORKFLOW.md`の範囲確認とブラウザ利用の事前報告を経て実施する。
- 停止件の担当者記録ゲートは、最新同期版`outputs/canvas-live-readback-20260926-demo-audit/Screen3.pa.yaml`で4つの`DisplayMode`と記録ボタンの`OnSelect`を静的確認する。2026-09-26の修正ではローカルYAML解析とCanvas MCPの6ファイル`Validation PASSED`、Studioの保存済み時刻14:29:23 JSTと修正後の式を確認した。停止件の記録ボタンは画面で無効。正常件・差し戻し件の保存操作と再起動後の表示は未実施であり、今回の静的検査だけでC05全体を合格にしない。既存の担当者欄の表示値は自動消去していない。
- 当該停止件1件のDataverse読取りでは指定日2026-09-26、`停止`／Excel`未確認`／担当者欄`一致`・`正常`が実際の保存値で、画面と一致した。PACの認証キャッシュlockfileにアクセスできない実行では、接続失敗でも終了コード0を返した。`pac org fetch --environment c1719a14-6cac-eb5b-b598-2254af1c64b7 --xmlFile outputs/t011-20260926/normal-demo-run101-read.fetch.xml`は認証にアクセスできる環境で実行し、エラーがなく対象行が返ることまで確認する。FetchXMLの`top`指定はPACのページ指定と衝突したため使用しない。既存の担当者値の保存時点・操作、当該件のExcel実体は未確認。

### T-003の重複試験（検証フローの範囲）

- 架空ログCは`samples/t003/mock-log-c.txt`。検証用Dataverse行ID`8992ebfd-84b8-f111-b377-7ced8d3141aa`と添付ID`8e92ebfd-84b8-f111-b377-7ced8d3141aa`をCLIのFetchXMLで確認済み。正確な照会条件は`outputs/t003-20260925/case-c.fetch.xml`と`attachment-c.fetch.xml`。CLIは認証キャッシュへアクセスできる実行環境を要し、通常サンドボックスではアクセス拒否された。
- フローのC対応式は、編集画面で値が見えただけでは保存済みとしない。保存後に編集画面を再読込し、Cの件IDからCの添付IDへ対応する式が残ることを確認する。最初の編集は残らず、1回目のC実行は添付IDがゼロとなり`NotFound`。Excelコピーと書込はスキップされ、Cの結果ファイルは作成されなかった。再編集後の2回目は実行履歴で成功し、CのExcel1冊の実内容が元ログと一致した。
- 重複試験では、Cの正常な結果ブック`evidence-8992ebfd-84b8-f111-b377-7ced8d3141aa.xlsx`を再送前の基準とする。アイテムIDは`01VTXCECEKQGJMOWYHINALGT27WEDNL2HO`、2026-09-25 11:12 JST時点のサイズは8045バイト、QuickXorHashは`6GPA89jn3jDF8TGlk2cJBDRiuyk=`。ユーザー承認後、同じ件IDで2026-09-25 11:17 JSTに1回再送。実行ID`08584113042339065918689600470CU21`のOneDriveコピーは「宛先ファイルが既に存在しています」で失敗、Excel行更新はスキップ。SharePointプラグインでC結果は1冊、アイテムID・サイズ・ハッシュ・更新日時・本文と識別情報が再送前と一致した。これは**検証フローのファイル上書き防止を確認した結果**であり、完成版の同一件登録防止、結果不明時の回復、過去の正常な証跡全般を合格にしない。
- 初回AはOneDriveで結果ブックのコピー後、Excelの行書込が`BadGateway`となり、旧結果ブックに本文行がないと確認した。確定した書込失敗の具体例として扱えるが、当時の検証フローには件別の失敗表示・回復判定がないためC03を合格にしない。Aの旧ブックはその後の承認済み試験で置換されており、同じ失敗ブックを再照合する方法は未設定。
- [Excel Online (Business)の公式制約](https://learn.microsoft.com/en-us/connectors/excelonlinebusiness/)では、行の書込成功後も実反映に遅れがあり、タイムアウトと再試行で重複し得る。そこで、フローの成功表示だけを合格根拠にせず、件ID・識別情報・全文を対象Excelから読み戻す。読戻し不一致や読出不能は結果不明として記録し、再書込しない。[フローのアクション設定](https://learn.microsoft.com/en-us/power-automate/flows-designer)のタイムアウト値は個々の要求のタイムアウトを変えないため、短い値に設定しても結果不明の安全な再現方法にはならない。クラウドの障害注入方法は未設定・未実施。
- 完成版の架空件BではExcel表の1行と元ログ全文が一致した一方、直後のExcel Online (Business) `GetItems`はHTTP 200／`value=[]`だった。修正版は、書込を繰り返さず、初回0行のとき10秒後に読取り専用のGetItemsを1回追加し、1行の9項目が一致したときだけ成功、なお空・複数・不一致・取得失敗なら`結果不明`を維持する。`python -m unittest discover -s outputs/flow-implementation -p test_flow_definition.py`の13件、`py_compile`、`python outputs/flow-implementation/build_flow_definition.py`の静的検査は合格。独立したGPT-6 Luna／maxレビューでも重大な論理欠陥なし。既存フローの`clientdata`更新はCLIでHTTP 204、再読取でローカル候補との文字列一致・有効状態を確認した。修正版を別の架空件Cで1回実行し、件状態`転記済み`／`全文一致`、Excel1冊・1行の9項目とログ全文、画面再表示の`処理完了`／`反映確認済み`を確認。旧Bは再実行・上書きせず、状態も維持。C1件の成功だけではキャッシュ回避の一般性、異なる環境の複数件、長文・異常・再実行を合格としない。

次のT-003試験は、既存の正常なA・B・Cを変更しない独立した架空件で行う。実行コマンドと障害注入方法は未設定、以下は確認観点であり未実施。

| 観点 | 試験前の確認 | 期待結果・証拠 |
| --- | --- | --- |
| 本文日付・識別情報の異常と正常件の混在 | 別の架空件を使い、正常件のExcelと件別状態を基準として記録。本文形式と事前登録の具体値はT-004で決める。 | 異常件だけ理由付き停止、正常件は継続。異常件のExcel未作成と正常件の実内容を確認。現在の検証フローには日付検査・複数件の独立処理がなく、方法未設定・未実施。 |
| Excel作成・書込の確定失敗 | 新しい架空件だけを対象に、失敗を再現する設定と戻し方を事前に確定。A・B・Cの結果ブックは対象から外す。 | 当該件を反映済みとせず、コピー・書込・状態のどこで止まったか記録。原因を直して再実行する前に、既存結果がないことを実物で確かめる。再現方法未設定・未実施。 |
| 書込結果不明 | 正常な証跡を残したまま結果だけ不明にできる方法を先に確定する。確認できない限り、クラウド試験を始めない。 | 自動再試行せず、件別状態と実Excelを照合してから扱いを決める。T-004で状態分岐を設計し、その判定はTDD対象とする。障害注入方法未設定・未実施。 |
| 失敗件だけの再実行 | 先の実行が終了し、固定先に対象Excelがないと確定した失敗件だけを再実行する。空・途中・不一致・書込結果不明では再実行できないことも確認。 | 対象件だけ1冊の結果を作り、他件のID・内容・ハッシュが維持される。実行方法・対象版・結果は未設定、未実施。 |

## 段階ごとの確認

| 段階 | 確認 | 合格の扱い |
| --- | --- | --- |
| T-001～T-003の方式検証 | 複数選択・件別保存、1件1冊のExcel実反映、失敗・結果不明・重複・再実行 | 方式の可否を示す証拠。完成品のC条件合格ではない。 |
| T-008～T-010の画面モック | 対象画面と影響する既存画面の文言、状態、欠け・重なり、仮操作 | 見た目と仮操作のみ。本処理・保存の合格ではない。 |
| 本処理結合後 | 変更した処理と保存・読出し、関連C条件・TR検査、影響画面 | 自動検査と実機確認を別々に判定。 |
| T-011全体確認 | C01～C06・C08～C10と関連TR検査・回帰を実機で確認 | 必須条件すべてを対象版・証拠付きで判定。 |
| T-012受入 | C07を作者の端末と架空データで相手が操作 | 操作・Excel・限界説明の受入を別記録。 |

実行できる機能ができた後は、新しい実装前に、その時点で実装済みの画面の表示と架空1件の主要操作を短く試す。SCR-02・SCR-03が実装済みなら一覧から件別画面への移動も確認し、4画面すべてはT-010後に対象とする。現状のT-001フォームでは別行への2件保存と各1添付に加え、件別一覧の入力保持・修正・取消、残件1件の添付本文一致まで確認済み。完成画面としての主要操作・回帰確認は未実施。

### T-001検証フォームの確認手順

検証用アプリとDataverseテーブルが接続されている場合、Canvas Authoring MCPの`list_data_sources`で`T001検証ログ受付`が`CdsNative`か、`get_data_source_schema`でログ名・環境名・サーバー名・実行回数・対象処理日・`{Attachments}`が見えるか確認する。ローカルYAML変更は同MCPの`compile_canvas`に`C:\dev\Powerapps_gas\log-evidence-app`を渡し、`Validation PASSED`と診断なしを確認する。これはデータの実保存を証明しない。

実機ではStudioの非公開プレビューで架空`.txt`2件を同時選択し、「検証用に件を並べる」で一覧化する。A・Bそれぞれに環境名・サーバー名・実行回数・対象処理日を別値で入力し、A→B→Aで全4項目と添付名がその件に戻ることを確かめる。Aの値を修正して再度切り替え、修正値がAだけに残ることを確認する。Aを保存前に取り消し、一覧にBのみ残ってBの4項目と添付が変わらないことを確認する。Bを1件保存し、保存受付と一覧からの除外、DataverseのBの1行・4項目・添付1件、取り消したAの新規行0件を照合する。2026-09-25にはBの保存行ID`c1cc3ee4-75b8-f111-b377-7ced8d3141aa`をCLIの`pac org fetch --xml`で読み取り、`annotation`のB添付1件と、`documentbody`のBase64を元の架空`.txt`100バイトのBase64と比較して完全一致した。本文はログや画面に出さず、比較結果だけ残す。CLIは認証キャッシュにアクセスできる実行環境が前提で、通常サンドボックスの成功を推測しない。アプリからのBの日付読み戻しは`2026/09/23`、CLIのUTC表示は`2026/09/22`。Studioの列設定は「日付のみ」書式と「ユーザー ローカル」調整の組合せ。完成版で時刻帯に左右されない日付を使うかはT-004で設計し、現在の列設定を完成条件の合格根拠にしない。

入力不正の保存拒否は、架空ログAの環境名を`検証環境A・失敗試験`とし、実行回数に整数列の上限を1超える`2147483648`を入れて保存操作する。範囲エラーと固定文言「保存できませんでした」がAの一覧項目と画面下部に表示され、Aの入力・添付・待ち状態が残り、CLIのFetchXMLで同環境名の行が0件なら、この**入力不正の経路だけ**確認済みとする。2026-09-25に実施済み。通信・Dataverse側の失敗と結果不明は別試験であり、方法未設定・未実施として`docs/TASKS.md`へ記録する。

## 技術上の規律の検査項目

2026-09-25のユーザー指示により、TR-03のアプリ外の全書込経路での空値拒否は今回の検査対象から外した。先のAPI試験で欠落値POSTが受理された事実は履歴として残す。完成版では、画面経由の未入力保存拒否、保存先の代替キーによる同じ4項目の2件目拒否、結果不明時の再送停止を別々に確認する。

規律の条件・理由は`docs/REQUIREMENTS.md`の「技術上の規律」を参照する。ここでは確認操作と合格判定だけを記す。自動検査の実行方法・対象パスは未設定。機械で判定できる部分はTDDの対象とし、クラウド保存とExcel実内容は実機でも確かめる。

| ID | 確認方法の下書き | 合格条件 | 現在 |
| --- | --- | --- | --- |
| TR-01 | 架空2件の識別・入力・Excel参照を照合。完成版の`Evidence`表は1件1冊で、短文でも`ChunkIndex=1`、`ChunkCount=1`を持つ。1セル30,000 UTF-16コード単位の境界とExcel上限の前後（32,767／32,768）、改行、Unicode・サロゲートペアを含む本文を分割・復元する判定はTDD候補、実Excelは実機。全行の件ID・4項目・番号・件数と連結本文を元ログと比較する。 | 件ごとに本文全文・識別情報・Excelが対応し、長文でも欠落・重複番号・順序違い・改行変更・他件混入がない。書込応答だけで実反映済みにしない。 | 短文の方式検証のみ実施。完成版の表・分割処理・検査コマンド・長文実機確認は未設定、未実施。 |
| TR-02 | 保存、処理、実反映、人の確認、レビューを別々に変化させて状態を照合。保存先に件の行だけあり添付がない／読めない場合は転記へ進まないこと、同じ件への後続の失敗応答が先に確認済みの実反映や人の記録を上書きしないことも調べる。状態遷移は自動テスト、保存先・Excel実内容は実機。 | 行だけの不完全な保存や処理応答だけで成功表示せず、結果不明を成功表示しない。後続の失敗で確定済みの別の根拠を失わない。 | 方法詳細未設定、未実施 |
| TR-03 | 環境・サーバー・実行回・対象処理日の4項目が同じで元ファイル名と追跡用IDだけ異なる2件を登録し、2件目が保存先でも拒否されるか確認。Dataverse代替キーを採用する場合は、アプリ経路で4項目の空値を拒否すること、キー状態`Active`、同時保存の2件目の拒否を確認する。アプリ外のAPIから欠落値を書けないことは今回の合格条件に含めない。保存済み件の開始前修正で別件と重複する場合も確認し、拒否後の元の件・添付・状態と、新規拒否件の添付・Excel未作成を調べる。保存済み件を開始前取消し、同じ4項目で元の件を再開してログを差し替え、件IDが不変で有効な添付が1件と確定するまでは開始できないことも確認。差し替え保存・有効添付切替の失敗と応答不明では、件の状態と有効添付ID、新旧ログの実物、フロー非起動を読み直す。回復操作と期待値はT-004の論理設計とSCR-01～SCR-04の画面仕様を参照し、技術的な実行手順をT-007で確定する。開始受付と修正・取消・差し替えを近い時刻に試し、古い版の更新と二重開始が拒否されるか確認する。さらに開始受付の応答を不明にした場合、保存先への照会で受付の有無が確定するまで修正・取消・差し替え・二度目の開始が保留されるかを確認する。書込結果不明、失敗件の再実行も再現する。重複判定・状態遷移・再実行許可は自動テスト、保存先・過去Excelは実機。 | 4項目が同じ2件目と重複する修正を受け付けず、既存件を壊さない。取消後のやり直しは元の件IDを使い、差し替え前後でログを取り違えず、2件目とExcelを作らない。差し替え応答不明を取消維持や差し替え成功の証拠にせず、状態と有効添付を確定できるまで開始も自動再送もしない。開始受付が確定した件は後続の修正・取消・差し替え・二重開始で変わらない。受付結果不明の間も操作を進めず、未受付が確定した場合だけ開始前操作へ戻す。新規拒否件の添付・Excelを作らず、重複転記・結果不明時の自動再試行・過去証跡消失がない。 | 業務上の判定基準と取消後の扱いは2026-09-25に確認済み。件表の4項目代替キーは実表で`Active`を確認。T-007では、既に保存された正常行と4項目が同じ1件の作成要求をHTTP 412で拒否することを確認したが、環境列を欠く要求はHTTP 201で受理され拒否条件は失敗。残るサーバー・実行回・日付の各欠落要求、近接・同時保存、条件付き更新は未実施。対応表の2項目重複拒否は確認済み。 |
| TR-04 | 冒頭行`処理日: YYYY-MM-DD`が正常／欠落／読取不能／形式不正／存在しない暦日／指定日と不一致の架空ログ、保存失敗、識別情報異常、開始前取消を正常件と混在させる。UTF-8 BOMの有無とLF／CRLFでも最初の行を正しく読み、全文の元の改行を変えない技術案を検査する。日付判定はテストを先に作り、継続とExcel未作成は実機。既存A・B・Cは冒頭行がないため本検査に流用しない。 | 問題件だけ理由付きで止まり正常件は続く。保存失敗・取消状態の間にある件は転記されない。 | 日付形式は2026-09-25に確認済み。文字コード・改行の扱いは技術案であり、検査コマンド・実機結果は未設定、未実施。 |
| TR-05 | 転記照合、判断・コメント、依頼、結果、差し戻し理由、再依頼の保存・表示を確認。差し戻し後は担当者の3記録だけ修正・保存でき、保存前の再依頼、ログ・4項目・既存Excelの変更、確認済み後の訂正を拒否するか調べる。結果保存後にSCR-02へ戻る際に担当者役へ戻り、次回のレビューではSCR-04で再切替できるか確認。再依頼後に古い依頼画面から回答を保存しようとしても、現在の依頼結果を上書きしないことを確認。入力条件、依頼版と確定後の更新拒否はTDD候補。 | 人の結果が自動確定されず別記録として保存され、保存失敗を結果済みと表示しない。差し戻し後の修正範囲と保存順、実演上の役割の戻し方を守り、確認済み後の訂正はできない。直近の差し戻し理由は再依頼時にも参照でき、古い依頼への回答は拒否される。 | 方法詳細未設定、未実施 |
| TR-06 | 開始操作→保存先の件・添付・事前登録の読直し→転記先が一つに確定した件だけ条件付きの開始受付・添付IDと版または内容の検査値、転記先の固定→検査→Excel作成・転記→件別状態→画面表示で対応と順序を追う。未登録の環境・サーバーは開始未受付で当該件だけ停止し、開始後に添付内容が変わった件も当該件だけ停止する。画面から渡した未確認の本文や保存先パスで転記しないことも確認する。開始後に事前登録の保存先を変えて同じ件を再実行し、初回の固定先以外に2冊目を作らないことも調べる。受渡し条件は自動テスト候補、結合は実機。 | 前段の必要な結果なしに後段を成功扱いせず、事前登録・固定した添付と転記先、件・Excelが対応する。対応変更後の再実行でも1件1冊を維持し、他件は継続する。 | 方法詳細未設定、未実施 |

TR-02・TR-03の再試行検査では、先のフローが終了し、コピー／書込の未着手または副作用なしの確定失敗、固定先のExcel未作成が確認できた件を用いる。同じ件ID・開始要求ID・固定添付・固定先を保って試行IDだけ進むこと、近接した再試行要求の片方が拒否されること、受付応答不明の間は再要求されないことを自動試験で確かめる。新試行の開始後に旧試行の成功・失敗・タイムアウトを遅延到着させ、新試行の状態と既に確認済みのExcel実反映・人の記録が上書きされないことを確認する。Excelが空・途中・不一致、外部処理が未終端または結果不明の場合は試行IDが進まず、コピーも始まらないことを確認する。実行ID・試行ID・件の版、固定先のExcel一覧と実内容を証拠にする。自動検査コマンドとクラウドでの安全な再現方法は未設定・未実施。

TR-06の事前登録検査では、環境・サーバーの有効な対応先がない件と正常な別件を同時に開始する。未登録件は開始受付ID・固定先・フロー実行・Excelを持たず理由付きで止まり、正常件だけ受付と処理が進むことを確認する。混在時はSCR-02で未登録件が「未処理」と理由で示され、「受付へ戻る」でSCR-01へ戻れる。戻っても受付済み件は編集不可。全件が未登録ならSCR-01に留まる。登録を是正した後の未登録件は**再実行ではなく初回開始**として受け付け、添付と新たに解決した先を固定する。一方、受付済み件の対応表を変更しても固定先を変えない。対応表の読出し失敗や変更が競合した場合は、複数の転記先に進まず確定まで保留する。状態分岐は自動検査、画面遷移・固定先・Excel実内容は実機で確認する。検査コマンド・実機結果は未設定・未実施。

TR-02・C08・C09の受付画面検査では、処理対象0件と全件取消後に「処理開始」が操作不可で、フローとExcelが作られずSCR-01に留まることを確認する。件ごとの「保存」は「処理開始」と別操作であり、保存だけでは開始受付・フロー・Excelが作られないことを確認する。保存失敗が確定した件は同じ件IDで再保存できる。保存要求の返答だけが途切れた件は「保存状況を確認しています」と表示し、その件の開始・再保存を止める。元の要求が未終了の間、保存先に行だけある場合、行と添付が一致する場合、元の要求が終了し行・添付なしが確定した場合を分ける。確定しない間は二重保存せず、成功表示もしない。状態分岐はT-008のTDD、保存先の照会と実画面は架空件で確認する。自動検査コマンドと安全な応答不明の実機再現方法は未設定・未実施。

**C08の件IDライフサイクル検査案（技術案）**：初回保存要求を送る前に割り当てた件IDを保持する。元要求の終了後、保存先にその件の行と関連添付がともにないことを確定してから、別ログを選び直し、同じ件IDで再保存する。再保存後は保存先から同じ件IDと選び直したログの添付との対応を読み戻し、一致を確認する。行だけある、添付がない・不一致、または読出不能の場合は結果不明のまま保留し、開始・再保存を行わない。この順序はC08を検査するための技術案で、実装方式としては未確定。今回のT-007実API試験ではこのシナリオを確認していない。安全な照会方法・実装・自動検査方法／コマンドは未設定で、検査は未実施。新方式の実API検証は対象・範囲を別途承認後に行う。

T-008・C10の再起動復元検査では、アプリを閉じて開き直し、保存済み件の4項目・ログ・件IDと保存済みの進捗が復元し、未保存の選択・入力は破棄されることを確認する。進行中の件が1件以上ならSCR-02、それ以外はSCR-01を開くことを確認する。永続化された値と再起動前後の画面・状態を照合し、モックのセッション内状態だけを合格根拠にしない。自動検査と実機の実施方法は未設定・未実施。C10の追加実証は今回承認された最大8件作成・読取・条件付き更新CLI試験の範囲に含まれない。

**C08+C10合成検査（確定保存失敗後の再起動）**：初回保存前に割り当てた件IDを記録し、保存要求の終了と保存先に行・添付がないことの両方を確認して確定失敗にする。その状態でアプリを再起動し、旧ログ選択と4項目入力は復元されず、同じ件IDと確定失敗状態は復元されることを確認する（復元の技術方式は未確定）。新しいログを選び4項目を再入力して同じ件IDで再保存でき、保存先の行と添付を読み戻して件ID・新ログとの対応が一致することを確認する。保存結果が不明な場合は再起動前後を問わず再保存を禁止し、行のみ、添付欠落・不一致、照会不能も確定失敗として扱わず保留する。この合成検査の実装・自動検査方法／コマンドは未設定で、実装・検査は未実施。新方式の実API試験は別途承認が必要。

TR-05・C06のレビュー検査では、Excel実内容確認前、担当者の照合だけ保存済み、判断・コメントだけ保存済み、両方保存済みを作り、最後の状態でだけ依頼を受け付ける。照合「不一致」や判断「異常」「判断保留」でも両記録が揃えば依頼できる。SCR-02の「レビューを確認」からSCR-04を開き、同じアカウントで「レビュアー役に切り替える」前には結果を記録できず、切替後に確認済みまたは理由付き差し戻しを保存できることを実画面で確認する。切替を別人の権限制御の証拠としない。依頼条件と古い依頼版の拒否はT-010のTDD、保存・再表示と遷移は架空件で実機確認する。自動検査コマンド・実機結果は未設定・未実施。

TR-03・TR-04とT-008の入力検査では、事前登録済みの環境・サーバーの組合せだけを候補に出し、環境を変えた後に旧サーバーが残っても有効と判定しない。保存後に対応登録を無効にした件は開始前に止め、正常な別件は続ける。実行回は初期空欄で、1以上の整数だけ保存候補とし、空欄・0・負数・小数・文字列を拒否する。保存先の整数上限を超える値も成功扱いしない。候補更新・拒否と4項目の一意性は自動検査、実候補と保存結果は架空件で実機確認する。コマンド・対象版・結果は未設定・未実施。

TR-05の担当者コメント検査では、判断「異常」「判断保留」かつ空欄では確認の記録とレビュー依頼を拒否し、コメント保存後に依頼できることを確認する。「正常」では空欄でも判断・コメント欄の保存が確定すれば依頼できる。転記照合と処理判断を混ぜず、再表示した値を照合する。条件分岐はT-010のTDD、保存・再表示は架空件で実機確認する。コマンド・結果は未設定・未実施。

## 完成条件C01～C10の実機確認

正確な操作と期待結果は`docs/REQUIREMENTS.md`の「確認用の完成条件案」を参照する。画面文言と操作は個別画面仕様を参照する。

| 条件 | 操作と確認する結果 | 証拠の例 |
| --- | --- | --- |
| C01 | 正常な複数件を開始。1件1冊のExcelを開き直し、全文・識別情報と実反映状態を照合。 | SCR-02、件別Excel、件IDと版。 |
| C02 | 冒頭の`処理日: YYYY-MM-DD`が正常／欠落／読取不能／形式不正／指定日と不一致の件、および本文・識別情報異常を混在。異常件だけ理由付き停止し、独立した正常件は続く。 | 件別状態と理由、Excelの対応。 |
| C03 | Excel作成・書込失敗と結果不明を再現。誤った反映済み表示と自動重複転記なし。 | 状態、処理履歴、Excel実内容。 |
| C04 | 同一件の再登録を拒否。旧試行が終わり、コピー／書込の副作用が残らず、固定先でExcel未作成と確定した失敗件のみ処理の原因を直して新試行で再実行する。件ID・ログ・4項目・固定先は維持し、対象件だけ新しいExcelを作る。二重再試行と旧試行の遅延応答を拒否する。空・途中・不一致のブックがある件、書込結果不明の件は確認待ちで止め、再実行されないことも確認。重複・過去証跡消失なし。 | 件ID・試行ID・フロー実行ID、再実行の許可・拒否、前後のExcel一覧と実内容。 |
| C05 | 担当者役が転記照合と人の判断・コメントを別々に保存。アプリがバッチ成否を自動断定しない。 | SCR-03と再表示後の値。 |
| C06 | 1アカウントで依頼、役割切替、確認済み、理由付き差し戻し、担当者の3記録の修正保存後の再依頼、確認済み後の訂正拒否。ログ・4項目・Excelが不変で、別人レビュー・実権限制御の達成と表示しない。 | SCR-02～SCR-04、件別記録とExcel実内容。 |
| C07 | 作者の端末で相手が架空データを操作し、Excelと限界説明を確認。 | 操作・受入記録。 |
| C08 | ログ保存失敗を再現し「保存できませんでした」を表示、転記なし。失敗が確定した件は同じ件IDで再保存でき、結果不明中は再保存・処理開始を行わない。 | SCR-01、同一件IDの読戻し、Excel未作成。 |
| C09 | 開始前の修正・取消、取消状態の間は処理・Excel作成なし。同じ件を再開し差し替え保存が確認できた後の処理、開始後の進行中取消なし。開始受付の返答が途切れた件は「開始状況を確認しています」を表示して4操作を保留し、受付済み／未受付／判定不能の照会結果ごとに承認済みの遷移を確認。 | SCR-01～SCR-02、件ID・添付、Excel一覧。 |
| C10 | アプリを閉じて再度開く。保存済みの件・進捗は復元され、未保存入力は破棄される。進行中の件があればSCR-02、なければSCR-01を開く。 | 再起動前後のSCR-01／SCR-02、保存先の件ID・ログ・4項目・進捗状態。 |

## TDDの適用と代替確認

TDDは期待結果のテストを先に作り、想定した失敗を実行で確かめてから処理を実装し、成功と回帰を確認する進め方。下表には今後予定する項目があり、そのテスト資産・コマンド・結果は未設定／未実施。T-007 Phase Bの実施結果は下表に記録する。採用方式で自動実行できない場合は理由と代替方法を実装前に記録して判断を受ける。

| 対象 | TDDと理由 | 代替・追加確認 |
| --- | --- | --- |
| T-008：件別入力の妥当性、件ごとの保存と処理開始の分離、保存失敗時の同一件IDでの再保存、保存結果不明時の保留、開始前取消、条件付きの開始受付、C10の再起動復元 | 適用。入力・保存・取消・開始の境界、保存／開始応答不明時の操作保留、再起動時の復元分岐を機械で判定できるため。 | 実際のファイル選択・保存結果と競合時の拒否、保存結果不明後の照会、再起動後の保存先読戻しとSCR-02表示は保存先・実画面で確認。 |
| T-009：日付照合、件別停止、状態遷移、重複判定・再実行許可、長文の分割・復元 | 適用。異常値・結果不明の条件分岐と全文保持に回帰リスクがあるため。 | フロー接続とExcel反映は架空データで実機確認。 |
| T-010：転記照合と人の判断の別保存、レビュー入力条件・結果保存 | 適用。保存の分離と理由必須を機械で判定できるため。 | 役割表示、4画面の遷移、再表示後の記録を実機確認。 |
| T-001～T-003：方式の探索的検証 | 適用しない。実環境で方式の可否を調べる作業だから。 | 架空データで実操作し、版・結果・証拠・制約を記録。 |
| T-004～T-006：設計・文書・データ準備 | 文書設計と準備自体には適用しない。 | 要件・画面仕様・規律の照合、参照・差分、架空データ確認、独立レビュー。 |
| T-007 Phase Bの同期プラグイン候補 | 適用・ローカル実施済み。4項目の欠落・null・空文字・空白、実行回の0／-1、Create／Updateの値合成を機械判定できるため。 | `.\dataverse-plugins\run-local-tests.ps1`。初回red 5/6→green 11/0、独立レビューHigh 2件を再現・修正、最終16/0。SDK・fake contextを使うnet48ローカル結果に限る。証拠と未確認境界は上のT-007 Phase B節を参照。新しい専用クラウド検証表、登録・展開、API試験は別途承認後。 |
| UIモックの余白・文字・配置 | 適用しない。見た目の調整だから。 | 対象と影響画面の文言・欠け・重なり・主要仮操作をブラウザで確認。 |
| クラウド接続・Excel実反映、T-011・T-012 | 単体ロジックのTDDだけでは合格にしない。 | C01～C10の実機再現とExcel実内容、C07の相手の操作・受入を確認。 |

## 実行時の記録

条件IDとTR検査IDごとに合格／不合格／未確認／対象外、対象版、日時、OS・ブラウザ、担当、操作、期待結果・実結果、証拠を`docs/TASKS.md`へ残す。対象外には理由・判定者・日付を付ける。実施できなければ理由、未確認範囲、影響、担当、次の行動を残す。正式な不合格回数と停止条件は`docs/WORKFLOW.md`の「評価・修正・停止」を参照し、ここに重複した回数規則を作らない。
