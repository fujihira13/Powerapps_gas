# 受付・対象確認：画面仕様の準備資料

## 状態と位置づけ

- 状態：**詳細仕様の下書き、未承認**。B案の単独参考画像は作成済み。画面数、正式な配置・文言・遷移、実装方式、実画面の受入は未決定・未実施。T-004の状態設計と技術検証に基づく確定は未実施。
- 本書は `docs/REQUIREMENTS.md` の「画面に必要な役割と情報」にある「ログの受付・対象確認」を引き継ぐ正式な個別仕様の保存先。1つの独立画面とする決定ではない。T-005 で役割の分け方を決めた際は、対応関係と承認履歴を本書へ追記する。
- 共通の判断は [UI共通ルール](../rules.md) に従う。製品要件と承認経緯は [要件定義](../../REQUIREMENTS.md)、作業状態・評価履歴は [作業一覧](../../TASKS.md) を参照する。要件・履歴を本書へ移して削除しない。

## 引き継ぐ役割と固定事項

| 区分 | 内容 | 根拠 |
| --- | --- | --- |
| 受付 | 担当者役が Power Apps で複数の取得済み架空ログを選び、作品が指定場所へ保存する。 | REQUIREMENTS「主要操作」1、U01、T-001 |
| 件別の確認 | ログごとに環境・サーバー・実行回・対象処理日を確認する。対象処理日は操作当日の日付で代用しない。 | REQUIREMENTS「主要操作」1、R01・R13 |
| 開始前 | 処理開始前は選択と入力を修正・取消できる。担当者役が対象を確認して処理を開始する。取消した件は処理せず、証跡 Excel も作らない。 | REQUIREMENTS「主要操作」1～2、U02、C09 |
| 保存失敗 | 保存に失敗したと分かった件には、確定した文言 **「保存できませんでした」** を表示し、その件を Excel へ転記しない。 | REQUIREMENTS「決定」、C08 |
| 開始後 | 進行中の処理を取り消す操作は設けない。件ごとの完了・失敗・結果未確認を追えることは後続の進捗の役割とつなぐ。 | REQUIREMENTS「主要操作」4、U02、C09 |
| 対象外 | 実務ログ・顧客情報・実務の証跡 Excel を使わない。画面に未承認の下書き再開機能や、メール・Teams 通知を加えない。 | REQUIREMENTS「今回は対象外」「仮定」、T-001 |

ここで固定したのは既存の要件・個別回答・完成条件である。表にない表示文言、ボタン名、画面数や見た目を決定したことにはならない。

## 画面ID・対応要件

- **SCR-01**。対応要件：**REQ-01、REQ-02、REQ-04**。対応完成条件：**C01、C08、C09**。画面IDはこの下書きの追跡用で、4画面構成は未承認。

## 目的

取得済みの架空ログを複数受け付け、件別情報と保存結果を確認してから処理を始める。担当者役が使用する。

## 表示

- 必要：選んだ件を識別できる情報、環境・サーバー・実行回・対象処理日、件別の保存結果。失敗が判明した件には固定文言「保存できませんでした」。
- この画面には不要：Excel本文・実反映の詳細、担当者判断、レビュー結果。これらは後続画面で扱う。開始後の進行中取消操作も置かない。

| ID・種別 | 正確な文言／値と承認状態 | 初期値・表示元／形式 | 制限・表示条件 | 操作結果 | 成功／失敗表示・遷移 |
| --- | --- | --- | --- | --- | --- |
| S01-D1 見出し | **未決定**。推奨「受付・対象確認」〔提案〕 | 固定文字。初期値は見出しに該当しない | SCR-01を表示する場合だけ | 表示専用で操作なし | 静的表示なので成功・失敗と遷移は該当なし |
| S01-D2 ログ名 | ファイル名は動的値。見出し「ログ」は〔提案〕 | 画面で選ばれた架空ログの元ファイル名をそのまま表示する案。選択前は項目なし | 選択した件ごとに表示。文字種・長さは未決定 | 表示専用。件の選択による詳細表示はS01-A2 | 保存成功・失敗にかかわらず件との対応を維持。画面遷移なし |
| S01-D3 保存結果 | 成功時の正確な文言は**未決定**。失敗が判明した件の「保存できませんでした」は**確定** | 件別の保存処理結果から表示。失敗文言は当該件の近くに置く案 | 保存結果が判明した時点でその件に表示。結果不明時の文言・処置は未決定 | 表示専用で操作なし | 成功時の表示は未決定。失敗時は当該件を転記せず、SCR-01にとどまる案 |

## 入口と出口

- 入口：担当者役が新たな取得済みログの受付を始める場合。アプリ起動直後の画面と決めていない。
- 出口：処理開始後に **SCR-02 進捗一覧** へ進む案。開始前の修正・取消中はSCR-01にとどまる。全件取消時、保存結果不明時の出口は未決定。

## 操作

| ID・種別 | 正確な文言と承認状態 | 初期値・表示元／形式 | 制限・表示／操作条件 | 操作結果 | 成功／失敗表示・遷移 |
| --- | --- | --- | --- | --- | --- |
| S01-A1 ログ選択操作 | ボタン等の正確な文言は**未決定**。推奨「ログを選ぶ」〔提案〕 | 入力初期値は該当なし（操作）。選択件は0件から始める案 | 取得済みの架空ログを複数選ぶ。ファイル形式・件数・サイズ制限は技術確認待ち | 選択した件を一覧へ加え、件別情報の確認へつなぐ | 選択成功：SCR-01で件を表示。選択失敗：文言・扱い未決定、SCR-01にとどまる案 |
| S01-A2 件の選択 | 画像のチェックボックスは仕様未確定。推奨は一覧の件を選んで詳細を切り替える操作〔提案〕 | 初期選択件は未決定 | 選ばれた件が存在するとき。処理対象への含除と詳細表示の選択を同じ操作にするか未決定 | 選択中の件の入力欄を表示 | 成功：SCR-01内で詳細を切替。失敗：対象件がない場合の文言未決定、遷移なし |
| S01-I1 環境 | 項目名「環境」は要件の用語。画面上の正確なラベルは〔提案〕 | 初期値未決定。推奨は未選択から担当者が確認。値の表示形式・候補元はT-004待ち | 件ごとに必要。転記先との事前登録対応を壊す値は使わない。選択方式・制限は未決定 | 対象件の環境を設定・修正 | 有効時：SCR-01に保持。無効時：具体文言未決定、開始させずSCR-01にとどまる案 |
| S01-I2 サーバー | 項目名「サーバー」は要件の用語。正確なラベルは〔提案〕 | 初期値未決定。推奨は未選択。候補元・形式はT-004待ち | 件ごとに必要。環境との組合せ制限は未決定 | 対象件のサーバーを設定・修正 | 有効時：SCR-01に保持。無効時：文言未決定、開始させずSCR-01にとどまる案 |
| S01-I3 実行回 | 項目名「実行回」は要件の用語。正確なラベルは〔提案〕 | 初期値未決定。例の「1」は固定値ではない。形式・値域はT-004待ち | 件ごとに必要。重複判定との関係はT-004待ち | 対象件の実行回を設定・修正 | 有効時：SCR-01に保持。無効時：文言未決定、開始させずSCR-01にとどまる案 |
| S01-I4 対象処理日 | 項目名「対象処理日」は要件の用語。正確なラベルは〔提案〕 | 初期値未決定。推奨は当日を自動入力せず未指定。表示形式は「YYYY-MM-DD」案 | 件ごとに指定する。本文の日付と比較する。許容日付・形式は未決定 | 対象件の日付を設定・修正 | 有効時：SCR-01に保持。形式不正等は理由の文言未決定、開始後は該当件だけ停止する要件を維持 |
| S01-A3 修正 | ボタン文言「修正」は〔提案〕。開始前の修正という意味は**確定** | 入力初期値は該当なし | 処理開始前の件だけ操作可能。保存後の修正方式は未決定 | 選択・件別情報を修正 | 成功：SCR-01で変更を確認。失敗：文言・保持状態未決定、SCR-01にとどまる案 |
| S01-A4 取消 | ボタン文言「取消」は〔提案〕。開始前取消の意味は**確定** | 入力初期値は該当なし | 処理開始前のみ。開始後の進行中取消は表示・操作させない | 該当件を処理対象から外し、その件のExcelを作らない | 成功：SCR-01に残る件を表示。仮置きログの片付け失敗・結果不明はT-004待ちで、表示・遷移は未決定 |
| S01-A5 処理開始 | ボタン文言「処理開始」は〔提案〕。対象確認後に開始する意味は**確定** | 入力初期値は該当なし | 保存失敗件は転記対象にしない。0件時、保存結果不明時の操作条件は未決定 | 対象を確定してREQ-02の件別照合・処理へ渡す | 開始受付成功：SCR-02へ進む案。開始失敗：正確な文言・状態は未決定、SCR-01にとどまる案 |

## 入力

入力欄は上表の **S01-I1～I4**、ログ選択は **S01-A1**。各欄の初期値、制限、失敗時の文言・遷移は上表に示し、未決定のものを画像の値で補わない。

## 状態

| 状態（画面上の文言ではない） | 入口条件 | 表示・可能な操作 | 次の状態・遷移 |
| --- | --- | --- | --- |
| 未選択 | 受付を開き、選択件がない | ログ選択操作。0件時の説明文・開始可否は補足候補で未承認 | 選択後に件別確認。全件取消時の表示は未決定 |
| 件別確認中 | 1件以上を選択し開始前 | 件ごとの4項目、修正・取消、保存結果 | 保存成功件を含む対象を確認して開始。保存失敗件は転記対象から外す |
| 保存失敗判明 | その件の保存失敗が確定 | その件に「保存できませんでした」。他の独立件は残す | 失敗件を転記しない。再選択・再保存の動作は未決定 |
| 開始後 | 処理開始を受け付けた | 進行中取消なし | SCR-02への移動を提案。件別の完了・失敗・結果未確認はSCR-02で追う |

## 処理と遷移

- **確定**：件ごとの対象処理日とログ本文日付を照合し、不一致・欠落・読取不能等は当該件だけ理由付きで止める。他の独立件は続く。取消件・保存失敗件のExcelは作らない。
- **未決定**：画面からの保存方式、保存の成否が分からない場合、開始前の仮置きログの片付け、0件時、画面間の自動遷移。推奨は失敗・結果不明を成功扱いせず、対象件を確認できる場所に留めること。採用と正確な文言は別途確認する。

## 動的な値・翻訳

| 値 | 表示元 | 形式・表示条件 | 固定文言との区別 |
| --- | --- | --- | --- |
| ファイル名 | 利用者が選んだ架空ログの元ファイル名 | 選択した件ごとに表示。長さや省略方法は未決定 | 値は動的。画像の`log_001.txt`等は固定しない |
| 環境・サーバー・実行回・対象処理日 | 件別入力と、採用するなら事前登録データ | 対象件を選ぶと表示。日付表示はYYYY-MM-DDを提案、正式形式は未決定 | ラベルの正確な文言も未承認。値は動的 |
| 保存結果 | 件別の保存処理結果 | 成否が判明した時点で件に表示。結果不明の表示は未決定 | 失敗時の「保存できませんでした」のみ固定 |

翻訳は該当なし。承認済み要件は日本語以外の表示を要求しておらず、翻訳元・切替条件も未作成のため、英訳や言語切替を追加しない。

## 使いやすさ

- B案の左一覧・右詳細を配置案とし、どの件を編集しているかを見失わないようにする。保存失敗はその件に結び付ける。文字や入力欄の欠け・重なりを避ける。
- 失敗件を誤って処理対象として見せない。色だけに頼らず、固定文言と件の対応で示す。選択チェックの意味は未決定なので画像から確定しない。

## 変更の境界

- **固定する意味**：複数件の受付、件別4項目、開始前修正・取消、保存失敗件の転記停止、進行中取消なし、要件で確定した「保存できませんでした」。
- **承認待ちで固定しないもの**：見出し・ボタン・ラベルの正確な文言、初期値、入力制限、選択方式、成功表示、結果不明表示、遷移、4画面構成。推奨は項目表と下の確認事項に記した案に限る。
- **初回実装で改善可能な見た目**：意味と順番を変えない余白・整列・文字サイズ・コントラスト・境界線、画面幅による欠けの防止。機能・文言・入力項目・操作結果・遷移の追加変更は含めない。仕様にない説明文・見出し・ボタンは追加禁止。

## 承認時に確認する事項

| 確認対象 | 推奨案（すべて未承認） | 決定に必要なこと |
| --- | --- | --- |
| 画面構成・入口と出口 | 受付をSCR-01とし、開始受付後はSCR-02へ進む | 4画面構成と自動遷移の採否 |
| 見出し・項目名・ボタン名 | S01-D1、S01-A1～A5、S01-I1～I4の表に示した短い文言を候補にする。画像内の文言を流用しない | 各行の正確な文言と、修正操作を独立ボタンにするか |
| 初期値・制限・選択の意味 | 件別4項目は未指定から確認し、日付を当日で埋めない。選択中の件と処理対象の含除を区別する | 許容形式・件数・サイズ、候補元、0件時の扱い。T-001・T-004の結果 |
| 保存成功・失敗・結果不明 | 失敗判明時だけ固定文言「保存できませんでした」を件に示す。結果不明を成功扱いせず当該件を留める | 成功・結果不明・入力不正の正確な文言、再選択・仮置きログの回復方法。T-001・T-004の結果 |

### B案の参考画像から採用する点・しない点

| 区分 | 判断と理由 |
| --- | --- |
| 採用する配置案 | 左に複数件の一覧、右に選択中の1件の詳細を置く。複数件の存在を見ながら、件別の4項目を読みやすく確認できる。 |
| 採用する表示案 | 保存失敗の文言を該当件の近くに置く。C08の「その件に表示」と一致する。 |
| 採用しない仕様化 | 画像のチェックボックスの意味、ファイル名・日付・環境の例、青い配色、ヘッダー名、各ボタンの正確な文言を確定仕様にしない。選択と保存の実現方式、文言、配置の最終判断は未決定。 |
| 採用しない機能 | 画像に描かれていても、要件にない選択方式・保存方式・追加操作を機能として増やさない。開始後の進行中取消も加えない。 |

画像は受付の役割だけを示す参考であり、後続の画面構成や保存成功の判定方法を証明しない。

## 設計時に決める事項

- 4つの役割をそれぞれ1画面とする提案の採否、各情報の最終配置、画面間の遷移、表示文言と表示条件。T-004の状態設計とユーザー確認後に確定する。
- 複数選択と件別入力・取消を組み合わせる方式、指定場所への保存方式と費用、取消時の仮置きログの扱い。T-001～T-004 の調査・設計結果が必要。
- 0件時の表示、通信などで保存の成否が分からない場合の表示・安全な再開、再表示時の状態保持。これらは REQUIREMENTS の「完成条件の補足案」にあり、正式採用は未決定。確定前に機能・文言として実装しない。
- 文言「保存できませんでした」以外の正確な表示文言と、動的な文言の表示元・表示条件。未決定。

## 確認条件との対応

| 条件 | 本書で引き継ぐ範囲 | 現在の結果 |
| --- | --- | --- |
| C01 | 複数の正常ログを選び、件ごとの対象処理日を指定して開始する受付部分 | 未確認。実装・検証は未実施。 |
| C08 | 保存失敗件の文言と転記停止 | 未確認。実装・検証は未実施。 |
| C09 | 開始前の修正・取消、取消件の非処理、開始後の進行中取消なし | 未確認。実装・検証は未実施。 |

## 資料・承認履歴との対応

- 旧来の独立した画面仕様：**未作成**。移行対象ファイルはなく、上記の内容は REQUIREMENTS の「主要操作」「画面に必要な役割と情報」「確認用の完成条件案」と T-001・T-005 から引き継いだ。両資料は出典・承認経緯・評価履歴の参照先として残す。
- U01・U02 と保存失敗時の文言は、REQUIREMENTS「決定」「調査・レビュー履歴」に 2026-09-24 の個別回答として記録されている。C01・C08・C09 と要件全体の扱いは TASKS 冒頭を参照する。**本書の作成を新たな画面案の承認と扱わない。**
- 参考画像：[受付・対象確認のB案](../references/受付・対象確認-B案-20260924.png)（2026-09-24、ChatGPT imagegenで作成、**未承認**）。一覧と選択した1件の詳細を並べる構成。画像内のファイル名・日付・環境名は架空の表示例。「処理開始」など、確定文言として本書に記載していないラベルは仮置き。画面数や機能・文言の承認、承認済み基準画像ではない。保存失敗と処理対象の扱いは、上記の固定事項とC08・C09を優先する。
- 承認済み基準画像：**未作成**。実画面の受入後に対象版・状態・表示条件・承認時点とともに本書へ記載する。
- 対応テスト：**未作成**。実物と確認手順を作成した時点で本書へ参照先を記載する。`docs/CHECKS.md` も現在は未作成。
- 参考画像：**B案1件を保存、未承認**。実装・実画面の確認・ユーザーの画面受入：**未実施**。
