# 件ごとの確認：承認済み画面設計

## 状態と出典

- 状態：**画面設計承認済み（版 UI-20260924-01）**。2026-09-24（JST）の本チャットで、ユーザーが本書の画面仕様と見た目の方向を実装する設計として承認した。具体的に記載された候補の文言・操作・状態・遷移・4画面構成を承認範囲とする。具体値のない「未決定」とT-004の検証依存事項は承認で埋まらない。実装・実画面の検証・実物の受入は未実施。
- 根拠：`docs/REQUIREMENTS.md` の「主要操作」4～6、「画面に必要な役割と情報」の「件ごとの確認」、C01・C03～C06。共通事項は [UI共通ルール](../rules.md)。
- 件別状態、反映の照合方法、再実行の前提と失敗後の回復はT-004の設計待ち。

## 設計承認の記録

| 項目 | 記録 |
| --- | --- |
| 承認した内容・時点 | 2026-09-24（JST）、本チャットのユーザー承認。SCR-03の目的、表示・入力項目、操作、状態と遷移、具体的に記載された候補を版 UI-20260924-01 として承認。 |
| 参考画像の保存先・版 | この画面の参考画像は**未作成**のため、保存先・版は該当なし。受付のB-20260924はこの画面の参考画像ではない。 |
| 改善可能な範囲 | 本書「変更の境界」に記した余白・整列・文字サイズ・スクロールの見やすさ・コントラスト・境界線に限る。共通のデザインルールは [UI共通ルール](../rules.md) を参照する。 |
| 残る未決定 | 具体的な値や失敗文言のない欄、照合・再実行の条件、結果不明からの回復など。理由と変更案を先に示し、必要な設計判断を得る。 |
| 実物の状態 | 実装・画面表示による検証・実物受入は未実施。基準画像と対応テストは未作成。 |

## 画面ID・対応要件

- **SCR-03**。対応要件：**REQ-03、REQ-04、REQ-05、REQ-06**。対応完成条件：**C01、C03～C06**。4画面構成は版 UI-20260924-01 で設計承認済み。

## 目的

1件の架空ログとその証跡Excelの転記内容を照合し、それとは別にログに記された処理結果を人が判断・記録する。失敗件の安全な再実行も件別に扱う。

## 表示

- 必要：件の識別情報、元ログと対応するExcelへの参照、転記内容の照合、Excel実反映の確認状態、担当者判断・コメント、失敗理由と再実行可否、レビュー依頼状態。
- この画面には不要：全件の全文の同時表示、別件の修正・再実行、Excel関数の結果、アプリが断定したバッチ処理の成否。

| ID・種別 | 正確な文言／値と承認状態 | 初期値・表示元／形式 | 制限・表示条件 | 操作結果 | 成功／失敗表示・遷移 |
| --- | --- | --- | --- | --- | --- |
| S03-D1 見出し | 「件ごとの確認」〔設計承認済み〕 | 固定文字。入力初期値は該当なし | SCR-03表示中 | 表示専用 | 静的表示で成功・失敗遷移は該当なし |
| S03-D2 件の識別 | 正確な連結文言は**未決定** | SCR-01の環境・サーバー・実行回・対象処理日。日付YYYY-MM-DD〔設計承認済み〕 | 選択した1件だけ。欠落時の表示は未決定 | 表示専用 | 読出失敗の文言・遷移は未決定 |
| S03-D3 元ログ | 項目名は「元ログ」〔設計承認済み〕 | その件の保存済み架空ログへの参照と本文。全文表示方法は未決定 | ログが参照可能なとき。保存失敗・読取不能時は見られる範囲と理由を区別 | 表示・参照のみ。別件の内容は表示しない | 参照成功：SCR-03で内容を確認。失敗：理由の文言未決定、SCR-03にとどまる案 |
| S03-D4 証跡Excel | 項目名は「証跡Excel」〔設計承認済み〕 | その件に対応する新しいExcelへの参照。作成前は参照なし | 作成された件のみ。過去に作った別の証跡を上書き表示しない | 表示・参照のみ | 参照成功：実内容を確認。作成・書込失敗や結果不明時は反映済みと表示しない。遷移先未決定 |
| S03-D5 実反映の確認 | 正確な状態名は「未確認／反映確認済み」〔設計承認済み〕 | Excel実内容の確認記録。初期状態と記録方法はT-004待ち | 処理完了だけで反映確認済みにしない | 表示専用 | 成功・失敗時の正確な文言未決定、画面遷移なし |
| S03-D6 転記照合の結果 | 正確な表示文言は**未決定** | S03-I1の担当者記録。未記録の表示はT-004待ち | 処理結果の人による判断とは別欄 | 表示専用 | 記録の成功・失敗はS03-A1で扱う |
| S03-D7 処理結果の判断・コメント | 判断値「正常・異常・判断保留」は**確定**。見出しの正確な文言は未決定 | S03-I2・I3の担当者記録。未記録の表示は未決定 | アプリの自動成否判定として表示しない | 表示専用 | 記録の成功・失敗はS03-A1で扱う |
| S03-D8 失敗理由・レビュー依頼状態 | 正確な表示文言は**未決定** | 件別処理結果、SCR-04への依頼記録 | 理由または依頼がある件で表示。結果不明を確定失敗と混同しない | 表示専用 | 更新失敗の文言・遷移は未決定 |

## 入口と出口

- 入口：SCR-02から1件を選ぶとき。差し戻された件を見直すときの入口も候補。
- 出口：確認・再実行後はSCR-02へ戻る〔設計承認済み〕。レビュー依頼後はSCR-04の依頼状態へつなぐ。1アカウントでの役割切替位置と自動遷移の有無は未決定。

## 操作

| ID・種別 | 正確な文言と承認状態 | 初期値・表示元／形式 | 制限・表示／操作条件 | 操作結果 | 成功／失敗表示・遷移 |
| --- | --- | --- | --- | --- | --- |
| S03-I1 転記内容の照合 | 選択値「一致／不一致／確認保留」〔設計承認済み〕。欄名の正確な文言は未決定 | 初期は未記録〔設計承認済み〕。照合対象は元ログと当該Excel | 担当者役がその件を確認する場合。比較基準と修正方法はT-004待ち | 転記内容について人が確認した結果を記録 | 有効時はS03-A1で保存。無効時の文言・遷移は未決定、SCR-03にとどまる |
| S03-I2 処理結果の判断 | 選択肢「正常」「異常」「判断保留」は**要件確定**。欄名は未決定 | 初期は未選択〔設計承認済み〕。元ログに書かれた結果を人が確認 | 担当者役が件ごとに選ぶ。ログ内容からアプリが自動選択しない | 人の判断をS03-I1と別に記録 | 有効時はS03-A1で保存。無効時の文言・遷移は未決定、SCR-03にとどまる |
| S03-I3 コメント | 欄名「コメント」は要件の用語で画面文言も〔設計承認済み〕 | 初期は空欄〔設計承認済み〕。担当者が入力した文章をそのまま表示 | 判断とともに記録する。必須条件、文字数、改行、機微情報対策は未決定。架空データのみ使う | 当該件の判断にコメントを付ける | 保存結果はS03-A1。入力失敗時の文言・遷移は未決定 |
| S03-A1 確認の記録 | ボタンの正確な文言は「確認を記録」〔設計承認済み〕 | 操作に入力初期値は該当なし | S03-I1とI2・I3を別々の意味で保存。必須項目と保存方式はT-004待ち | 担当者確認と人の判断を件別に記録 | 成功：SCR-03で記録結果を示す案。失敗：保存済みと表示せず、文言未決定、SCR-03にとどまる案 |
| S03-A2 対象件の再実行 | ボタン文言は「この件を再実行」〔設計承認済み〕 | 操作に入力初期値は該当なし | 失敗件の状態と既存Excelを確認後、安全に重複を防げる場合だけ。具体条件はT-004待ち | その件だけ再実行し、独立件と過去証跡を残す | 受付成功：SCR-02で進捗を追う案。失敗・結果不明：反映済みにせず、理由・表示と遷移はT-004待ち |
| S03-A3 レビュー依頼・再依頼 | 正確な文言は「レビューを依頼」、差し戻し後は「再依頼」〔設計承認済み〕 | 操作に入力初期値は該当なし | 担当者役が記録を確認した件。依頼可能条件と差し戻し修正の範囲は未決定 | SCR-04に依頼または再依頼の記録を作る | 成功：SCR-02の依頼状態を更新しSCR-04へつなぐ案。失敗：未依頼のままSCR-03、文言未決定 |
| S03-A4 一覧へ戻る | ボタン文言は「一覧に戻る」〔設計承認済み〕 | 操作に入力初期値は該当なし | SCR-03表示中。未保存入力がある場合の扱いは未決定 | 一覧へ戻る | 成功：SCR-02。失敗・未保存時の表示と遷移は未決定 |

## 入力

入力欄は上表の **S03-I1～I3**。転記照合、処理結果の人による判断、コメントを別々に扱う。具体的に記した選択値と初期値は設計承認済み。未記載の制限・保存結果は推測で補わない。

## 状態

| 件別状態（画面上の文言ではない） | 表示・根拠 | 可能な操作 | 次の状態・遷移 |
| --- | --- | --- | --- |
| Excel反映未確認 | 処理完了だけでは実反映を示さない | 元ログとExcelの参照、状態確認 | 実反映が確認された場合に照合へ。結果不明時の回復はT-004待ち |
| 担当者の確認待ち | 元ログとExcelを照合できる | 転記照合と処理結果判断・コメントを別々に記録 | 記録後はレビュー依頼候補。正確な依頼条件は未決定 |
| 失敗・結果不明 | 理由または未確認である事実を件別に表示 | 状態と既存証跡を確認 | 安全条件が成立した件のみ再実行。自動重複転記しない |
| レビュー差し戻し | 理由を確認できる状態 | 必要な修正と再依頼 | 依頼状態を更新。修正範囲と遷移は未決定 |

## 処理と遷移

- **確定**：ログとExcelの転記照合、ログに記された処理結果の人の判断は別記録。判断値は「正常・異常・判断保留」とコメント。アプリはバッチ処理の成否を自動断定しない。失敗した件だけを状態確認後に再実行し、重複転記と過去証跡の消失を防ぐ。
- **未決定**：照合欄の正確なラベル、コメント制限、実反映確認と再実行の具体条件、差し戻し時の修正対象、操作後の未記載の表示・遷移。結果不明時に成功表示も自動再実行もしない方向は設計承認済み。

## 動的な値・翻訳

| 値 | 表示元 | 形式・条件 | 固定文言との区別 |
| --- | --- | --- | --- |
| 件の識別・ログ全文 | SCR-01の件別記録と保存済みの架空ログ | 対象の1件だけ。全文の表示・改行・長文の扱いはT-004待ち | 内容は動的。サンプル文面は固定しない |
| 証跡Excelと実反映結果 | 当該件に作成されたExcelと実内容の確認記録 | 作成・参照可能な件だけ。結果不明は反映済みとしない | 参照先・状態は動的。Excel関数結果は扱わない |
| 担当者判断・コメント・レビュー状態 | S03-I1～I3の記録と依頼記録 | 記録済みの件だけ表示。文面は入力値を保持 | 判断の3択は固定値。コメントと状態は動的 |

翻訳は該当なし。日本語以外の表示要件がなく、翻訳元・切替条件も未作成のため追加しない。

## 使いやすさ

- 元ログと対応Excelの関係を明確にし、照合結果と人の処理結果判断を別の欄にする。長いログ全文を読めるようにしつつ、他件の操作と混ざらないようにする。
- 再実行は対象件と過去証跡を確認してから行う構成にし、失敗・結果不明を成功と誤認させない。

## 変更の境界

- **固定する意味**：転記照合と人の判断の分離、判断の3択とコメント、件別の安全な再実行、レビュー依頼・再依頼、アプリによる成否の自動断定なし。
- **未決定で固定しないもの**：照合欄名、コメントの制限、記録と再実行の具体条件、未記載の成功・失敗表示と遷移。承認済みの選択値・初期値・ボタン文言・操作の意味は独断で追加・変更しない。設計変更が必要なら理由と案を先に示す。
- **初回実装で改善可能な見た目**：余白・整列・文字サイズ・スクロールの見やすさ・コントラスト・境界線。操作の意味、入力、文言、状態、遷移を変えず、仕様外の説明文・見出し・ボタンを追加しない。

## 承認された具体案と残る未決定事項

| 確認対象 | 設計承認済みの具体案と残る未決定 | 決定に必要なこと |
| --- | --- | --- |
| 照合と判断の入力 | 転記照合は「一致／不一致／確認保留」、処理結果は要件の「正常／異常／判断保留」を別々に記録する | 照合欄名、コメントの必須条件・文字数、保存失敗時の正確な文言 |
| 再実行 | 状態と既存Excelを確認できた失敗件だけを再実行対象とし、結果不明では開始させない | 安全な再実行の具体条件と重複防止。T-003・T-004の結果 |
| 依頼・遷移 | 記録後にレビュー依頼、差し戻し後に再依頼を行い、SCR-02で状態を追う | 依頼可能条件、差し戻し修正の範囲、未保存時と読出失敗時の表示・遷移 |

## 資料と未決定

- 受付のB案画像はこの画面の参考画像ではない。**この画面の参考画像・承認済み基準画像・対応テストは未作成**。
- T-004の状態・回復設計、技術検証、未決定事項の追加確認、独立レビューは未実施。画面設計は2026-09-24にユーザー承認済み。
