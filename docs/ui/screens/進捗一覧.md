# 進捗一覧：画面仕様案

## 状態と出典

- 状態：**役割整理案、未承認**。4つの役割をそれぞれ1画面とする提案の一部であり、画面数・文言・配置・遷移は未確定。
- 根拠：`docs/REQUIREMENTS.md` の「主要操作」2～4・6、「画面に必要な役割と情報」の「進捗一覧」、C01～C04・C06・C09。共通事項は [UI共通ルール](../rules.md)。
- 件別の状態名と更新条件は T-004 の設計待ち。保存方式、Excelへの実際の反映確認方法、結果不明からの回復方法を画像や本書から推測して決めない。

## 画面の役割整理案

| 項目 | 内容 |
| --- | --- |
| 目的 | 複数件を一覧で追い、ログ保存、転記処理、Excelへの実際の反映、担当者確認、レビューの進み具合と失敗理由を混同せず確認する。 |
| 主操作 | 件ごとの状態と失敗理由を確認し、確認または対応が必要な件を選んで件ごとの確認へ進む。レビュー依頼・結果の状態も一覧で確認する。 |
| 補助操作 | 失敗件の状態確認と安全な再実行のため、該当件の詳細へ進む。再実行の実施箇所と条件はT-004の状態設計後に決める。 |
| 必要な情報 | 件を区別する環境・サーバー・実行回・対象処理日、ログ保存、転記、Excelの実反映、失敗理由、担当者の確認、レビュー依頼・結果の状態。処理が終わった表示と実反映の確認を区別する。 |
| この役割には不要な情報 | ログ全文やExcel全文の常時表示、担当者判断の入力欄、レビュー結果の入力欄。詳細確認・入力は各役割へ分ける。 |
| 入口 | 受付・対象確認で処理を始めた後、または件ごとの確認・レビューの結果を一覧で見直すとき。アプリ再起動後の復帰は未決定。 |
| 出口 | 対象件を選んで件ごとの確認へ進む案。依頼された件をレビューする導線も候補だが、役割切替の場所と細かな遷移は未決定。 |

## 境界と未決定

- 受付の参考画像B案はこの画面の参考画像ではない。**この画面の参考画像・承認済み基準画像・対応テストは未作成**。
- 日付不一致・読取不能などの件は理由付きで止まり、独立した正常件は続く。Excel作成・書込失敗や結果不明を「反映済み」と見せない。重複防止と再実行条件の具体化はT-004・技術検証待ち。
- 並べ替え、絞り込み、件数集計、進捗率などは要件にないため、この案に追加しない。正確な見出し・状態文言と表示条件は未決定。
