# 作業一覧と進捗記録

本書は、ユーザーが2026-09-24に「承認したREQUIREMENTS.md」と明示した要件と、完成条件 **C01～C10** を作業へ移したもの。R01～R16・U01～U02は要件整理中の回答IDであり、REQ-001・AC-001というIDは存在しない。主要操作には画面仕様との対応を追うため **REQ-01～REQ-06** を後から付けた。REQUIREMENTS.mdの要件整理中の「未承認」は当時の履歴として残し、現在の承認状態は同資料の「要件本文」を参照する。補足案と「技術上の規律」の草案を承認済み条件として扱わない。

完成品で目指す流れ：架空ログを複数選ぶ → 件ごとの対象処理日などを確認して開始する → 問題のある件だけ理由付きで止める → 正常件ごとに新しいExcelを1冊作り、ログ全文と識別情報を入れる → Excelへの実際の反映を確認する → 担当者役が内容と処理結果を確認する → レビュアー役に依頼し、確認済みまたは差し戻しを記録する。1アカウントで二役を実演する。

2026-09-24の**資料調査で確認したこと**：Power Appsの添付ファイル欄はPCブラウザで複数ファイルを選べ、SharePointリストのフォーム内で保存する。SharePointには添付ファイル本文の取得とファイルのコピーがあり、Excel Online (Business)には表への書き込みがある。よって「SharePointに一時保存 → 件ごとにExcelひな形をコピーして転記」は検証候補になる。一方、件ごとの入力・取消を複数選択と組み合わせられるか、実際の反映確認と安全な再実行ができるかは**実物で未確認**。Excelは書き込み成功後にも反映が遅れる場合があり、タイムアウト後の再試行で重複行ができ得る。1セルは32,767文字まで。SharePointとExcel Online (Business)は標準コネクターだが、本人の保存場所の利用権・費用は未確認。ユーザーがPower Appsを使えることは確認済みで、再確認しない。根拠：[Power Appsの添付ファイル欄](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-attachments)、[フォームのエラー処理](https://learn.microsoft.com/en-us/power-apps/guidance/coding-guidelines/error-handling)、[SharePoint](https://learn.microsoft.com/en-us/connectors/sharepoint/)、[Excel Online (Business)](https://learn.microsoft.com/en-us/connectors/excelonlinebusiness/)、[Excelの制限](https://support.microsoft.com/en-us/excel/excel-specifications-and-limits)。資料からの候補であり、完成可否の実証ではない。

## 現在位置

- 全体の結論：**完成アプリは未完成**。Developer環境の非公開4画面アプリで新規受付・保存・開始・進捗再表示を確認した。異なる環境の正常D・Gを同時開始し、各Excel1冊・9項目・本文全文と件状態を確認してC01は合格。C02更新版では3種類の異常ログを個別停止・Excel未作成と確認し、正常A/A・run102の単独回帰もExcel実体まで確認した。更新版での異常件と正常件の混在、添付本文読取不能などは未確認。正常Aと日付不一致Cの同時開始は更新前フローの部分試験。先行Cは担当者確認から差し戻し・再依頼・確認済みまで1件の一巡を確認した。旧BのExcel実体は正しいが件状態`結果不明`は維持し、再実行していない。失敗・安全な再実行などC01～C10全体の合格と受入は未完了。
- 現在のタスク：**T-008～T-011の結合実装・実機確認**。GPT-6 Luna／max担当がExcel読戻し0行時の読取り専用再照会を実装し、ローカル検査と独立レビューを経て既存フロー1件へCLIで反映した。新規C・F各1件と正常D・Gの同時開始は実機で成功した。C03のローカルフロー候補には自動再試行抑止・結果不明分岐の静的ガードを追加し、変異テスト24件と独立レビューに合格したが、稼働中フローは未変更で、C03失敗経路は実機未確認。C04の安全な再実行は未実装。C08候補は構造テストと独立レビューを通過し、承認範囲内のCanvas MCP検査6ファイル1回で`Validation PASSED`。検査前の6ファイルはコミット基準とSHA-256一致し、検査後の6ファイル再同期はテキスト一致（Screen1生ハッシュ差は行末差のみ）。Studio保存は未実施で、Chrome保存の回答待ち・未承認扱い。添付名／内容一致、安定ID再保存、再起動後Unknown復元も未完了のためC08全体は未完了。2026-09-25のユーザー指示により、アプリ外の全書込経路の空値防御と同期プラグイン導入は今回の実装ゲートから外している。
- 現在の実物：Developer環境の非公開アプリ「架空ログ証跡アプリ」に完成版用Dataverseの件表・転記先表と4画面を接続・保存済み。Power Apps Studioの「アプリ内」に件別Excel転記フローが1件あり、`新しい件を受付`から空の受付画面へ遷移できる。CとFの処理後、進捗一覧には`処理完了`と`反映確認済み`を表示し、件確認画面のFの証跡Excelリンクは有効。検証専用の旧`T001検証ログ受付`とT-002フローは別資産であり、完成版の実機合格の根拠と混同しない。
- 次に行うこと：C02更新版は無効日付・日付行欠落・サーバー識別情報欠落の個別停止とExcel未作成、正常A/A・run102の単独回帰を確認済み。更新版での異常件と正常件の混在、添付本文読取不能、長文などは残る。C03の次候補は隔離した架空件1件の決定的なExcel書込失敗だが、注入方法・副作用確認・復旧手順と操作範囲を定め、個別承認を得るまでは実施しない。C04の安全な再実行は未実装。C08候補のCanvas MCP検査は6ファイルで`Validation PASSED`。ChromeでのStudio保存は未承認・未実施のため、次は保存状態の確認後に画面確認へ進む。添付名・内容照合、安定ID再保存、再起動後Unknown復元は未実装・未確認。旧Bは結果不明のまま保全して再実行しない。Chrome操作が必要なら事前に報告する。
- 本人のrun101試行：`normal-demo-run101.txt`本文の日付`2026-09-25`に対し、画面指定は`2026-09-26`だったため停止。これは本人の正常経路受入としては不成立で、手順側でサンプルの4項目を明記していなかった点も原因として扱い、ユーザーだけの操作ミスとしない。停止件の担当者欄`一致`／`正常`が見える問題は別途確認中で、`Excel実反映：未確認`を成功に読み替えない。一方、更新後フローの正常単独回帰は別のrun102で実施済み。本人受入とは区別する。詳細はT-011評価履歴と`docs/CHECKS.md`。
- 判断待ち・制約：BのExcel実体は正しいが件状態は`結果不明`で、状態整合は未実施。Cでは再照会後の成功を確認したが、キャッシュが必ず避けられるかは未証明。C08では`Form1.OnSuccess`から添付の保存先照合を経ずに保存済みとする経路を静的に確認し、行状態更新の失敗でも保存済み表示となり得る。行と関連添付の確認・安定IDでの同一件再保存は未実装・未検証。長文分割、同時起動防止、安全な再実行、保存・開始の返答不明時の復旧は未確認または未実装。担当者確認・レビューはCの最小一巡のみ実証。Developer環境で今回の架空データは追加購入なしに保存できたが、将来の利用権・費用は未確定。公開操作は未実施。ブラウザ・コンピュータユースは事前報告と了承を受けた範囲だけ使用する。
- 2026-09-25の実装事実（完成版フロー作成前の記録）：完成版候補のDataverse件表に状態・確認・レビュー用9列を追加し、CLIとCanvas MCPで列定義を読み戻した。受付・進捗・件別確認・レビューのCanvas YAMLをローカルで作成。最新6ファイルをMCP`compile_canvas`で`Validation PASSED`にし、実行前の別ディレクトリ`outputs/canvas-live-readback-20260925/`への`sync_canvas`は4ファイル・2画面、実行後の`outputs/canvas-live-readback-after-20260925/`への`sync_canvas`は6ファイル・4画面と確認した。Screen3・Screen4にはサーバー正規化差分があるが、主要内容を読み戻した。Studio表示・Save・実行・公開は未確認。再実行と処理開始は未結線のため無効。当時、クラウドの完成版フローとCanvas側フロー接続は未作成。ユーザーがPower Apps (V2)トリガーとText入力を画面で作成したが、アクションがないため保存エラー。当時のDataverse CLIの名前一致GETで保存済みフロー0件。この下書きとは別に、後続のT-009で定義をCLIから作成・有効化した。Chromeへの操作接続は午後からタイムアウトし、別経路はURL判定の安全機能で停止。Windows sandbox ACLエラーとの因果は未確認。Codex内ブラウザではPower Automateのサインイン画面まで開けたが、ユーザーのChrome指定に従いタブを閉じ、使用しない。以上を実機合格・完成と扱わない。
- 最終更新日時：2026-09-26（C02更新後フローで異常ログ3件の個別停止理由・Excel未作成と、正常A/A・run102の単独回帰・Excel実体一致を確認。混在等は未確認）

## タスク一覧

| ID | 作業 | 状態 | 先に必要な作業 | 関連要件・完成条件 |
| --- | --- | --- | --- | --- |
| T-001 | 架空ログ2件を画面で選び、保存・修正・取消できるか確認する | 実装中 | なし | 主要操作1、U01・U02／C08・C09、C01の受付部分 |
| T-002 | ログ1件ごとに新しいExcelを作り、全文の反映を確認する | 完了 | T-001 | 主要操作2～3、R03・R04・R12・R13／C01・C02 |
| T-003 | 異常件・結果不明・重複・再実行を確認する | 評価中 | T-001、T-002 | 主要操作2～4、R05・R06／C02～C04・C08・C09 |
| T-004 | 件別データ・処理状態・失敗後の回復方法を設計する | 完了 | T-001～T-003で得た安全な証拠。未確認はT-009・T-011で検証 | データと架空サンプル／C01～C06・C08・C09 |
| T-005 | 受付・進捗・担当者確認・レビューの画面を設計する | 完了 | T-004の論理設計。承認済み仕様は画面資料へ反映済み。技術方式はT-007で確認 | 画面に必要な役割と情報／C01・C05・C06・C08～C10 |
| T-006 | 決めた保存先と架空の実演データを準備する | 実装中 | T-004、T-005。承認済みの処理日形式でローカルサンプルのみ先行 | データと架空サンプル／C01～C04・C07・C08 |
| T-007 | 実装・検証の詳細計画を現方針へ更新する | 実装中 | T-004～T-006、承認済み画面仕様。以前の全書込経路の空値防御試験は現方針の必須条件から除外し、失敗結果は履歴に残す。残り5件の旧試験は未実施 | 全要件・画面仕様／C01～C10 |
| T-008M | 4画面を架空データで操作できる先行モックにする | 完了 | 承認済み画面設計。実データ接続のT-007実証は不要 | SCR-01～SCR-04／C01～C06・C08・C09の表示と遷移 |
| T-008 | ログ受付・件ごとの保存・再起動復元・開始前の修正と取消を実装する | 実装中 | T-005の承認済み画面仕様、T-008Mの画面方向・仮遷移受入、現方針の実装計画 | 主要操作1、U01・U02／C08～C10 |
| T-009 | 日付照合・Excel転記・進捗・再実行を実装する | 実装中 | C・F・新Aの正常転記とExcel実体を確認。新Cの日付不一致は停止し、同時開始した正常Aは継続。旧Bは結果不明のまま保全。再実行・長文と残る異常は未確認 | 主要操作2～4、R03～R06・R12・R13／C01～C04 |
| T-010 | 担当者確認・レビュー依頼・差し戻しを実装する | 実装中 | T-009。Cの担当者確認・依頼・差し戻し・修正・再依頼・確認済みの最小一巡は実機確認。異常判断と残る分岐は未確認 | 主要操作5～6、R07～R11・R14／C05・C06 |
| T-011 | C01～C06・C08～C10を実機で検証し、修正する | 評価中 | T-008～T-010。Bの読戻し失敗、C・F・新Aの正常転記、日付不一致Cの件別停止、Cのレビュー一巡、C10の一部を確認。全条件の合格判定は未実施 | 操作と期待結果／C01～C06・C08～C10 |
| T-012 | 架空データを相手に操作してもらい、受入を確認する | 未着手 | T-011 | 実演と限界、R09・R10／C07 |

状態は、未着手／要件確認中／計画承認待ち／実装中／評価中／修正中／依存作業待ち／判断待ち／ユーザー受入待ち／停止中／完了から一つ選ぶ。T-005は承認済みの4画面仕様と2026-09-25の追加承認を画面資料へ反映済みで、画面設計作業として完了。T-007の旧API試験では最大8回のCLI作成要求のうち3回を実施し、正常1件、重複拒否1件、環境列欠落の予想外の保存1件を確認した。ユーザー指示でアプリ外の全書込経路の空値防御を現方針から外したため、旧試験の停止は完成版着手を妨げない。残り5回の旧試験と条件付き更新は未実施。T-008～T-010は実装中で、完成版の保存・フロー・レビューはそれぞれ一部の結合・実機確認を実施したが、全条件の合格は未確認。T-001は検証用の実物で一部操作を確認した。T-002は短い架空ログA・Bの2冊と本文全文・識別情報の一致を独立レビューでも確認し、方式検証として完了した。Excelの1セル32,767文字制限はMicrosoft資料で確認済み。上限を超える本文はT-004の論理案で順序付き複数行としたが、完成版の長文実機試験は未実施。T-001～T-003は**小さな検証用の実物**で方式の可否を確かめる作業で、完成アプリの実装やC条件の正式な合格判定ではない。各作業の期限は未設定。外部サービスにデータを作る前には対象、操作、費用と戻し方を説明し、実行範囲を確認する。

## タスク別の記録

### T-001：架空ログ2件を画面で選び、保存・修正・取消できるか確認する

- 目的と今回の範囲：PCブラウザのPower Apps画面で短い架空テキストログを2件選び、保存し、処理開始前に件ごとの環境・サーバー・実行回・対象処理日を確認・修正・取消できるか確かめる。検証用の保存先には既存Developer環境のDataverseテーブルを使用した。保存失敗が判明した件には「保存できませんでした」と示し、転記対象にしない。
- 今回変更しないこと：完成画面、Excel転記、実務ログ、公開設定を作らない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQ-01、U01・U02／C08・C09、C01の受付部分。受付・対象確認の画面設計は版 UI-20260924-01 で承認済み。参考画像は同資料のB-20260924を参照。検証計画は下記に作成済みで、実機結果に応じて更新中。
- 担当と先に必要な作業：技術確認担当が試し、統括が結果をまとめ、独立レビュー担当が確認する。実担当は着手時に割り当てる。先行作業なし。
- 未決定・ユーザーの判断待ち：完成版で使う保存先と件ID、通信・保存先側の失敗と結果不明時の扱い、対象処理日を時刻帯に左右されない日付として保存する設計、将来の利用権と費用。SharePoint候補はこの試験では使っていない。
- 確認する操作・期待結果・対象端末：PCブラウザで2件選択→保存→件別情報を修正→一方を開始前に取消。保存失敗時は表示し、処理しない。OS・ブラウザの版は着手時に記録。
- 関係する画面・データ・課金・資料：受付画面、架空ログ2件、SharePoint候補、REQUIREMENTS.md。追加費用は未確認。
- 正式な不合格回数：未確認（この作業の正式評価は未実施。前工程の要件整理は正式不合格0回とREQUIREMENTS.mdに確認済み。停止上限は初回を含め3回）。
- 次の行動：入力不正以外の保存失敗・結果不明の安全な再現方法を調べる。対象処理日列は検証用テーブルでユーザー ローカル動作と確認済み。完成版で日付のみ動作を使うかをT-004で判断する。本文取得と件別保存の成功経路は下記の実機結果をT-002へ渡す。

#### T-001の実行計画（2026-09-25に作成した当時の計画。以下の結果と差異を参照）

- **目的と対象**：SCR-01／C08・C09・C01受付部分のため、PCブラウザで短い架空の`.txt`を2件選び、各件の4項目を別々に保持し、一方の修正・取消と保存結果を確認する。最終的なログ形式や件数・サイズ上限はこの試験で確定しない。
- **試験データ案**：`samples/t001/mock-log-a.txt`は本文日付`2026-09-24`、架空の環境`検証A`・サーバー`架空A`・実行回`1`・対象処理日`2026-09-24`。`samples/t001/mock-log-b.txt`は本文日付`2026-09-23`、`検証B`・`架空B`・`2`・`2026-09-23`。本文は短い架空の開始・終了行だけとし、実務ログ、個人情報、秘密値を含めない。値は方式確認用であり、正式な選択肢・入力制限ではない。
- **保存先候補と未確認事項**：SharePointリストは当初の第一候補だが、本人が使えるサイト／リストは未確認。既存Developer環境のDataverseテーブルも候補で、Microsoft公式資料では両者のフォーム内で添付欄を使える。複数選択した2件を別の件別記録へ安全に結び付ける方法はどちらも未検証。現アプリのデータソース・APIは各0件、CLIの既存接続一覧も0件。Canvas Authoring MCPにデータソース追加操作はなく、CLIの`pac connection create`はサービスプリンシパルによるDataverse接続用であり、今回のSharePoint接続追加にはそのまま使えない。接続先と追加手段が確定するまで保存成功を表示しない。
- **試験の順序**：①接続先と利用条件、作る検証専用リスト／レコード、保持・片付け方法を確定。②2ファイルを選び、選択件と元ファイル名を確認。③各件の4項目を入力し、Aだけ修正してBは開始前に取消。④残るAを保存し、保存先を開いて本文と4項目の対応を確認。⑤保存失敗を再現できる安全な方法が判明した場合だけ、失敗件の表示「保存できませんでした」と転記対象外を確認。失敗を再現できなければC08は未確認と記す。
- **合格の判定**：複数選択、件別の入力保持、修正、取消、保存先の実内容を件IDとともに照合できること。取消したBに後続処理・Excel作成がないことの確認はT-002以降の連携後に別途行う。2件を1つの添付レコードへ保存できただけでは件別保存の合格にしない。
- **外部操作と戻し方**：試験で作るものは、確定した検証用の保存先と上記の架空データに限る。作成前に接続先、追加料金の有無、保存先の権限、残す／削除する対象と復元方法を示す。実API試験については`docs/WORKFLOW.md`に従いAIかユーザーの実行者を決める。実際の作成・試験は下の履歴に記録した。削除は未実施。

#### 2026-09-25の実機結果と計画との差異

- **承認と対象**：ユーザーが、AIによるPower Apps Studioでの検証専用Dataverseテーブル作成・アプリ接続・短い架空`.txt`2件のアップロードと試験を許可。Developer環境`c1719a14-6cac-eb5b-b598-2254af1c64b7`、非公開アプリ`3186a908-ac5b-48cc-90f5-d50e07282ee7`で実施。購入・課金設定変更・公開・削除はしていない。ブラウザはChrome、OSはWindows。正確な版番号は未記録。
- **作成したもの**：Dataverseテーブル`T001検証ログ受付`を1つ作成し、ログ名（主列）、環境名、サーバー名、整数の実行回数、日付のみの対象処理日を追加。テーブルの「添付ファイルを有効にする (メモとファイルを含む)」を有効化し、Studioでアプリのデータソースに追加。MCPの`list_data_sources`は`CdsNative`1件、`get_data_source_schema`は各列と`{Attachments}`を確認。`log-evidence-app/Screen1.pa.yaml`はT-001用の単一レコードの編集フォーム、添付欄、保存ボタン、保存後の受付表示を持つ。完成画面ではない。
- **操作と結果**：PCブラウザで架空`.txt`2件を一度に選択でき、添付欄には両方「未保存」と表示。Power Fxの`Concat(DataCardValue5.Attachments, Name, " | ")`で選択中の2ファイル名を別々に取得できた。保存前にBを選択から外し、Aの環境名を修正して保存した。続いてBを単独で選び別のフォーム送信で保存した。テーブル編集画面にはA・Bが別々の2行として表示され、環境名、サーバー名、実行回数、対象処理日も入力値と一致。アプリから`CountRows(LookUp(...).Attachments)`を読み直すとA・Bとも1件だった。これらの確認用ラベルは検証後に画面定義から削除した。架空ログ本文の再取得・照合は未実施。
- **追加の件別保存試験（08:44～08:49 JST）**：添付データカードの`Update`をファイル名で1件に絞り、保存ボタンで2件の添付をコレクションへ保持する方式を試した。A・Bを一度に選択し、Aをフォーム送信した後、ファイルを選び直さずBを別のフォーム送信で保存した。保存先には「検証環境A・連続分離試験」と「検証環境B・連続分離試験」の2行が別々に現れ、サーバー・実行回・対象処理日も各入力値と一致。アプリから読み直した添付件数は**A=1、B=1**。途中の分離試験でもAの新しい行が1件だけ増え、添付は1件だった。確認用ラベルは削除済み。
- **保存済み件の除外試験（08:52 JST）**：`Form1.OnSuccess`で保存成功したログ名の添付だけを入力待ちコレクションから外す式を追加。Studioを再読み込みして一時状態を空にした後、架空A・Bを同時選択し、Aを送信した。画面には「T-001 保存受付: mock-log-a.txt / 入力待ち: mock-log-b.txt」と表示され、Aだけが待ち行列から外れた。Bは未送信のままプレビューを閉じ、Studioを再読み込みして一時状態を破棄した。添付本文の取得・一致はまだ確認していない。
- **件別一覧・修正・取消・本文照合の試験（09:00～09:18 JST）**：`Screen1.pa.yaml`に添付元レコードと件別4項目を分けたコレクション、GUIDで選ぶ一覧、選択件の保存前取消を追加。初回の入力切替では文字項目が保持されず、入力反映の遅延を止め、切替直前に選択件へ値を記録する式へ修正した。フォーム外から子コントロールを`Reset`する案はMCPの構文検査を通ったが、Studio実行時に拒否されたため撤回した。再試験ではA・Bの4項目を切り替えて保持し、Aの環境名を修正後に再表示して保持を確認した。Aを保存前に取り消すとBの値と添付だけが残った。Bを1件保存し、CLIの`pac org fetch`で行ID`c1cc3ee4-75b8-f111-b377-7ced8d3141aa`のログ名・環境名・サーバー名・実行回数を確認。メモ添付はBのファイル名で1件。CLIで取得した本文のBase64と元の架空ファイル100バイトをバイト単位で比較し一致した。Aの修正値に該当する行は0件。アプリでのBの対象処理日の読み戻しは入力通り`2026/09/23`、CLIのUTC表示は`2026/09/22`であり、列の時刻帯をまたぐ扱いは未確定。検証用の一時表示は削除し、MCPの`compile_canvas`は3ファイルで`Validation PASSED`、Studioは09:17 JSTに保存済み・非公開。CLIは通常のサンドボックスでは認証キャッシュのロックファイルへのアクセス拒否で失敗し、権限のある実行で上記読み取りを完了した。
- **入力不正での保存拒否試験（09:18～09:20 JST）**：架空ログAだけを一覧へ追加し、環境名を`検証環境A・失敗試験`、実行回数を整数列の上限を超える`2147483648`として保存操作を行った。フォームは`-2147483648`～`2147483647`の範囲エラーを表示し、`OnFailure`で「保存できませんでした」を通知した。古いBの成功表示が残る不具合を修正し、Aの一覧項目と画面下部に固定文言が残ること、Aの入力値・添付・入力待ちが保持されることを再試験した。CLIのFetchXMLで`検証環境A・失敗試験`の行は0件。MCPの`compile_canvas`は3ファイルで`Validation PASSED`、Studioは09:19 JSTに保存済み・非公開。これは**入力検査による拒否**であり、通信障害・Dataverse側の失敗・結果不明の再現ではない。
- **対象処理日列の閲覧調査（09:30 JST）**：Studioの検証用テーブルの列編集画面で、`cr6cb_targetdate`の書式は「日付のみ」、高度なオプションの「タイム ゾーンの調整」は「ユーザー ローカル」と表示。設定変更はせずキャンセルで閉じた。アプリの`2026/09/23`とCLIのUTC表示`2026/09/22`の差は、この設定とMicrosoft公式の[日付・時刻列の動作説明](https://learn.microsoft.com/ja-jp/power-apps/maker/data-platform/behavior-format-date-time-field)に整合する。完成版で同じ設定を採用する決定ではない。日付照合に必要な動作はT-004で決める。
- **計画との差異と判定**：直接の`SubmitForm`で複数添付を一括保存する方法は件別条件を満たさない。コレクションを介した**同時選択から2行・各1添付への連続保存**と、件別一覧での4項目の保持・修正・取消、残件保存と添付本文一致を確認した。計画上のA保存・B取消とは逆にAを取消しBを保存したが、同じ件別分離と取消条件を検証した。入力不正による保存拒否時には失敗文言と未保存件の保持を確認した。検証用の日付列はユーザー ローカル動作と判明した。**通信・Dataverse側の失敗と結果不明、取消件の後段不実行、Excel作成、完成版の日付列設計は未確認**。C01・C08・C09を合格としない。T-001の正式評価は未実施。
- **次の検証案**：入力不正以外の保存失敗と結果不明を安全に再現できるか調べる。完成版の対象処理日を時刻帯に左右されない日付として扱うか、T-004のデータ設計で決める。件別本文と識別情報のT-002への受け渡し・1件1冊のExcel作成方法を検証する。これは方式検証であり、承認済み画面仕様や正式なデータ設計の変更ではない。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C08・C09、C01の受付部分 | 2件の選択・保存・修正・取消と保存失敗の表示 | 未確認 | T-001検証用非公開アプリ、2026-09-25 09:19 JST保存版 | 同時選択からの別行・各1添付の連続保存、件別4項目の保持・修正・取消、残件の保存と添付本文一致を確認。入力不正による保存拒否で失敗文言・未保存件の保持・行0件を確認。通信・Dataverse側の失敗、取消件の後段不実行・Excel実反映は未確認。 |

#### 評価・修正履歴

- **前工程の要件整理（2026-09-23～24）：** 独立レビュー第1～第13段階を実施。当初の転記中心との整合、Excel関数の対象外、1件1冊、異常・結果不明・二役実演・取消を確認した。第5段階の質問不足、第7段階のExcel作成単位、第11～第12段階の追加確認漏れ、第13段階のC09の取消結果などの指摘はREQUIREMENTS.mdへ反映された。第13段階では確認用本文に重大な矛盾や未承認機能の混入は見つからなかった。当時の暫定レビューであり、前工程の正式不合格は0回と記録されている。この0回をT-001の回数へ流用しない。証拠：REQUIREMENTS.md「調査・レビュー履歴」。
- **前工程の調査：** Excelの反映遅延・同時書込、添付欄、Developer Planや共有の一般条件をMicrosoft公式資料で確認。本人環境への適用は未確認。証拠：REQUIREMENTS.md「リスク」「追加確認の監査」。
- **2026-09-24の追加資料調査：** 本書冒頭のMicrosoft公式資料により、SharePointの添付・ファイル操作、Excelへの書き込みと制約、標準コネクター分類を確認。T-001の実動作評価・修正は未実施。
- **2026-09-25の環境確認：** Power Apps StudioでDeveloper環境に空のアプリを作成、保存済み・非公開。共同編集を有効にした後、Canvas Authoring MCPの接続と`log-evidence-app/`への同期が成功した。同期ファイルは`App.pa.yaml`、`Screen1.pa.yaml`、`_EditorState.pa.yaml`の3件。空アプリの`compile_canvas`は`Validation PASSED`。データソース・APIは0件。Canvasアプリ作成スキルの資料は読み込めており、スキル自体のエラーは確認していない。MCPの初回接続は共同編集が無効で失敗し、設定変更後に解消。CLIの`pac auth list`は権限を付けて実行できたが、`pac org who`は`AADSTS530035`で失敗。これらは接続・空アプリ検証であり、C08・C09・C01の実動作評価ではない。
- **2026-09-25のCLI再認証：** 旧CLIプロファイルは有効表示でもトークン期限が2026-09-17で、`pac org who`は`AADSTS530035`で失敗した。非表示の対話型認証とデバイスコード認証の試行は中断し、完了していない。ユーザーが見えるWindows PowerShellから通常の`pac auth create --name Codex-Dev-20260925 --environment c1719a14-6cac-eb5b-b598-2254af1c64b7`を実行し、認証プロファイル作成に成功。その後、AIが`pac auth list`で新プロファイルが有効なこと、`pac org who`で対象Dataverse環境へ接続できること、`pac canvas list`で「架空ログ証跡アプリ」を取得できることを確認。旧プロファイルは一覧に表示されなくなった。アプリやデータソースの変更・機能テストは行っていない。
- **2026-09-25の保存先調査：** CLIの`pac connection list`は既存接続0件、再接続したCanvas Authoring MCPの`list_data_sources`と`list_apis`も各0件。`pac connectivity list-connectors --filter sharepoint`はフィルター指定が不適切でHTTP 400となり、コネクター利用可否の証拠にはしない。CLIの課金ポリシー照会はHTTP 404となり、本人環境の追加費用は未確認。SharePointサイト／リストの有無は本人に分かる前提で質問したが判断できなかったため、別の確認方法を探す。Microsoft公式資料ではDeveloper環境は開発・試験用で、DataverseとPower Automateを利用できる一般条件を確認したが、この環境固有の権利・課金を保証するものではない。外部保存・ファイル選択の実動作試験は未実施。
- **2026-09-25の代替手段調査：** 現在のCanvas Authoring MCPには既存データソースの照会とアプリYAMLの同期・検証があり、データソースの追加操作はない。Power Platform CLIは環境・アプリ・接続の照会に成功するが、今回のSharePoint接続を直接追加できるコマンドは確認できていない。`pac auth token`の値を表示せず宛先だけ確認したところPower Platform API向けで、Dataverse Web APIへそのまま使えると扱わない。当時のSharePointプラグインは未導入で、接続を提案した。その後プラグインは導入・有効化されたが、このタスクの利用可能なツール一覧には再起動後もSharePoint操作ツールが0件で、読み取り試験は実施できていない。ユーザー指示により現在は使用しない。Canvasアプリへのデータ接続追加が自動で済むとも確認していない。ブラウザー／コンピュータユースは今回未使用。
- **2026-09-25の再開確認：** Canvas Authoring MCPへ同じアプリ・環境で再接続し、データソース0件を確認。`sync_canvas`はローカルの未コミットYAMLを上書きする可能性で自動審査に一度拒否された。空アプリの3ファイルを確認し、別の一時ディレクトリへ個別に退避してSHA-256の一致を確認した後に再試行し、3ファイルの同期に成功。`describe_control`で添付ファイル欄とフォームのプロパティを取得した。ファイル選択・保存・件別入力・取消の実動作試験は未実施。
- **2026-09-25のStudio閲覧（ユーザーが閲覧のみ許可）：** 既存の「架空ログ証跡アプリ」のデータ欄は「アプリにデータがありません」と表示。追加候補として現在の環境のDataverseテーブル、SharePointコネクターが見えたが、いずれもこのアプリへ追加していない。画面にはプラン要件の案内が表示されたが、本人アカウントの権利・追加費用を確定できる表示ではなかった。追加パネルを閉じて元の画面に戻した。MCPの`compile_canvas`は同期済みの空アプリ3ファイルで`Validation PASSED`、`list_apis`は0件。保存先・ファイル選択・機能の試験は未実施。ブラウザでの作成・保存・公開は行っていない。
- **2026-09-25のSharePoint閲覧（ユーザーが閲覧のみ許可）：** 既存のSharePointタブで最近使用・おすすめ・お気に入りのサイトを確認したが、いずれも「利用可能なサイトはありませんでした」と表示。本人が利用できるサイトが絶対に存在しないとの証明ではない。元の概要画面へ戻し、サイト・リスト・ファイル・接続の作成や変更は行っていない。T-001に使える既存のSharePoint保存先は未確認。
- **2026-09-25のライセンス閲覧（ユーザーが閲覧のみ許可）：** Microsoft 365管理センターのライセンス一覧にはMicrosoft 365 Business Basicだけが表示され、Power Apps Premiumのサブスクリプションは一覧に見えなかった。従量課金制のページには請求ポリシーの行が表示されなかった。ただし、これらの表示だけで本人のDeveloper環境での検証が無償か、追加作成が可能かは確定できない。管理センターの元のページへ戻し、契約・購入・課金設定の変更は行っていない。
- **2026-09-25のT-001実機試験（ユーザーがAIの試験範囲を許可）：** 上記「実機結果と計画との差異」の通り。2件の同時選択とファイル名取得、保存前の1件取消、別々の2行・各1添付・件別メタデータの保存を確認。保存ファイル本文の再取得と、同時選択から各件へ分けて保存する完成方式は未確認。`compile_canvas`は検証用の3 YAMLファイルで`Validation PASSED`、Studioでは保存済み・非公開。購入・課金設定変更・公開・削除はしていない。正式評価はまだ行わず、不合格回数は未加算。
- **2026-09-25の連続分離試験：** CLIの`pac power-automate help`で現行版はフローの一覧・アクション・履歴の照会のみと確認し、作成コマンドの成功は確認していない。MCPで添付テーブルの絞り込みとコレクション保持の式が`Validation PASSED`。Studioで同時選択したA・Bを選び直さず2行に連続保存し、保存先の4項目と各1添付を確認。続いて保存済み件を入力待ちから除く式を追加して`Validation PASSED`、実機でA保存後にBだけが残ることを確認。Studio保存済み・非公開。本文の一致、件別入力・取消は未確認。正式評価の不合格回数は未加算。
- **2026-09-25の件別一覧試験：** 上記09:00～09:18の実機結果を確認。MCPの検査後、Studioプレビューで4項目の切替保持・修正・取消とBだけの保存を実施。CLIのFetchXMLでBの保存行、添付1件と元ファイル本文100バイトの一致、Aの修正値に該当する行0件を確認。CLIの時刻表示とアプリの日付読み戻しに1日の差があり、別時刻帯の扱いは未確認。保存失敗と結果不明、Excel連携は未確認。作業中に出た`Reset`の実行時エラーは式を撤回して同じ操作を再試験し解消。正式評価ではなく自己確認のため不合格回数は未加算。
- **2026-09-25の入力不正試験：** 上記09:18～09:20の実機結果を確認。範囲外の実行回数で`OnFailure`を確認し、件別の「保存できませんでした」の表示と入力保持・Dataverse行0件を照合。直前の成功表示を残す不具合は修正・再試験した。通信障害や結果不明の検証に読み替えず、正式評価の不合格回数は未加算。
- **2026-09-25の日付列調査：** 検証用Dataverse列は「日付のみ」書式と「ユーザー ローカル」の時刻帯調整が同時に設定されているとStudioで閲覧確認。アプリの読み戻しとCLIのUTC表示が1日違う理由と整合。列は変更していない。完成版の採用設定と別時刻帯の実機結果は未決定・未確認。

#### 完了記録

- 変更内容：検証専用Dataverseテーブルとアプリ接続、`log-evidence-app/Screen1.pa.yaml`のT-001用フォーム・添付絞り込み・件別一覧・4項目の一時保持と保存前取消を作成。架空ログA・Bを同時選択から別々の行に連続保存した試験と、A取消・B保存の試験を実施。
- 自動検証・ブラウザ・実機確認の結果：Canvas Authoring MCPの3 YAMLファイルの`Validation PASSED`、Studio保存済み・非公開。件別4項目の保持・修正・取消、B保存行の4項目・添付1件・本文一致、Aの修正値に該当する行0件を確認。入力不正の保存拒否では失敗文言・件の保持・行0件を確認。T-001全体とC条件の正式合格ではない。
- 確認した対象版：Developer環境`c1719a14-6cac-eb5b-b598-2254af1c64b7`、アプリ`3186a908-ac5b-48cc-90f5-d50e07282ee7`、2026-09-25の非公開保存版。ビルド番号は未記録。
- ユーザーの受入結果：未確認。
- 残る課題と、その引き継ぎ先：通信・Dataverse側の保存失敗と結果不明の再現、日付列の時刻帯の意味をT-001・T-003・T-004で確認し、本文と識別情報の件別取得結果をT-002へ渡す。

### T-002：ログ1件ごとに新しいExcelを作り、全文の反映を確認する

- 目的と今回の範囲：T-001で保存した短い架空ログ2件について、別々の新しいExcelを1冊ずつ作り、全文と環境・サーバー・実行回を入れ、Excelを開き直して反映を確認する。ひな形のコピーとExcel Online (Business)の表への書き込みが候補。
- 今回変更しないこと：長いログの形式やExcel内の最終配置を決めたふりをしない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQ-02～REQ-03、R03・R04・R12・R13／C01・C02。関連する4画面の設計は版 UI-20260924-01 で承認済み。検証計画、関連画面の基準画像・対応テストは未作成。
- 担当と先に必要な作業：技術確認担当、独立レビュー担当、統括。T-001。実担当は着手時に割り当てる。
- 未決定・ユーザーの判断待ち：ログとExcelの最終形式、1セル上限を超えるログの扱い、完成版での自動反映確認方法。検証用OneDriveフォルダーと今回の架空データによる試験範囲は承認済み。3接続の認証は完了済み。
- 確認する操作・期待結果・対象端末：短い架空ログ2件で開始→異なるExcel2冊を開く→それぞれに対応する全文と識別情報のみが残る。今回は正確なPCブラウザ・Excelの版を記録できなかったため、次の実機検証で採取する。
- 関係する画面・データ・課金・資料：架空ログ、証跡Excel、業務用OneDriveの「ドキュメント」、Excel Online (Business)、REQUIREMENTS.md。今回の検証フローからの読み書きは成功。将来の利用権・費用は未確認。
- 正式な不合格回数：0回（短い架空ログ2件の方式検証に関する独立レビューで重大な不整合なし。完成品のC条件評価は未実施。停止上限は初回を含め3回）。
- 次の行動：結果不明と再実行条件はT-003へ、長文の扱いと完成版の保存先・費用・戻し方はT-004・T-006へ引き継ぐ。

#### T-002の検証用構成と実施した試験

- **ローカルひな形**：`outputs/t002-20260925/evidence-template.xlsx`は1シート「証跡」とExcel表`Evidence`（`A4:G5`）を含む。列は`CaseId`、`LogFileName`、`Environment`、`Server`、`RunNumber`、`TargetDate`、`LogText`。5行目の`CaseId=__TEMPLATE__`は検証用の仮行で、フローが件の内容で置換する。最初の空表はExcel Onlineで修復され表自体が削除されたため、1行を持つ表へ作り直した。修正版はExcel Onlineで修復警告なしに開け、A・Bへの行更新が成功した。完成版の帳票形式・長文分割方式・日付の保存方式はT-004で決める。
- **クラウド試験**：業務用OneDriveの「ドキュメント」に検証専用フォルダー`架空ログ証跡アプリ検証-T002-20260925`を作り、ひな形1冊と結果A・B各1冊を配置。Developer環境の検証フローは、Dataverseの件と添付をID指定で取得し、ひな形を件別名にコピーして仮行を更新する。SharePointプラグインで両冊を開き直し、各`CaseId`・本文全文・4項目と対象処理日が対応し、他件の行がないことを確認した。保存結果不明や再実行の安全条件はT-003で別に試す。検証用ファイルは削除せず、片付け方の確認をT-006へ引き継ぐ。
- **操作手段と限界**：SharePointプラグインで保存先と実内容の確認、ファイルの作成・差し替えを実施。現行CLIにフロー作成コマンドはなく、この環境のフロー作成MCPも見つからなかったため、事前報告のうえPower Automate画面で編集・試験した。CLIの`pac power-automate list-flow-runs --workflow-id ... --json`は`null`で、実行結果の判定には使えなかった。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C01の正常転記部分 | 短い架空ログ2件で1件1冊、全文と識別情報の実際の反映 | 方式検証は確認済み。完成品のC01判定は未実施。C02の異常件混在・正常件継続はT-003で未確認 | Developer環境の検証フロー`7f1b320f-a8b0-1884-2957-1a9dffb91d02`、2026-09-25保存版 | A・Bの2冊をSharePointプラグインで開き直し、各1件の全文・識別情報と別件混入なしを確認。 |

#### 評価・修正履歴

- **2026-09-25 09:37 JSTの準備調査（正式評価ではない）：** CLIの`pac connection list --environment c1719a14-6cac-eb5b-b598-2254af1c64b7`は既存接続0件。`pac power-automate list-cloud-flows`はHTTP 400で、既存フローの有無は未確認。ユーザーが今回に限り読み取りを許可したSharePointプラグインの`list_drives`は成功し、業務用OneDriveの「ドキュメント」ライブラリと内部用の`PersonalCacheLibrary`を返した。前者をT-002の保存先候補とする。保存先の存在だけを確認した結果であり、Excelの作成権限・フロー接続・転記・費用の検証ではない。ファイル・接続・フローの作成や変更は未実施。
- **2026-09-25 09:39 JSTのローカルひな形確認（正式評価ではない）：** スプレッドシートのスキルと同梱のArtifact Toolで、上記の検証用`.xlsx`を作成。ワークブックは1シート・1表で、表名`Evidence`と範囲`A4:G4`をXLSX内部の表定義でも確認した。描画画像で見出しの欠けがないことを確認。数式エラー検索は0件。クラウド上での表認識、2冊作成、全文転記と開き直しは未実施。C01・C02は未確認、正式な不合格回数は未加算。
- **2026-09-25 09:44 JSTのクラウド配置（正式評価ではない）：** ユーザーがT-002のAI実行を許可し、SharePointプラグインでOneDrive「ドキュメント」に検証専用フォルダー`架空ログ証跡アプリ検証-T002-20260925`を1つ作成、上記の架空Excelひな形を1冊アップロードした。アップロード結果のファイルサイズはローカルと同じ4663バイト。結果用2冊、フロー、接続はまだ作成していない。CLI 2.12.2の`pac power-automate help`は一覧・アクション・履歴の照会のみで作成コマンドなし。利用可能なMCPにもフロー作成ツールは見当たらない。事前報告後、ブラウザでPower Automateの検証用フロー編集画面を開き、名前と手動起動・件ID入力を設定したが保存は未実施。Dataverse接続のサインイン直前で、接続の権限範囲についてユーザーへ実行時確認中。フローの実行・Excel転記は未実施。C01・C02は未確認、正式な不合格回数は未加算。
- **2026-09-25 10:00 JSTのフロー編集（正式評価ではない）：** ユーザーはDataverse、OneDrive for Business、Excel Online (Business)の3接続作成を許可した。Power Automate画面でDataverse接続を作成し、手動起動の`CaseId`、T-001検証表のID指定取得、添付を保持する「メモ」のID指定取得を未保存フローに設定した。後者の行IDは検証用A・Bの件IDから各添付IDだけに対応付け、その他は存在しないGUIDへ送る式とした。添付IDは`pac org fetch`でA・Bの親行IDに絞ったFetchXMLから確認した。最初に「メモ」表を一覧取得する操作を試みた際、自動審査が架空2件以外の個人・業務データを読み得るとして拒否したため、一覧取得アクションを未保存フローから除き、再試行しなかった。OneDriveのファイル複製アクションを追加したが、接続認証ポップアップ内の既存アカウント選択がブラウザ操作で応答せず、接続は未作成。保存ボタンは接続欠如と必須ファイルパス2項目でエラーになり、フローは**未保存・未公開・未実行**。Excel Online (Business)接続は未着手、結果用2冊も未作成。ブラウザ操作での認証完了はユーザーに具体的な1クリックを依頼中。C01・C02は未確認、正式な不合格回数は未加算。
- **2026-09-25 10:21 JSTのクラウド試験（正式評価ではない）：** ユーザーが認証ポップアップを操作し、OneDrive for BusinessとExcel Online (Business)の接続を作成。Dataverseと合わせ3接続がPower Automate画面で「接続されています」と表示された。検証フロー`T002検証_架空ログからExcel`（ID`7f1b320f-a8b0-1884-2957-1a9dffb91d02`）を保存。内容は手動の件ID入力、検証用Dataverse行と既知の添付IDの1件取得、OneDriveのひな形複製（上書きなし）、複製結果IDを用いたExcel表`Evidence`への1行追加。公開ボタンは操作していない。架空ログAだけで実行ID`08584113081580581615531092475CU21`を開始。Dataverseの2取得とOneDrive複製は成功し、A専用Excelが1冊作成された。Excel書込は1回目が`BadGateway`、既定の自動再試行の2回目も`BadGateway`。取消操作を試みたが完了は確認できず、実行全体は依然「実行中」。プラグインでA用Excelを開き直した時点では見出しのみで本文行なし。CLIの`pac power-automate list-flow-runs --workflow-id ... --json`は`null`を返し判定不能。今後の実行向けにExcel書込アクションの再試行ポリシーを「なし」に変更して保存したが、既に動いているAには反映されない。Aの再実行・Bの開始はしない。C01・C02は未確認、正式な不合格回数は未加算。
- **2026-09-25 10:24～10:32 JSTの失敗原因確認（正式評価ではない）：** ユーザーの画像と実行画面に「フロー実行が取り消されました」と表示され、キャンセルボタンが無効になった。Aの旧Excelには本文行なし。Excel Onlineで元ひな形を開くと「ブックが修復されました」と表示され、技術詳細で`/xl/tables/table1.xml`内のテーブルが削除された。最初のXLSXはローカルの表定義・描画検査に通ったが、クラウドの表としては使えなかった。Artifact Toolの表出力を今回の用途に利用できないと判断し、`openpyxl`でひな形を作り直した。見出しだけの表もExcel Onlineで修復されたため、`CaseId=__TEMPLATE__`の仮行を含めて`A4:G5`の表とし、Excel Onlineで修復警告なしに開くことを確認した。旧ひな形を同名で差し替えたが、Aの旧結果はこの時点では変更していない。
- **2026-09-25 10:38～10:39 JSTのB実機試験（正式評価ではない）：** 同じ検証フローのExcel操作を「表に行を追加」から`CaseId=__TEMPLATE__`をキーとした「行の更新」に変更し、自動再試行を「なし」に設定して保存。フローチェッカーはエラー0・警告0。Bの件IDを1回送るとB専用ブックが作成され、プラグインで開き直した結果はBの1行だけ。件ID、`mock-log-b.txt`、環境`検証環境B・件別一覧`、サーバー`架空サーバーB`、実行回`8`、対象処理日`2026-09-23`、架空ログ本文2行と末尾改行が元データと一致した。別件Aの行なし。フロー全体の実行履歴画面は応答せず最終表示は未確認だが、Excelへの実反映は確認済み。
- **2026-09-25 10:43～10:45 JSTのA再試験（正式評価ではない）：** 初回Aの取消表示と旧Excelの空内容を確認してから、検証の1回に限りコピー操作の「上書きする」を「はい」に変更。Aの件IDを1回送るとフロー画面で「正常に実行されました」、Dataverse取得・OneDrive複製・Excel行更新が成功。A用ブックは同名の新しいアイテムとして作り直され、旧アイテムのURLは無効になった。プラグインで新アイテムを開き直し、Aの1行だけに件ID、`mock-log-a.txt`、環境`検証環境A・修正`、サーバー`架空サーバーA`、実行回`1`、対象処理日`2026-09-24`、架空ログ本文2行と末尾改行が一致し、Bの行がないことを確認。試験直後にコピーの上書きを「いいえ」に戻して保存し、通常時の重複上書きを禁止した。最初のAは失敗事例として履歴に残すが、空の旧結果ブック自体は新アイテムへ置換した。これは過去の正常な証跡を保護できることの検証ではなく、T-003で扱う。正式な不合格回数は未加算。
- **2026-09-25の独立レビュー（T-002の方式検証）：** レビュー担当がローカルのひな形・作成スクリプト・資料を照合し、最初の記録にあったC02の異常件混在まで確認済みに見える表現と、ブラウザ・Excelの版の未記録を指摘。C01の正常転記部分に限定し、版は次の実機検証で採取すると修正した。続いて、ユーザーが承認したSharePointプラグインの読み取りで指定A・Bの結果ブック各1件だけを開き直し、各1行の件ID・元ファイル名・環境・サーバー・実行回・対象処理日・本文2行と末尾改行を元の架空ファイルと照合した。別件混入はなし。重大な不整合なし。フロー設定、初回Aの旧ブック、B実行履歴の最終表示、長文、再実行安全性はレビュー対象外であり、この判定をT-003や完成品C条件の合格に流用しない。クラウド変更・フロー実行・ブラウザ操作はレビューでは行っていない。

#### 完了記録

- 変更内容：検証用のExcelひな形を1件の仮行付き表へ作り直し、同じ検証フローで件別コピー後に仮行を更新する方式に変更。A・B各1冊を作成。通常のコピーは上書きなし、Excel行更新は自動再試行なし。
- 自動検証・ブラウザ・実機確認の結果：フローチェッカーはエラー0・警告0。Excel Onlineで修正版ひな形を修復なしで開き、プラグインでA・B各冊の1件だけの全文・識別情報と別件混入なしを確認。初回Aの失敗・取消と安全な再実行一般、長文は未合格。
- 確認した対象版：Developer環境の検証フロー`7f1b320f-a8b0-1884-2957-1a9dffb91d02`の2026-09-25保存版と、検証用OneDriveのA・B各冊。版番号、OS・ブラウザの正確な版は未記録。
- ユーザーの受入結果：未確認。
- 残る課題と、その引き継ぎ先：長文上限、結果不明と再実行・過去証跡保護をT-003・T-004へ、費用と片付け方をT-006へ渡す。T-002は短い架空ログ2件の方式検証として完了。完成品のC条件、長文と安全な再実行の合格を意味しない。

### T-003：異常件・結果不明・重複・再実行を確認する

- 目的と今回の範囲：正常ログと日付・識別情報に問題がある架空ログを混ぜ、問題の件だけ止まるか確かめる。保存・Excel作成・書込失敗と結果不明を再現し、反映済みの誤表示、重複、過去証跡の消失なしに対象件だけ再実行できるか確認する。
- 今回変更しないこと：未検証の回復方法を安全と呼ばない。結果不明のまま同じ書き込みを自動再試行しない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQ-02～REQ-04、R05・R06／C02～C04・C08・C09。関連する4画面の設計は版 UI-20260924-01 で承認済み。検証計画、基準画像・対応テストは未作成。
- 担当と先に必要な作業：技術確認担当、テスト観点担当、独立レビュー担当、統括。T-001、T-002。実担当は着手時に割り当てる。
- 未決定・ユーザーの判断待ち：同じ環境・サーバー・実行回・対象処理日の組合せを1件とする業務上の基準は2026-09-25にユーザー確認済み。保存先での一意性の実現、結果不明時の既存Excelの確認方法は未決定。通信結果不明の補足条件を正式採用するかは別判断。
- 確認する操作・期待結果・対象端末：異常件と正常件を同時開始→異常件だけ止まる。書込失敗・結果不明→反映済みにしない。同じ件を再登録・再実行→重複・証跡消失なし。Cのフロー実行画面に表示されたUser-AgentはGoogle Chrome 153／Windows。OSとExcel Onlineの正確な版は未記録。
- 着手前に整理した安全な検証方法：現行のT-002検証フローは1回に件IDを1つ受け取り、既存ファイルを上書きしない設定でExcelへ転記する。したがって同じ件IDを再送した場合にコピーが失敗するか、その後の行更新が動かないか、既存Excelの内容が不変かを確認するのが重複試験の候補。ただし現行フローに完成版の状態保存・本文日付検査・異常件を含む複数件の独立処理はなく、この候補だけでC02～C04を合格にしない。書込結果不明の安全な再現方法は未設定であり、既存の正常な証跡を上書きして再現しない。
- **2026-09-25 11:00～11:12 JSTのC準備と初回作成（正式評価ではない）：** ユーザーが、Developer環境の検証専用Dataverse表へ架空ログCを1件保存し、同じ件IDでフローを2回起動して重複を試す範囲を承認。`samples/t003/mock-log-c.txt`を検証フォームで添付し、環境`検証環境C・重複試験`、サーバー`架空サーバーC`、実行回`3`、対象処理日`2026/09/25`を入力して保存。CLIの`pac org fetch`を認証キャッシュにアクセスできる実行環境で用い、保存行ID`8992ebfd-84b8-f111-b377-7ced8d3141aa`と添付1件のID`8e92ebfd-84b8-f111-b377-7ced8d3141aa`を取得した。通常サンドボックスのCLIは認証キャッシュのロックファイルへアクセス拒否。検証フローの添付ID式にCを追加した最初の編集は画面表示だけで永続化されず、1回目のフロー実行ID`08584113048957185802411656538CU23`は古い式のゼロIDで添付取得が`NotFound`。Excelコピーと書込はスキップされ、Cの結果ファイルは未作成。式を再入力し、保存後にページを再読込してCの条件が残ることを確認。フローチェッカーはエラー0・警告0。2回目の実行ID`08584113046214150289711407801CU53`は実行履歴で成功、CのExcel1冊を作成。SharePointプラグインで実内容を開き直し、Cの件ID、ファイル名、4項目、対象日、本文2行と末尾改行が元ファイルに一致することを確認した。C結果のアイテムIDは`01VTXCECEKQGJMOWYHINALGT27WEDNL2HO`、確認時点のQuickXorHashは`6GPA89jn3jDF8TGlk2cJBDRiuyk=`、サイズ8045バイト。A・Bのファイルは操作していない。
- **2026-09-25 11:17～11:19 JSTのC重複再送（正式評価ではない）：** ユーザーが追加1回の起動を許可。同じCの件IDで3回目のフローを起動した実行ID`08584113042339065918689600470CU21`は618ミリ秒で失敗。Dataverseの件・添付取得は成功し、OneDriveの「パスを使用したファイルのコピー」は「宛先ファイルが既に存在しています」で失敗、Excelの「行の更新」はスキップ済み。SharePointプラグインでフォルダーを再一覧し、Cの結果ブックは1冊だけと確認。既存ブックのアイテムID、サイズ8045バイト、QuickXorHash`6GPA89jn3jDF8TGlk2cJBDRiuyk=`、更新日時`2026-09-25T02:11:42Z`と実内容は再送前と一致。**この検証フローの上書き防止だけ確認**。同一件の登録防止、結果不明・異常件混在、安全な失敗件だけの再実行は未確認。
- **2026-09-25の再現方法調査（閲覧のみ）：** CLI 2.12.2の`pac power-automate list-flow-actions`は通常実行で`System.InvalidOperationException`、認証キャッシュへアクセスできる実行でも`No results returned`となり、このフローの設定確認・試験起動には使えなかった。利用可能なCanvas Authoring MCPにはフローの失敗注入・実行操作がない。Microsoft公式の[Excel Online (Business)コネクター](https://learn.microsoft.com/en-us/connectors/excelonlinebusiness/)は、行操作の成功応答後も反映に遅れがあり、タイムアウトと自動再試行で重複が起こり得ると説明する。[フローデザイナーの設定](https://learn.microsoft.com/en-us/power-automate/flows-designer)ではアクションのタイムアウト設定は個々の要求のタイムアウトを変えない。よって、設定値を短くして安全に「書込結果不明」を再現できるとは判断しない。既存Aの初回はExcel書込失敗と空の結果ブック、Cの再送はコピー失敗と既存内容不変の具体例だが、完成アプリに件別の失敗・結果不明状態や安全な再実行はまだ実装されていない。正常なA・B・Cを変更する障害注入は行わず、結果不明の分岐はT-004で設計しT-009のTDD・T-011の結合確認へ引き継ぐ。クラウドでの安全な障害注入方法は未設定・未実施。
- 関係する画面・データ・課金・資料：受付・進捗一覧・件別確認、架空ログ・Excel、REQUIREMENTS.md。
- 正式な不合格回数：未確認（正式評価は未実施。停止上限は初回を含め3回）。
- 次の行動：既存A・Cの失敗証拠をT-004の状態設計に渡す。結果不明は実サービスの障害を無理に起こさず、まずT-009で応答不明を入力とする状態分岐を先にテストする。異常件混在、登録時の重複判定、安全な失敗件だけの再実行は独立した架空件を使う方法と実行範囲をT-007で確定する。
- 進行順序の変更：2026-09-25にユーザーが、T-003の未確認を合格扱いせず、T-004の状態・回復設計を先に行い、T-009でテスト先行の処理実装、T-011で実機確認する順番を承認。T-003の評価・未実施項目とC02～C04の未確認判定は維持する。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C02～C04・C08・C09 | 異常件の停止、結果不明、再実行、保存失敗、取消後の状態 | 未確認 | Developer環境の検証フロー2026-09-25保存版 | 初回AではExcel書込が失敗し空の結果ブックが残った。Cの1回目は添付ID設定が残らずExcel前に失敗、2回目は正常Excel1冊。3回目の同一ID再送ではコピーが既存ファイルを理由に失敗し、行更新をスキップ。Cの1冊の内容・ID・ハッシュは不変。完成版の件別状態、同一件登録防止、結果不明・異常件混在・安全な再実行の評価は未実施。 |

#### 評価・修正履歴

正式評価は未実施。11:00～11:19 JSTのCの準備・試験の記録は上記。レビュー前の個別失敗を正式な不合格回数へ加算しない。条件別の評価、指摘と修正、残る問題は正式評価時に追記する。

#### 完了記録

- 変更内容：作業中。検証用Dataverse表に架空ログCを1件保存し、既存フローの添付ID式へCを追加した。
- 自動検証・ブラウザ・実機確認の結果：フローチェッカーはエラー0・警告0。Cの1回目は式が永続化されず失敗しExcel未作成、再編集後の2回目は正常実行とExcel実内容一致。3回目の重複再送はコピー失敗・行更新スキップ、既存CのExcel1冊と実内容・ID・ハッシュ不変を確認。
- 確認した対象版：Developer環境の検証フロー`7f1b320f-a8b0-1884-2957-1a9dffb91d02`の2026-09-25保存版。版番号は未記録。
- ユーザーの受入結果：未確認。
- 残る課題と、その引き継ぎ先：検証した方式と未解決の失敗をT-004へ渡す。

### T-004：件別データ・処理状態・失敗後の回復方法を設計する

- 目的と今回の範囲：検証結果を基に、1件の識別情報、対象処理日、ログとExcelの参照先、保存・転記・反映・担当者確認・レビューの状態、失敗理由、重複防止と再実行の順序を決める。
- 今回変更しないこと：検証で通らなかった方法を前提にしない。完成条件を弱めない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQUIREMENTS.md「主要操作」「データと架空サンプル」、R01～R08・R12～R14／C01～C06・C08・C09。4画面の設計は版 UI-20260924-01 で承認済み。技術・状態設計、承認済み実装計画、基準画像・対応テストは未作成。実装計画は草案のみ作成済み。
- 担当と先に必要な作業：設計担当、独立レビュー担当、統括。T-001～T-003で安全に確認できた証拠を使用し、未確認の実動作はT-009・T-011へ渡す順番を2026-09-25にユーザー承認済み。実担当は着手時に割り当てる。
- 未決定・ユーザーの判断待ち：業務上の重複基準は4項目、取消済みの保存件のやり直しは同じ件IDでログ差し替え、開始後の再実行前に直すのは処理原因だけと確定。長文を1冊の`Evidence`表に分割する論理形式は設計案として具体化したが、実ひな形・コネクターでの動作は未確認。保持期間、取消時の仮置きログ、保存先での一意性・条件付き更新の実現、結果不明時の実際の復旧、未承認の画面補足条件は未決定。
- 確認する操作・期待結果・対象端末：正常、日付不一致、保存失敗、書込結果不明、差し戻し、再実行を状態表で最後まで追える。文書レビューが中心で、実動作はT-011で確認する。
- 関係する画面・データ・課金・資料：全画面が参照する件別データ、架空ログ・Excel、T-001～T-003の証拠。
- 正式な不合格回数：未確認（停止上限は初回を含め3回）。
- 次の行動：状態・データ・再試行の論理設計を独立レビューで照合し、残る技術的な実現方法と実機試験をT-007・T-009・T-011へ明示して渡す。画面の未決定事項はT-005へ渡す。
- 2026-09-25の作業：T-004の設計案に、4項目が同じで元ファイル名・追跡IDだけ違う登録の拒否、同時保存、拒否件の後段未作成、長文全文の分割・復元候補、取消済みの保存件を再選択した場合の未決定事項を追加。仕様で決まっていない取消後の扱いは実装側で確定しない。T-003の未確認を引き継ぐ順番はユーザー承認済み。
- 設計草案の独立レビュー：取消と確定保存失敗を同じ回復行にまとめて再保存を許していた点、保存済み件の開始前修正で4項目が重複する場合の検査漏れ、T-002の長文に関する通過条件が短文の実績より広い点を指摘された。`docs/IMPLEMENTATION_PLAN.md`と`docs/CHECKS.md`を修正し、レビュー担当が該当箇所に新たな矛盾がないことを再確認した。文書の読み取り照合であり、実機の合格ではない。
- 2026-09-25の追加判断：ユーザーは保存済み件の開始前取消後、同じ4項目でやり直す場合「元の件を再開してログを差し替える」を選択。二件目を作らず、差し替えが確定するまで処理しない案を`docs/REQUIREMENTS.md`、`docs/ui/screens/受付・対象確認.md`、`docs/IMPLEMENTATION_PLAN.md`、`docs/CHECKS.md`へ反映した。旧ログの扱いと保存失敗・結果不明の実動作は未決定・未確認。
- 追加判断の独立レビュー（当時の案）：取消件を無条件に処理しないという記述は再開後と矛盾するため、C09・TR-04・受付画面・CHECKSで「取消状態の間」に限定した。当時は差し替え保存の失敗・結果不明時に取消状態を維持すると想定していたが、応答不明時まで断定できないため、後記12:24 JSTの設計修正に置き換えた。具体的な旧ログの保護・回復方法と実機結果は未決定・未実施。
- 差し替えの回復案（当時の案）：同じ件IDで差し替えを試み、失敗・応答不明なら取消状態のまま旧ログのみ／新ログのみ／両方／添付なし／読出不能を調べると記載した。この「応答不明でも取消状態のまま」という前提は後記12:24 JSTに撤回し、現行案は`docs/IMPLEMENTATION_PLAN.md`のT-004設計案を参照する。Dataverse添付の実更新順序、旧ログの保持、削除・上書きの対象と復元方法は未確認であり、クラウド操作は未実施。
- 部品間の設計案：完成版の件表と環境・サーバーから保存先を引く事前登録表をDataverseの候補とし、件IDを受けたフローが保存先の件・添付・対応先を読み直す方式を記載した。同じ件の二重起動時に後続の失敗が先行する実反映済み状態を壊さない検査をCHECKSのTR-02・TR-06へ加えた。実表・フローの作成と競合試験は未実施。
- 2026-09-25の日付形式の判断：ユーザーは完成版の架空ログの冒頭に`処理日: YYYY-MM-DD`を1行置き、指定した対象処理日と照合する方式を選択。`docs/REQUIREMENTS.md`のデータ節、`docs/IMPLEMENTATION_PLAN.md`のT-004案、`docs/CHECKS.md`のTR-04・C02へ反映した。既存A・B・Cのサンプルは日時行だけで日付照合には使えず、T-006で新形式の架空データを用意する。日付検査の実装・テストは未実施。
- 2026-09-25の受付画面の追加判断：ユーザーは開始受付の返答が途切れた件に「開始状況を確認しています」を表示し、修正・取消・ログ差し替え・再開始を一時停止する案を承認。保存先で受付済みと確認できればSCR-02へ、未受付と確認できればSCR-01の開始前操作へ戻し、判定不能なら保留を続ける。`docs/ui/screens/受付・対象確認.md`と`docs/CHECKS.md`のC09へ反映した。自動照会のタイミングと通信失敗時の案内、実装・実機結果は未決定・未実施。
- 最終追加分の独立レビュー：保存先・事前登録の読直し、差し替え失敗時の取消状態維持、二重起動の結果保護は資料間で整合し、実施済みの過大主張はないとの確認。受付画面の概要と取消行で「取消した件は処理しない」が無期限に読める表記だけ、他の資料と同じく「取消状態の間」に直した。これは文書レビューであり、状態の実動作は未実施。
- T-004全体の独立レビュー：T-005へ未確定の設計入力としては渡せるが、T-004完了には開始受付時の保存先での境界・競合拒否と、事前登録変更後も同じ件を別の保存先へ転記しない条件が不足との指摘。Dataverseの楽観的同時実行制御とWeb APIの`If-Match`をMicrosoft公式資料で確認したが、Canvas／フローから使える実手順と利用権は未検証。計画案には開始受付済み・受付結果不明の状態、古い版による修正・取消・差し替え・二重開始の拒否、添付IDと版または内容の検査値、初回転記先の固定を追加し、CHECKSのTR-03・TR-06に競合・事前登録変更の試験を結び付けた。T-004は完了扱いにしない。
- 開始受付の応答不明時の追加レビュー：受付済みか未受付か不明な間に修正・取消・差し替え・再開始を許すと、開始時に固定すべき添付と保存先を変え得るとの指摘。`docs/IMPLEMENTATION_PLAN.md`の開始受付状態案、`docs/REQUIREMENTS.md`のTR-03草案、`docs/CHECKS.md`のTR-03とT-008のテスト予定に、保存先の照会で受付の有無が確定するまで操作を保留する条件を反映した。画面の正確な表示・回復操作はT-005待ち。条件付き更新の実装と失敗時の実動作は未設定・未実施。
- 12:20～12:24 JSTの調査と修正：`pac org who`は認証済みアカウントへの接続を返し、CLI自体は動作。PAC CLI 2.12.2の`power-automate help`ではクラウドフローの一覧・アクション・実行履歴の閲覧コマンドのみ確認し、フロー作成コマンドは確認できなかった。Microsoftの[Dataverse代替キー資料](https://learn.microsoft.com/en-us/power-apps/maker/data-platform/define-alternate-keys-reference-records)で、複数列の一意キー、空値では一意性が効かないこと、索引が`Active`になるまで使えないことを確認。4項目必須・代替キー候補と検査を計画・CHECKSへ追記したが、完成版の表・キー作成と実際の拒否動作は未実施。新しい架空ログのUTF-8、BOM、LF／CRLFの扱いも技術案として追記し、実処理は未実施。
- 差し替え応答不明の設計修正：以前の草案には「応答が不明でも取消状態を維持」とあり、保存先で有効添付の切替が完了して返答だけ失われた場合を説明できなかった。応答不明時は取消維持・差し替え成功のどちらも推定せず、件の状態・有効添付ID・新旧ログの実物を再照会する案へ修正。確定まで開始と自動再送を止め、差し替えが確認されても開始は別操作にする。`docs/IMPLEMENTATION_PLAN.md`と`docs/CHECKS.md`のTR-03を更新した。添付の実保存順序と切替方法は未検証。
- 12:33 JSTのMCP読み取り：Canvas Authoring MCPでDeveloper環境の検証用アプリへ再接続し、`T001検証ログ受付`が書込可能な`CdsNative`接続であることと、添付コントロールに追加・削除の入力があることを確認。`get_data_source_schema`では`{Attachments}`、メモ関連、行の`versionnumber`が見えたが、既存行の差し替えや条件付き更新の動作を証明するものではない。ローカル`log-evidence-app/Screen1.pa.yaml`の現行フォームは`FormMode.New`、保存後に`NewForm`へ戻す構成であり、保存済みの元の件を編集して添付を差し替える動作は未実装・未検証。画面・クラウドへの変更、ブラウザ／コンピュータユースは行っていない。T-004の差し替え候補とT-007の技術確認へ引き継ぐ。
- 12:36 JSTの同期照合：Canvas Authoring MCPの`sync_canvas`でサーバーの3 YAMLを`outputs/t004-canvas-snapshot/`へ別保存し、ローカルの`log-evidence-app/`と比較した。`App.pa.yaml`と`_EditorState.pa.yaml`はハッシュ一致。`Screen1.pa.yaml`はローカル版の入力欄4つに明示された`DelayOutput=false`が同期版にはない点だけ異なる。既存のローカルファイルもサーバー上のアプリも上書きしていない。実動作への影響は未確認であり、T-007で完成版の編集元と反映方法を決める前に照合する。CLI 2.12.2の`pac canvas help`にアプリのアップロード命令は見つからず、MCPの同期・検査だけでクラウド保存までできるとは扱わない。
- 12:40 JSTのT-004設計レビュー：文書設計としても、論理データの項目・型と遷移、取消後差し替えの中断、Excel途中失敗の再実行範囲、長文のExcel表形式、再依頼時の差し戻し理由が未決定と指摘された。実表やコネクターの実証へ渡す前に、論理設計をT-004で確定する必要がある。計画草案に件表・環境とサーバーの対応表・既存メモ添付の最小候補と、直近の差し戻し理由を再依頼時に残す案を追記した。実体の作成、状態遷移の実装、実機試験は未実施。
- 2026-09-25のC04追加判断：ユーザーは途中までExcelができた件や書込結果不明の件を確認待ちとして止め、Excelが存在しないと確定した失敗件など安全な件だけを修正・再実行する範囲を承認。`docs/REQUIREMENTS.md`のC04・TR-03、`docs/ui/screens/件ごとの確認.md`のS03-A2、`docs/CHECKS.md`のC04、`docs/IMPLEMENTATION_PLAN.md`の再実行判定へ反映した。質問にある「要確認」は処理を止める意味として扱い、画面に出す正確な状態名は既存のSCR-02/SCR-03の承認済み「結果未確認／未確認」とT-005で照合する。実動作・テストは未実施。
- 12:45 JSTの論理設計の具体化：`docs/IMPLEMENTATION_PLAN.md`に件表・環境とサーバーの対応表・既存メモ添付の最小構成、開始要求IDと固定先、差し替えの4段階の条件、再依頼時に直近理由と依頼回数を保つ遷移、1冊の`Evidence`表に30,000 UTF-16コード単位以下で順序付き分割する長文形式を記載。`docs/REQUIREMENTS.md`のTR-05と`docs/CHECKS.md`のTR-01・TR-05へ機械検査を結び付けた。これらは論理設計であり、実表、既存添付の差し替え、長文Excel、競合拒否を実装・検証した結果ではない。T-004の完了判定は独立レビュー後に行う。
- 12:49 JSTの再レビューと補正：先の5点は論理案として具体化したが、開始受付済みの件の安全な再実行は初回受付をやり直さず、新しい試行として受け付ける条件が欠けていると指摘された。`docs/IMPLEMENTATION_PLAN.md`に件ID・開始要求ID・固定添付と転記先を維持する試行ID／番号、先の実行終了とExcel未作成の確認、条件付きの試行更新、旧試行の遅延応答の拒否を追加。`docs/REQUIREMENTS.md`のTR-03と`docs/CHECKS.md`のTR-02・TR-03・C04の試験に結び付けた。長文TDDの「採用時」と旧い「方式未設定」の表記も論理案と実機未実施を区別するよう修正。条件付き更新の実現、実際のフロー状態と外部処理の副作用がないことの判定、実機結果は未設定・未実施で、T-004の完了判定は引き続き保留。
- 12:52 JSTの仕様差異と回答：C04の「失敗した件を修正して再実行」と、開始受付後のログ・4項目の固定が、開始後に誤りを見つけたログの扱いで両立しなかった。二案を示して確認し、ユーザーは**開始後は処理の失敗原因だけ直し、ログ・4項目は開始前に直す**案を選択。`docs/REQUIREMENTS.md`のC04、`docs/IMPLEMENTATION_PLAN.md`の再試行境界、`docs/ui/screens/件ごとの確認.md`の再実行条件をこの意味へ更新した。技術的な再試行判定と実動作は未実装・未検証。
- 12:57 JSTの独立再レビュー：再試行IDと旧試行の遅延応答対策、C04の開始後データ固定は資料間で整合。一方、転記先が事前登録されていない件を受付前に止める記述と、受付後の本文検査で止める記述が混在と指摘された。論理案は**各件の開始受付前に転記先を一つに解決**し、未登録件は開始未受付・理由付き停止でフローとExcelを作らず、登録済みの別件は続ける方式へそろえた。登録是正後はその件の初回開始、受付済み件は固定した旧先を使う。`docs/IMPLEMENTATION_PLAN.md`の状態表と`docs/CHECKS.md`のTR-06に反映。未登録件の正確な画面表示・遷移はT-005、競合時の更新方法と実機動作はT-007・T-011待ち。
- 12:57 JSTのCLI読み取り：PAC CLI 2.12.2の`pac solution help`でソリューションのimport/export等を確認。`pac solution list`は通常サンドボックスでは認証キャッシュのロックファイルを読めず失敗したが、読み取り専用で許可された環境では既定ソリューション・Common Data Services Default Solution・AI Sample Dataの3件を表示した。今回のアプリ／フローをCLIでパッケージ化・取り込みできることは未確認。作成・変更・公開は行っていない。
- 初期の設計案（後続の12:45～12:49 JSTの具体化前）：`docs/IMPLEMENTATION_PLAN.md`の「T-003の結果から作るT-004の設計案」に、件別ID・日付・状態の分離と失敗後の判定表を記載。既存A・Cの試験とMicrosoft公式資料を根拠にした**候補**。同じ4項目で元ファイル名・追跡IDだけを変えた2件目と同時保存の拒否、拒否件の添付・Excel未作成を検査案へ追加した。長文形式と安全な再実行条件は後続の12:45～12:49 JSTの記録に更新した。完成版の実表・ひな形・長文実機確認は未設定・未実施。同一件を判断する4項目の組合せはユーザー確認済みだが、保存先での一意性は未実証で、T-004の論理案を採用技術で実現できるかT-007で確認する。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C01～C06・C08・C09 | 完成アプリで件別データ、状態、失敗後の回復が動作する | 未確認 | 完成版は未設定 | T-004の論理設計と独立した文書レビューは完了。完成版の実表・フロー・画面、クラウドでの競合と結果不明、安全な再実行、Excel全文反映は未実装・未評価。 |

#### 評価・修正履歴

2026-09-25 13:02 JST、T-004の**論理設計という作業範囲**を正式評価し合格。対象版は同時点の`docs/IMPLEMENTATION_PLAN.md` T-004案、`docs/REQUIREMENTS.md` C04・TR-01～TR-06、`docs/CHECKS.md` TR・C確認案。件IDと4項目の一意性、添付・転記先の固定、状態の分離、取消後の差し替え、未登録件の開始前停止、安全な再試行の試行IDと旧結果の拒否、長文の複数行、レビュー差し戻しの記録について状態と失敗時の順序を追えることを確認した。独立レビューでは最後の事前登録の境界を補正した後、論理設計を妨げる高影響の欠落なしと判定。文書内容・参照・`git diff --check`を確認し、コード用テストは文書のみの変更には適用していない。初回の正式評価は合格のため、正式不合格0回。上表のC条件は完成アプリの動作条件であり、この合格から転用しない。条件付き更新・Dataverse添付差し替え・Excel長文・フロー再試行の実現方法と費用・実機試験はT-007・T-009・T-011へ渡す。

#### 完了記録

- 変更内容：件別の論理データ、受付・状態・回復・再試行・長文・レビューの設計とTR検査の対応を記録。C04の仕様差異はユーザー回答で解消。
- 自動検証・ブラウザ・実機確認の結果：文書差分の`git diff --check`は合格。コード用自動テスト、ブラウザ・実機はT-004の文書評価には未実施であり、完成アプリの証拠ではない。
- 確認した対象版：2026-09-25 13:02 JSTの作業ツリー資料。実装版・Build IDは未設定。
- ユーザーの受入結果：C04の修正範囲ほか個別判断は確認済み。完成アプリの受入は未確認。
- 残る課題と、その引き継ぎ先：画面の文言・操作・遷移をT-005、保存先と架空データをT-006、条件付き更新・Dataverse添付・長文Excel・フロー再試行の技術と費用をT-007、実装と実機確認をT-008～T-011へ渡す。

### T-005：受付・進捗・担当者確認・レビューの画面を設計する

- 目的と今回の範囲：ログの選択・件別入力、進捗一覧、ログとExcelの照合、処理結果の判断とコメント、レビュー依頼・差し戻し・再依頼を、どの画面でどう操作・表示するか決める。
- 今回変更しないこと：要件と承認済み画面設計にない文言・機能・項目・操作の意味・状態・遷移を増やさない。設計変更が必要なら理由と案を先に示す。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQUIREMENTS.md「画面に必要な役割と情報」、REQ-01～REQ-06、R07～R11・R14／C01・C05・C06・C08～C10。共通ルールはdocs/ui/rules.md。承認済み設計版 UI-20260924-01 と2026-09-25の追加承認はdocs/ui/screens/の4画面へ反映済み。受付・対象確認の参考画像はB-20260924（同資料から参照）。後続3画面の参考画像、全画面の基準画像と対応テストは未作成。
- 担当と先に必要な作業：画面設計担当、独立レビュー担当、統括。T-004。実担当は着手時に割り当てる。
- 未決定・ユーザーの判断待ち：保存結果を安全に照会する技術方式と費用、成功・入力不正時の具体的な案内文言は未確定。個別の保存・開始操作、再起動復元、保存失敗確定件の同じIDでの再保存と結果不明中の禁止、進行中件がある場合のSCR-02表示はユーザー承認済みで画面仕様へ反映済み。実装・実画面の確認はT-007～T-011で行う。
- 確認する操作・期待結果・対象端末：主要操作1～6を画面仕様上で追い、件ごとの「保存」と「処理開始」を分け、再起動後の保存済み件・進捗復元と未保存入力の破棄、進行中件がある場合のSCR-02表示も条件化する。書込処理と実際の反映、転記照合と人の判断を混同しない。対象端末はT-007で定める。
- 関係する画面・データ・課金・資料：受付・一覧・件別確認・レビューの役割、T-004の状態、UIルールと画面仕様。
- 正式な不合格回数：未確認（停止上限は初回を含め3回）。
- 次の行動：承認済み仕様の反映と関連資料の同期は完了。残る保存・再起動復元の技術方式確認、具体的な未決定文言、実装と実画面の確認をT-007～T-011へ引き継ぐ。
- 2026-09-25の追加確認：開始受付の返答が途切れた件の表示・操作保留・保存先照会後の遷移をユーザーが承認し、`docs/ui/screens/受付・対象確認.md`へ反映した。取消済み件のログ差し替えを既存の「修正」から行う案は当初回答待ちとし、後記13:05 JSTの回答で決定。その他の未決定欄と採用技術の実現方法は残る。
- 2026-09-25 13:03 JSTのT-005追加判断：T-004は事前登録なし件を開始未受付で止め、別の登録済み件を進める論理設計。混在時にSCR-02へ進んだ後、未受付件の入力を直すSCR-01へ戻る導線がなかったため、SCR-02に「受付へ戻る」を追加し、全件未登録ならSCR-01に留める案を示した。ユーザーは追加を承認。`docs/ui/screens/進捗一覧.md`にS02-A3と未処理・理由の表示、`docs/ui/screens/受付・対象確認.md`に開始未受付件の表示と受付済み件の編集禁止、`docs/CHECKS.md`のTR-06に混在・全件未登録・戻りの試験を反映。実画面・遷移の実装と試験は未実施。
- 2026-09-25 13:05 JSTのT-005追加判断：ユーザーは、保存済み件を開始前取消した後に同じ件IDでログを差し替える入口として、既存の「修正」を使う案を承認。`docs/ui/screens/受付・対象確認.md`のS01-A1・A3・A4と件別状態へ、取消件を同じIDで選び、修正中のログ選択を新規件登録にせず差し替え候補として扱い、読戻しと有効添付切替が確認されるまで開始させない動作を反映。新ボタンなし。添付コントロールで旧ログ保持と切替を実現できるかはT-007、実動作はT-008・T-011で確認する。
- 2026-09-25のT-005追加判断：処理対象0件と全件取消後は「処理開始」を操作不可としてSCR-01に留める。ログ保存の返答が途切れた件には「保存状況を確認しています」と表示し、保存先で結果が確定するまで当該件の開始・再保存を止める。ユーザーが二案をそれぞれ承認。`docs/ui/screens/受付・対象確認.md`のS01-D3・A5・状態表と`docs/CHECKS.md`のTR-02・C08・C09の検査へ反映した。成功時と入力不正時の詳細文言、応答不明の実機再現方法は未決定・未実施。
- 2026-09-25のT-005レビュー導線と依頼条件：ユーザーはSCR-02の「レビューを確認」からSCR-04へ入り、同画面の「レビュアー役に切り替える」で1アカウントの実演役割を変える案を承認。レビュー依頼はExcel実内容が確認され、担当者の転記照合と処理結果の判断・コメントが両方保存された件だけ許す。照合不一致や判断異常・保留でも記録が揃えば依頼できる。`docs/ui/screens/進捗一覧.md`のS02-A2、`docs/ui/screens/レビュー.md`のS04-A1、`docs/ui/screens/件ごとの確認.md`のS03-A3、`docs/CHECKS.md`のTR-05・C06へ反映。コメントの空欄許可と結果訂正、実画面・実機確認は未決定・未実施。実演役割は権限制御の証明ではない。
- 2026-09-25のT-005入力とコメント判断：ユーザーは、環境・サーバーを有効な事前登録の組合せから選ぶ、実行回を初期空欄で1以上の整数にする、担当者コメントを「異常」「判断保留」で必須・「正常」で任意にする案を承認。`docs/ui/screens/受付・対象確認.md`のS01-I1～I3、`docs/ui/screens/件ごとの確認.md`のS03-I3・A3、`docs/CHECKS.md`のTR-03～TR-05に対応する自動・実機検査案を追加した。保存先の数値上限、候補の実表示、記録の保存・再表示は未設定・未実施。前記レビュー導線記録の「コメントの空欄許可は未決定」はこの回答で更新した。
- 2026-09-25のT-005レビュー最終状態の判断：ユーザーは、差し戻し後に担当者が転記照合・処理結果判断・コメントだけを修正保存してから再依頼し、開始後のログ・4項目・既存Excelを変えない案を承認。「確認済み」はこの実演で確定扱いとし、後から訂正する操作を設けない。`docs/REQUIREMENTS.md`のREQ-06・C06、SCR-03・SCR-04、`docs/IMPLEMENTATION_PLAN.md`のレビュー遷移、`docs/CHECKS.md`のTR-05・C06へ反映。画面・状態更新の実装と検査は未実施。
- 2026-09-25のT-005独立レビューと役割の回答：レビュー結果を保存してSCR-02へ戻った時点で、実演役割を担当者役へ戻す案をユーザーが承認。次回のレビューはSCR-04で再びレビュアー役へ切り替える。差し戻し後の担当者記録修正への導線をSCR-04と計画・CHECKSへ反映した。独立レビューが挙げた、SCR-03からSCR-04への入口、0件・保存結果不明・レビュー依頼条件の古い未決定表記も承認済みの仕様へそろえた。実画面の操作と保存状態は未実装・未検証。
- 2026-09-25の追加承認：件ごとの「保存」を「処理開始」と分けること、アプリ再起動時に保存済み件と進捗を復元し未保存入力を破棄すること、進行中件があればSCR-02、なければSCR-01を表示することが承認された。保存失敗が確定した件は同じ件IDで再保存し、保存結果が不明な間は再保存を禁止する。S01-A6・C10を含む対応資料へ反映中で、実装・再起動・保存結果不明の実機検証は未実施。この機能承認とは別に、T-007の最大8件の作成要求・読取・条件付き更新CLI試験は実行範囲承認済み・未実施。フロー・Excelの実操作は未承認・未実施。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C01・C05・C06・C08～C10 | 承認済み操作・状態を反映した画面仕様 | 未確認 | UI-20260924-01と2026-09-25の追加承認（設計のみ） | 4画面の基本設計と個別追加承認、件ごとの保存・再起動復元・同一ID再保存条件は画面仕様へ反映済み。T-004の論理設計は独立レビュー済み。技術方式は未確定で、完成版の実装・実画面検証・実物受入は未着手・未実施。設計承認を条件合格に流用しない。 |

#### 評価・修正履歴

2026-09-24：REQUIREMENTSの4つの画面役割を基に、各役割の目的・主操作・補助操作・必要／不要な情報・入口／出口を文章案へ整理した。受付のB案参考画像について採用する配置と採用しない仮の文言・機能を分けた。自己照合のみで、独立レビューと正式評価は未実施。不合格回数には加算しない。評価した日時、対象版、条件別結果、証拠、指摘と修正、不合格回数への加算理由、残る問題を実施時に追記する。

2026-09-24：4画面にSCR-01～SCR-04を付け、主要操作のREQ-01～REQ-06へ対応させた。目的、表示、入口と出口、操作、状態、入力、処理と遷移、使いやすさ、変更の境界を項目別・状態別の表で下書き保存した。未決定の文言・初期値・制限・結果・遷移は提案と分け、確定させていない。独立レビュー、ユーザー確認、実装、テストは未実施。

2026-09-24：ユーザーが4画面の仕様と見た目の方向を実装する設計として承認。版 UI-20260924-01 とし、各画面資料へ承認内容・日付、参考画像の保存先と版、改善可能な見た目の範囲を記録した。具体的に記載された文言・操作・状態・遷移を承認範囲とし、具体値のない未決定事項は残す。参考画像B-20260924は設計の方向のみを示し、基準画像ではない。承認は実装検証・実物受入ではなく、正式評価や不合格回数には加算しない。

2026-09-25：独立資料レビューで指摘された最新承認の現況表記、T-008Mのモック受入と完成版受入の混同、T-005の仕様反映状況を修正し、対象資料間で承認済み仕様の反映と未着手の実装・技術確認を区別した。これは文書のみの照合・修正で、T-005の正式な条件評価、完成版の実機確認、ユーザーの完成品受入ではない。不合格回数には加算しない。

#### 完了記録

- 変更内容：画面設計版 UI-20260924-01 と2026-09-25の追加承認は4画面の資料へ反映済み。T-005は画面設計作業として完了。技術方式・費用・一部の具体的案内文言は未確定で、実装・実画面確認・完成版受入は未着手・未実施。
- 自動検証・ブラウザ・実機確認の結果：未実施。
- 確認した対象版：UI-20260924-01（設計のみ）。
- ユーザーの受入結果：設計承認済み。実物受入は未確認。
- 残る課題と、その引き継ぎ先：保存・再起動復元の実現方式と費用、具体的な未決定文言、実装・実機確認をT-007～T-011へ渡す。T-007の実API試験は実行範囲承認済み・未実施。

### T-006：決めた保存先と架空の実演データを準備する

- 目的と今回の範囲：検証で通った保存場所・状態記録先・環境とサーバーの対応、Excelひな形、正常・異常・日付不一致・欠落・結果不明・差し戻しを再現する架空データを完成品用に準備する。
- 今回変更しないこと：実務資料・個人情報を入れない。課金・公開を既定の準備作業として扱わない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQUIREMENTS.md「データと架空サンプル」、R03・R04・R12／C01～C04・C07・C08。画面設計は版 UI-20260924-01 で承認済み。実装計画はT-007で作成する。
- 担当と先に必要な作業：外部準備担当、独立レビュー担当、統括。T-004、T-005。実担当は着手時に割り当てる。
- 未決定・ユーザーの判断待ち：保存先、利用権と費用、保持期間、実演データの初期化方法。外部サービスへの作成・変更は対象と影響を示して確認する。
- 確認する操作・期待結果・対象端末：架空サンプルだけで正常・異常・失敗例を再現でき、過去の証跡を残せる。対象端末はT-007で定める。
- 関係する画面・データ・課金・資料：保存先、事前登録、架空ログ・Excel、費用と利用条件、T-004・T-005の設計。
- 正式な不合格回数：未確認（停止上限は初回を含め3回）。
- 次の行動：準備済みの架空環境について、T-007の小さい試験でExcel Online適合、条件付き更新と接続方法を判定する。結果不明・差し戻しの再現はT-009～T-011へ渡す。既存A・B・Cの検証証拠を上書きしない。
- 2026-09-25のローカル準備：`samples/t006/`へ架空ログ9件を新規作成。`normal-a.txt`と`normal-b.txt`は別の架空環境・サーバーで先頭日付が2026-09-25、`date-mismatch.txt`は先頭日付2026-09-24を画面指定2026-09-25と比較する候補、`missing-date.txt`は冒頭日付行を欠く候補。`invalid-date.txt`は存在しない暦日、`missing-identifier.txt`は識別情報の欠落、`abnormal.txt`と`judgment-hold.txt`は担当者の異常・判断保留の入力を試す候補。長文は次項。9件とも厳密なUTF-8として読め、BOMなしと先頭行をローカルで確認した。具体的な本文形式は要件上まだ未決定のため**検査用の下書き**であり、識別情報の読取式と実機処理は未設定・未実施。クラウドへの配置は後記の承認済み作業で行った。既存A・B・Cの検証ファイルは変更していない。結果不明・差し戻しの再現方法と片付け方は今後整える。
- 長文のローカル下書き：`samples/t006/long-unicode.txt`を架空文だけで作成。冒頭の処理日行は2026-09-25、本文は改行と絵文字を含み43,258 UTF-16コード単位で、Excelの1セル32,767文字を超える。UTF-8として厳密に読め、BOMなし。SHA-256は`1D5FF6864C9396A63DD51F2704721447772F70E39C7DC46FE7A1942D95352C78`。T-004の30,000単位以下で複数行へ分割・復元する実装とExcel実内容の照合は未実施。本文形式は検査用の下書きで、クラウドにも同じ架空ファイルを配置したが処理には使っていない。
- 完成版ひな形のローカル候補：`outputs/t006-20260925/build-template.py`から`evidence-template.xlsx`を作成。1シート「証跡」、表`Evidence`はA4:I5、件ID・元ファイル名・4項目・`ChunkIndex`・`ChunkCount`・`LogTextPart`の9列。5行目の`CaseId=__TEMPLATE__`は最初の分割行で更新し、2行目以降を追加する想定。T-002で空表のArtifact Tool出力がExcel Onlineに修復され、仮行付きのopenpyxl出力が動いた記録を踏まえ、今回も同じライブラリで作成した。openpyxlでの再読込とArtifact Toolでの表・画面表示を確認した。完成版のフローによる仮行更新・追加、長文転記、Excel Onlineで表が保たれることは**未実施・未確認**。クラウドへは後記の承認済み作業で配置した。
- **2026-09-25の承認済みクラウド準備：** ユーザーはDeveloper環境に完成版専用の件表・転記先対応表を各1つ、業務用OneDrive「ドキュメント」に完成版検証フォルダー1つと架空ログ9件・Excelひな形1冊を作る範囲を承認。SharePointプラグインで`架空ログ証跡アプリ完成版検証-T006-20260925`（フォルダーID`01VTXCECE5BP476QXS7RFIAAAMC5QNGY6X`）を作り、`samples/t006/`の9ファイルと`outputs/t006-20260925/evidence-template.xlsx`を上書きなしで配置した。再一覧で10ファイルを確認。ひな形のアイテムIDは`01VTXCECFCHLCOP5KJYFG3SOSB36BFWFWD`。同じファイルをSharePointプラグインで取得し、9列の見出しと仮行を文字抽出で確認。これは**配置と読取の確認**で、Excel Onlineの開封・修復有無、フローでの更新・追加、長文の全文転記は未実施。
- **同日のDataverse準備：** 認証済みPAC CLI 2.12.2には表作成操作を確認できなかったため、Microsoft公式のプレビュー版Dataverse CLI `@microsoft/dataverse`を利用した。Developer環境の既存認証でAPI接続を確認し、件表`cr6cb_evidencecase`（メタデータID`dacf55c4-9cb8-f111-b377-7ced8d3141aa`、メモ添付あり）と対応表`cr6cb_evidencedestination`（メモ添付なし）を新規作成、メタデータを読み戻した。件表へ環境・サーバー各100文字、1以上の整数の実行回、`DateOnly`動作の対象処理日を追加。対応表へ環境・サーバー各100文字、ドライブID255文字、フォルダーID100文字、有効フラグを追加した。作成用JSONは`outputs/t006-20260925/`にある。最初の列追加は言語コード1033が組織に合わず失敗し、既存表のラベルで1041を確認して修正後に追加成功。件表の4項目、対応表の環境・サーバーに代替キーを作り、14:04 JSTに両方`Active`を確認した。対応表への同一組合せの2件目のPOSTはキー違反で拒否され、その時点の8件を維持。その後に独立レビューの指摘対応としてHを1件追加し、現在は9件。件表の重複拒否、キー構成列の空値拒否、条件付き更新、完成版の状態列と画面・フロー接続は未実装・未確認。
- **対応表の実データ：** 架空9組（A～Hと長文）を対応表へ登録し、読み戻しで9件すべて有効、ドライブID`b!sJQ6Cjnu_0-V2PVDfBquhZ3w-BDqyatNkcUB825NHJR8Ir9o1-n1Qa3iOm8G0eik`と上記フォルダーIDの一致を確認した。Hは`missing-identifier.txt`の本文検査用で、画面では登録済みの「架空環境H／架空サーバーH」を選ぶが、ログ本文のサーバー行は欠けたままにする。これにより開始前の転記先未登録で止まらず、本文の識別情報欠落を検査できる計画。実際のExcel出力とアプリからの解決・本文検査は未実施。
- T-006独立レビュー：長文の文字数・ハッシュと秘密値混入なしを確認した一方、当初はHの対応がなく本文のサーバー欠落試験が開始前に止まる矛盾を指摘された。Hの画面選択用対応を上記のとおり追加し、9件の読戻しを確認した。これは検査データの整合修正であり、異常検出の実機合格ではない。
- CLI以外の手段：クラウド準備前にブラウザ操作の可能性を報告したうえでUI接続を試したが、タブ操作はタイムアウトし、UIによる変更は行わなかった。以後の今回の作成はCLIとSharePointプラグインで実行。購入・課金設定変更・公開・削除、既存T-001/T-002検証資産の変更はしていない。費用の将来条件と検証資産の片付け方は未確定。
- **検証資産の保持と片付け案：** T-006フォルダーID、ひな形ID、10ファイルの名前とT-006専用2表・対応9行を対象一覧として維持する。実機試験で増える件行・結果Excel・フローもIDと用途を追記する。試験中と未確認が残る間は保持し、全体の片付け時期は未決定とする。片付け前に参照中のアプリ／フローと必要な証跡を調べ、削除対象を個別に提示して復元可否を確認し、別途承認を受ける。既存T-001～T-003の資産、T-006フォルダー全体、正常な結果Excelの一括削除を既定の操作にしない。この案は手順の記録であり、削除実施や保持期間の決定ではない。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C01～C04・C07・C08 | 保存先、対応登録、架空ログとExcel、失敗例の準備 | 一部準備済み・完成条件は未確認 | Developer環境のT-006専用2表と業務用OneDriveのT-006フォルダー | 表2つ、架空の対応9組、架空ログ9件・ひな形1冊の配置は確認。件表の状態列、画面・フロー、Excel Onlineでの実処理、結果不明・差し戻しの再現、費用・片付け方は未確認。 |

#### 評価・修正履歴

2026-09-25の準備確認：OneDriveの10ファイル、Dataverseの2表、代替キー`Active`、対応表9件の読み戻し、同一組合せの2件目の拒否を確認。これは準備段階の部分確認であり、C01～C09の正式評価ではない。正式な不合格回数は未確認。Excel Onlineの開封・フロー転記、件表の重複拒否、失敗・結果不明の回復は未実施。

#### 完了記録

- 変更内容：T-006の承認範囲で2表、代替キー、対応表9行、OneDrive検証フォルダーと架空ファイル10点を準備。T-006全体は未完了。
- 自動検証・ブラウザ・実機確認の結果：CLIとSharePointプラグインで上記の実在・読戻しを確認。ブラウザ操作は接続タイムアウトで変更なし。Excel Onlineの開封と完成版フローの検証は未実施。
- 確認した対象版：Developer環境の2026-09-25のT-006表（対応9件の読戻し後）と業務用OneDriveのT-006フォルダー。アプリ・フローの完成版は未設定。
- ユーザーの受入結果：未確認。
- 残る課題と、その引き継ぎ先：準備結果をT-007～T-012へ渡す。

### T-007：実装・検証の詳細計画を作り、承認を得る

- 2026-09-25の方針更新：ユーザーはポートフォリオ完成を優先し、アプリ外の全書込経路の空値防御を目的とする堅牢化をやめるよう指示した。`docs/IMPLEMENTATION_PLAN.md`冒頭に今回の対象・順序・検証・外部操作・戻し方を追記した。同期プラグイン候補と先の空値API試験は履歴に残し、完成版の実装ゲートにしない。Canvas 4画面とフロー担当を GPT-6 Luna／max へ分担して実装開始。クラウドの新規操作と実機合格は未実施で、各操作はWORKFLOWの範囲確認後に行う。

- 目的と今回の範囲：設計済みの方式について、作る順番、対象ファイル・画面、利用者から見た動き、費用・外部通信、失敗時の戻し方、C01～C10の検証手順・対象端末・証拠を示す。件ごとの保存と処理開始の分離、同じ件IDでの確定失敗再保存、結果不明中の再保存禁止、C10の再起動復元も対象にする。docs/CHECKS.mdの実行方法も用意する。
- 今回変更しないこと：計画承認を公開承認と混同しない。検証前の推測を確定方式にしない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQUIREMENTS.md全体／C01～C10。承認済み画面設計は版 UI-20260924-01と2026-09-25の追加承認。受付・対象確認の参考画像B-20260924は画面資料を参照。実装計画とdocs/CHECKS.mdは草案作成済み。承認済み実装計画、基準画像、実行可能な確定済み検証手順は未作成。
- 担当と先に必要な作業：統括・計画担当、独立レビュー担当、計画承認はユーザー。T-004～T-006。実担当は着手時に割り当てる。
- 未決定・ユーザーの判断待ち：再起動復元を含む技術方式・実現性、API経由の空値検証方式、費用・外部通信、具体的な計画の承認。承認済み画面仕様の反映は完了。最大8件の作成要求・読取・条件付き更新CLI試験は範囲承認済みだったが、環境列欠落要求が201で受理されたため停止し、残り5件のPOSTと条件付き更新は未送信。件表は2行残存し、必須値拒否は失敗。現行未保護表へ欠落値POSTを追加せず、新しい保護方式は未採用。ローカル同期プラグイン候補は別途明示承認を受けてTDD・独立レビュー修正まで完了したが、Phase Aの実環境権限・費用・戻し方と方式判断、専用クラウド表作成・登録・新方式API試験は未完了で、それぞれ必要な別途承認後に行う。T-007試験の再開、フロー・Excel操作とC10の追加実証も実施前に範囲を確認する。
- 確認する操作・期待結果・対象端末：C01～C10の各操作に、作る部分、確認方法、対象端末、証拠、戻し方が対応し、ユーザーが範囲を判断できる。C10では保存済み件・進捗の復元、未保存入力の破棄、進行中件がある場合のSCR-02表示、それ以外はSCR-01表示を確認する。
- 関係する画面・データ・課金・資料：全画面・全データ、画面仕様、REQUIREMENTS.md、CHECKS.md、費用記録。
- 正式な不合格回数：未確認（停止上限は初回を含め3回）。
- 2026-09-25（実API試験前の進捗）：要件と画面仕様から、`docs/IMPLEMENTATION_PLAN.md`の計画草案、`docs/REQUIREMENTS.md`の独立した「技術上の規律」TR-01～TR-06草案、ひな型を反映した`docs/CHECKS.md`の確認方法草案を作成。AGENTS.mdの参照案内とWORKFLOW.mdの引き継ぎ・記録先を照合し、仕様と規律を実装・レビュー担当へセットで渡す参照に更新した。公開時の参照資料`docs/RELEASE.md`は未作成と明記し、README.mdの進捗表示も現状へ合わせた。独立レビューで指摘された承認状態の表記差、複数件処理の表現、段階的な起動確認、C02・C05・C06の確認条件を修正。コード、アプリ、フロー、外部保存先、テスト実行には着手していない。T-001～T-006の未検証事項が残るため、T-007の完成・正式評価・ユーザー承認は未実施。
- 2026-09-25のT-007方式調査：Microsoft公式の[クラウドフローをコードで扱う資料](https://learn.microsoft.com/en-us/power-automate/manage-flows-with-code)では、ソリューション内のフローをDataverse `workflows`表のWeb APIから作成できる。実際の`@microsoft/dataverse` CLIは認証済み環境へのWeb API GET・POSTに成功しており、`api request --target dataverse`を使える。一方、既存のT-002検証フローIDを`workflows`から読むと`Entity Does Not Exist`で、このフローの定義をそのままCLIで取得・複製する手段にはならない。完成版フローを新規にコード作成する案は**候補**で、実際の接続参照・作成・起動・費用・戻し方は未検証。ブラウザ操作なしでフローを作れると断定しない。
- 同日の接続確認：`pac connection list --environment c1719a14-6cac-eb5b-b598-2254af1c64b7`で既存のDataverse、OneDrive for Business、Excel Online (Business)接続が各1件、すべて`Connected`と表示された。これは既存接続の存在確認であり、完成版のソリューション内フローがその接続参照を使えること、追加費用なしで継続利用できること、Excelの長文転記が成功することの証拠ではない。
- 同日の接続参照読取：Dataverse `connectionreferences`には対応する3種類の参照があり、Dataverse・Excel Online (Business)各1件、OneDrive for Business 2件のうち1件が上記CLIの接続済みIDと一致した。接続参照の実在とID対応は確認できたが、完成版フローの定義へ結合し、保存・起動できるかは未実施。異なるIDのOneDrive参照は採用候補にしない。
- 同日の表メタデータ読取：件表・対応表の`IsOptimisticConcurrencyEnabled=True`をDataverse CLIで確認。Web APIの`If-Match`条件付き更新の前提はあるが、Canvasと完成版フローが古い版を拒否する実装・競合試験は未実施。T-007では実装経路と費用、T-008・T-011では古い版の拒否を確認する。
- 同日の計画具体化：Dataverse CLIの`api request --help`で任意ヘッダー指定を確認。公式資料に沿う停止状態のソリューション内フロー作成と、架空行の版条件付き更新を小さい実証候補として`docs/IMPLEMENTATION_PLAN.md`に順序・通過条件・失敗時の扱いを追記した。いずれも本環境での実行は**未実施**。先に取得したソリューション一覧には完成版専用のものはなく、既定の非管理ソリューション等がある。再読取しようとした`pac solution list`はこの回は結果が返らず中断したため、最新の再確認済みとは扱わない。新しいフロー・試験行・ソリューションは作っていない。
- 同日のT-007追加独立レビュー（実API試験前）：実API試験に伴うGETと既存資産の通常読取りの境界、ならびに「新規行1件」と重複・空値・近接登録試験の要求件数が一致しない2点を指摘された。計画とCHECKSへ、試験GETも事前確認対象、最大8回の架空行作成要求、期待2行・失敗時最大8行が残り得ること、行IDを記録してフローを起動せず止めることを反映した。これは文書の修正であり実API試験の実施ではない。
- 同日のローカル試験準備（実API試験前の記録）：`outputs/t007-20260925/build-case-test-payloads.py`で架空の行作成要求JSONを8点生成。正常と重複、4項目を各1つ欠いた要求、近接要求2点の構成をJSONの静的検査で確認した。クラウドへの送信、行の作成・更新、競合の実証はその時点では未実施。後続のユーザー回答で最大8件の作成要求・読取・条件付き更新CLI試験の実行範囲は承認された。
- 次の行動：API欠落値試験とローカル候補TDDは実施済み。まずPhase Aで未確認の実環境登録権限・ツール利用可否・費用・無効化と戻し方を読み取り専用で確認し、その結果をユーザーへ示して専用表作成・登録・API試験の方式判断を受ける。残り5件のPOSTと条件付き更新は停止条件により未実施で、現行の未保護表へ欠落値POSTを追加しない。新方式の実API試験、プラグイン登録・展開、新しい専用クラウド検証表の作成は別途承認後に行う。T-006のExcel Online適合とフロー作成経路、C10の永続化・再起動復元方式は未確認。件表以外の実API更新、フロー作成、結果Excel作成は、`docs/WORKFLOW.md`に沿った対象・費用・影響の実行範囲確認後にのみ行う。未確認のまま実装方式を採用済みとせず、費用・戻し方とTR-01～TR-06を含む詳細計画の正式評価・ユーザー承認へ進む。承認後は同じ正式資料へ反映し、対象の仕様と規律を実装担当・レビュー担当へセットで渡す。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C01～C10 | 実装範囲と検証・証拠の計画 | 未確認 | 計画草案 | 計画・確認方法・規律の草案は作成。C10の再起動復元条件と件ごとの保存・開始分離は関連資料へ反映済み。ローカル同期プラグイン候補は別途承認範囲でTDD最終16/0、独立レビューHigh 2件を修正・再確認。実環境の登録権限・費用・戻し方、専用表作成・登録・新方式API試験、T-007全体の正式評価・計画承認は未完了。実API試験では3要求を実施し、正常1件・重複拒否1件・環境列欠落の受理1件を確認後停止。件表は2行残存し、必須値拒否は失敗。残り5件のPOSTと条件付き更新は未送信。新しい保護方式は未採用。 |

#### 評価・修正履歴

2026-09-25（実API試験前）：計画・規律・確認方法の草案について独立した資料レビューを実施。承認状態の表記差、複数件処理の表現、保存結果不明の未決定事項、段階的な起動確認、C02・C05・C06の確認文を修正した。再確認では指摘が解消し、新たな高影響の不整合は見つからなかった。**資料の静的照合のみ**で、T-007の正式な条件評価、アプリ・テストの実行、ユーザー承認ではない。正式な不合格回数には加算しない。今後の正式評価では対象版・条件別結果・証拠・残る問題を記録する。

2026-09-25の追加承認を受け、T-007にC10の再起動復元、件ごとの保存と処理開始の分離、保存失敗確定件の同じ件IDでの再保存、結果不明中の再保存禁止を追記対象とした。**実API試験前の時点では**、最大8件の作成要求・読取・条件付き更新CLI試験は実行範囲の承認を得たが未実施だった。フロー・Excelの追加操作、C10の新たな実API試験は実行範囲未確認。詳細計画の正式評価と承認も未了のため、T-007は依存作業待ちと記録していた。

2026-09-25（実API試験前）：独立資料レビューでT-007の現況欄に残っていたC10・件ごとの保存の「反映中」を「反映済み」へ修正し、CLI試験の実行範囲承認と未実施、別途確認が要るフロー・Excel・C10追加実証を明記した。これはTASKS/CHECKSの文書照合・修正のみであり、T-007の正式評価・計画承認・実API試験結果ではない。不合格回数には加算しない。

2026-09-25：T-007実API試験で、事前GETはHTTP 200・0行、正常作成はHTTP 201でID `fed4e447-b1b8-f111-b377-7ced8d3141aa`、同じ4項目の重複作成はHTTP 412で拒否、環境列を省いた作成は予想外にHTTP 201でID `3086ba97-b1b8-f111-b377-7ced8d3141aa`を記録。最終GETはHTTP 200・2行で、環境列欠落の行の環境値は空。空値拒否の個別実証は失敗とし、残り5件のPOSTと条件付き更新は送らず停止。**現行の未保護表へ残りの欠落値POSTを送らない。新方式の実API試験とプラグイン等の展開は別途承認が必要。**削除・フロー・Excel操作なし。詳細は`outputs/t007-20260925/api-test-run-20260925.md`。これは個別試験の実行結果で、WORKFLOWの正式評価（独立レビュー担当による条件別総合判定）ではなく、正式な不合格回数には加算しない。

2026-09-25：ユーザーはT-007 Phase Bの同期プラグイン候補コードとローカルTDDを明示承認した。承認範囲はローカル候補に限り、専用クラウド表の作成、Dataverse登録・展開、新方式API試験は未承認。`dataverse-plugins/`に実SDKを参照する`IPlugin`候補を作成。`.\dataverse-plugins\run-local-tests.ps1`でnet48・fake execution context・NuGetソースなしのオフラインTDDを実行した。初回redは5 pass／6 fail、greenは11／0。証拠は`dataverse-plugins/evidence/tdd-red.txt`と`tdd-green.txt`。

同日の候補コード独立レビューでHigh指摘2件（実行回0／-1が通る、既存件表名が固定されている）を受けた。赤テストで再現し、入力検査と対象表名の固定を修正。追加redの後、最終は16 pass／0 fail、ビルド0警告／0エラー。独立再レビューでHigh 2件の解消を確認した。詳細証跡は`dataverse-plugins/evidence/tdd-review-fix-table-red.txt`、`tdd-review-fix-red.txt`、`tdd-review-fix-green.txt`。この独立コードレビュー結果とTDDの意図的なredはローカル候補の範囲で、T-007全体の正式評価・合格や正式不合格回数とは別。

#### 完了記録

- 変更内容：計画草案・確認方法と同期プラグインのローカル候補コードを作成。ローカル候補・TDDのみユーザーが明示承認。2026-09-25に承認済み範囲のDataverse API試験を開始し、必須項目拒否条件の失敗で停止。詳細は進捗・評価履歴、`outputs/t007-20260925/api-test-run-20260925.md`と`dataverse-plugins/README.md`。
- 自動検証・ブラウザ・実機確認の結果：Dataverse APIの事前GET、正常・重複・環境列欠落の3作成要求と各後続読戻しを確認し、件表には現在2行が残存。環境列欠落がHTTP 201で保存されたため必須値拒否の実証は失敗。残り5件のPOSTと条件付き更新は未送信で、現行の未保護表へ追加の欠落値POSTを送らない。別途承認されたローカル候補TDDは最終16 pass／0 fail、独立レビューHigh 2件の修正確認済み、ビルド0警告／0エラー。net462ビルド、Dataverse登録・実動作、権限、費用、戻し方は未確認。新しい保護方式は未採用。ブラウザ・フロー・Excel操作は対象外。
- 確認した対象版：Developer環境`c1719a14-6cac-eb5b-b598-2254af1c64b7`の件表`cr6cb_evidencecase`、2026-09-25。アプリ・フローとの接続は未設定。
- ユーザーの受入結果：未確認。
- 残る課題と、その引き継ぎ先：空の環境列を受理した原因と残存する試験用2行の扱い、同期プラグインの実環境登録権限・費用・戻し方を調査し、ユーザーへクラウド方式判断を示す。現行の未保護表へ欠落値POSTを追加せず、残りのPOST・条件付き更新の再開、新方式の実API試験、サーバー側保護の登録・展開、新しい専用クラウド検証表の作成には別途承認を得る。ローカル候補の成功をTR-03適合や採用済みと扱わない。T-007の正式評価・計画承認後に次工程へ渡す。フロー・Excel操作とC10追加実証は個別の実行範囲確認後。

### T-008M：4画面の先行モックを操作できる形にする

- 目的と承認範囲：2026-09-25のユーザー依頼により、SCR-01～SCR-04をすべて先に架空データのモックとして作り、画面・操作・遷移を本人が確認してから保存・Excel連携へ進む。実サービスの処理結果をモックの表示で代用しない。
- 対象：`outputs/ui-mock/index.html`のローカル操作モックと`docs/ui/screens/`の承認済み設計。既存のT-001フォームは`log-evidence-app/`へ保持し、同期した`log-evidence-mock/`のCanvas YAMLも変更しない。T-001～T-003のDataverse行・Excel・検証フローは変更しない。画面はPCブラウザでローカルファイルを開いて確認する。
- 画面で試せる範囲：架空件の選択・修正・取消と開始、進捗一覧への移動、件の詳細と人の確認、レビュー依頼・実演役割切替・確認済み／差し戻し、一覧への戻り。正常・異常・結果不明の表示を切り替える。画面内の架空状態はセッション中のみであり、実際のログ選択・永続保存・Excel作成・処理・レビュー記録ではない。
- 確認方法：`docs/CHECKS.md`のT-008Mに従い、CLIのJavaScript構文確認、4画面の文言・表示・主要操作・正常／停止／差し戻し経路のPCブラウザ確認を分けて記録する。ユーザーの受入は別に記録する。見た目と一時遷移のモックにはTDDを適用せず、保存・判定・レビューの本処理で適用する。
- 外部操作と戻し方：ローカルHTMLは外部送信しない。Power Appsへ後から反映する前には対象アプリ、送る内容、既存T-001フォームへの影響、復元方法を説明して`docs/WORKFLOW.md`の承認を受ける。ブラウザ／コンピュータユースをAIが使う直前には報告する。ローカルの旧YAMLは保持し、既存のクラウド資産を削除しない。
- 現在の結果：Canvas Authoring MCPで既存の非公開アプリへ接続し、`log-evidence-mock/`に3 YAMLファイルを同期した。クラウドの画面は変更していない。`outputs/ui-mock/index.html`に4画面のローカル操作モックを作成。上部のモック専用欄に「使い方を表示」と「進捗一覧を見る」を追加した。Node.jsの構文確認は成功し、CLI起動のChromium 145.0.7632.6で4画面の表示・主要遷移、取消件を同じ件IDでログ差し替え、差し戻し→再依頼→確認済み、安全な停止件の再実行、結果不明件の再実行不可、受付に戻っても処理済み件を再処理せず進捗へ戻れること、狭い画面の横はみ出しなしを確認した。5枚の表示画像は同フォルダーの`review-*.png`。この記録時点ではユーザーの操作・受入は未確認だった。後記の2026-09-25回答で画面の方向と仮遷移は受入済みだが、本人が全操作を完了したか、完成版を受け入れたかは未確認。画面操作ツールからChromeを開く試みはタイムアウトしたが、Windowsの既定ブラウザでローカルHTMLを起動するコマンドは成功した。実際にユーザーの画面に表示されたかは未確認。
- 次の行動：受け入れられた画面の方向をPower Apps実装へ引き継ぐ。保存・Excel・レビューを実サービスへ結ぶ前にT-007の技術確認を行う。モック内の「保存済み」「反映確認済み」は架空の例であり、実サービスの成功記録ではない。
- ユーザー受入：2026-09-25、本チャットで「OK、モック確認しました。この感じで作ってもらって大丈夫だと思います」と回答。画面の方向と仮遷移を受け入れたが、本人が全操作を完了したかは未確認。完成版の受入や公開承認には当たらない。次はT-007の必要な技術確認を終えてからT-008のPower Apps実装を進める。

### T-008：ログ受付・件ごとの保存・再起動復元・開始前の修正と取消を実装する

- 2026-09-25の実装開始：ユーザーはDeveloper環境の既存非公開アプリ4画面更新、件表への最小9列追加、フロー1件、架空ログ2件とT-006結果Excel最大2冊の試験を承認。Canvas Authoring MCPで既存アプリを`log-evidence-app-complete/`へ同期し、T-001資産とは別のローカル先を用意。既存2表「架空ログ証跡件」「架空ログ転記先」はStudioで接続し、MCPの一覧・列定義で確認した。`Screen1.pa.yaml`の受付フォームを完成版候補件表の既存4項目・Notes添付へ結び直し、MCPコンパイルがPASSED。これはローカルYAMLの静的検査で、保存・復元・実画面受入は未実施。
- 同日の件表準備：`@microsoft/dataverse` CLI 1.0.80で`cr6cb_evidencecase`へ処理状態、Excel照合状態、停止理由、証跡Excel URL、転記照合、処理結果判断、担当者コメント、レビュー状態、差し戻し理由の9列（Text 6、Memo 3）を追加。事前GETで対象9列がないこと、追加後GETで全9列の論理名・型・長さ・表示名を確認。既存T-007試験2行は変更・削除していない。追加列がCanvasで表示されるか、実保存で使えるかは未確認。フロー・結果Excelは未作成・未実行。

- 目的と今回の範囲：完成品で架空ログを複数選び、件ごとの情報を確認し、SCR-01の各件にある「保存」から個別に保存する。「処理開始」は保存と別操作とする。再起動後は保存済み件と進捗を復元し、未保存入力は破棄する。進行中の件があればSCR-02を開く。判明した保存失敗件は「保存できませんでした」と示し、同じ件IDで再保存する。結果不明中は開始・再保存しない。
- 今回変更しないこと：進行中取消、実務ログの利用、未承認の下書き機能を追加しない。保存ボタンから処理開始やフロー起動へ進めない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQ-01、U01・U02／C08～C10、C01の受付部分。[受付・対象確認の画面仕様](ui/screens/受付・対象確認.md)の承認済み設計版と2026-09-25追加承認を参照。参考画像は同資料からB-20260924を参照。承認済み実装計画・基準画像・対応テストは未作成。
- 担当と先に必要な作業：実装担当、テスト担当、独立レビュー担当、統括。T-005の承認済み仕様反映は完了。T-007の計画承認・技術方式確認が先行する。実担当は着手時に割り当てる。
- 未決定・ユーザーの判断待ち：ログ保存・永続化・照会の実現方式と費用、具体的な成功・入力不正の案内、保存失敗・結果不明後に画面で案内する細部、未保存の仮置きファイル処理はT-007で確認する。件別「保存」と「処理開始」の分離、再起動後の保存済み状態復元・未保存入力破棄、進行中件がある場合のSCR-02表示、確定保存失敗件の同じ件IDでの再保存、結果不明中の再保存禁止は承認済み。
- 確認する操作・期待結果・対象端末：複数件を選び、対象件の「保存」を操作して1件ずつ同じ件IDで保存する。「処理開始」を押す前にフロー・Excelが始まらないことを確認する。保存失敗確定後はその件だけ同じIDで再保存でき、結果不明中は再保存不可。再起動で保存済み件・進捗が復元され、未保存入力が破棄され、進行中件があればSCR-02、なければSCR-01が開くことも確認する。取消件は処理されない。端末はT-007で定め、実画面・保存先・状態を記録する。
- 関係する画面・データ・課金・資料：受付・対象確認、架空ログ、保存先、画面仕様、CHECKS.md。
- 正式な不合格回数：未確認（停止上限は初回を含め3回）。
- 現在の追記（2026-09-26）：完成版の非公開4画面アプリでは、架空件の受付・保存・処理開始と、保存済み件を再起動後に表示する一部操作を実機確認した。下の「未着手」「未確認」は当初の計画時点の記録であり、最新判定は冒頭「現在位置」とT-011の条件別結果を参照する。
- 次の行動：保存確定失敗からの同件再保存、返答不明中の二重保存防止、開始前の取消・修正、C10の進行中件がない場合を条件別に確認する。失敗時に件IDと保存先の実体を読戻し、未確認を合格へ変えない。

#### 実画面確認と提示の条件（工程8）

| 対象 | 実際に表示して確認すること | 記録と提示条件 |
| --- | --- | --- |
| SCR-01の各状態 | 未選択・件別確認中・保存失敗判明・保存結果不明・開始後を表示し、承認済み仕様と照合する。各件の「保存」と「処理開始」が別操作であることを確認する。仕様外の説明文・見出し・ボタンがないこと、文字や入力欄の欠け・重なりがないことを確認する。 | 対象版、端末・ブラウザ、表示条件、画面の証拠、差異と修正結果を記録する。不一致を修正して再確認してからユーザーへ提示する。確認不能なら未確認の理由を報告し、検証済みの実画面として提示しない。 |
| 実操作 | 複数件の選択、件別4項目の入力、「保存」による件単位の保存、保存成功・確定失敗・結果不明、開始前修正・取消、「処理開始」との分離、開始後の進行中取消がないことを操作して確認する。確定失敗は同じ件IDで再保存でき、結果不明中は再保存不可。 | 操作結果、件IDと添付の読戻し、保存操作だけではフロー・Excel・開始受付が起きないこと、明示的な処理開始後のSCR-02遷移を記録する。設計承認を操作・保存の合格に流用しない。 |
| 再起動・復元 | 保存済みの受付件と進捗を用意し、別の未保存入力も作ってアプリを閉じて再度開く。保存済み件・進捗が戻り、未保存入力が破棄されることを確認する。進行中の件が1件以上ならSCR-02、それ以外はSCR-01が最初に開くことを確認する。 | C10として保存先の読戻し、件ID、進捗状態、再起動前後の表示画面を記録する。モックや画面内一時状態だけを合格根拠にしない。 |

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C08～C10、C01の受付部分 | 件ごとの保存、確定保存失敗後の同ID再保存、結果不明時の保留、再起動復元、開始前修正と取消、複数件の受付 | 部分確認。全体は未判定 | 2026-09-26の非公開アプリ | 正常D・Gの各件保存と同時開始、C10の保存済み件復元・未保存入力破棄を確認。保存失敗・返答不明・同ID再保存・開始前の全操作・C10の別条件は未確認。詳細はT-011。 |

#### 評価・修正履歴

未実施。評価した日時、対象版、条件別結果、証拠、指摘と修正、不合格回数への加算理由、残る問題を実施時に追記する。

#### 完了記録

- 変更内容：未実施。
- 自動検証・ブラウザ・実機確認の結果：未実施。
- 確認した対象版：未設定。
- ユーザーの受入結果：未確認。
- 残る課題と、その引き継ぎ先：実装結果をT-009・T-011へ渡す。

### T-009：日付照合・Excel転記・進捗・再実行を実装する

- 目的と今回の範囲：ログ本文の日付と指定日を照合し、正常件ごとに新Excelを作って全文と識別情報を転記する。書き込みと実際の反映、失敗理由、結果不明を区別し、重複・過去証跡の消失を防いで失敗件だけ再実行する。
- 今回変更しないこと：Excel関数やバッチ処理の自動成否判定を追加しない。結果不明を反映済みと表示しない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQ-02～REQ-04、R03～R06・R12・R13／C01～C04。[進捗一覧](ui/screens/進捗一覧.md)と[件ごとの確認](ui/screens/件ごとの確認.md)の承認済み設計は版 UI-20260924-01。両画面の参考画像・基準画像・対応テストと承認済み実装計画は未作成。
- 担当と先に必要な作業：実装担当、テスト担当、独立レビュー担当、統括。T-008。実担当は着手時に割り当てる。
- 未決定・ユーザーの判断待ち：反映の照合と結果不明からの復旧方法はT-004・T-007で確定する。満たせない場合は報告する。
- 確認する操作・期待結果・対象端末：C01～C04の正常・異常・結果不明・再実行を条件別に確認する。端末は承認済み計画による。ユーザー提示前に実際のSCR-02・SCR-03を表示し、承認済み文言との一致、仕様外の表示、欠け・重なり、件別状態と画面間の操作・遷移を確認する。
- 関係する画面・データ・課金・資料：進捗一覧・件別確認、架空ログ・Excel・状態、画面仕様、CHECKS.md。
- 正式な不合格回数：未確認（停止上限は初回を含め3回）。
- 現在の追記（2026-09-26）：下の「未確認」表と初期の評価履歴は作成当時の記録。後続でCanvasのフロー結線と正常C・F・A・D・Gの実行、Excel実内容の照合、日付不一致Cの停止を実施した。最新の条件別判定はT-011を参照する。
- 次の行動：C03・C04のExcel失敗、結果不明、重複防止、安全な再実行を先に設計・実装・検証する。長文分割と残る入力異常も確認する。旧Bは結果不明のまま再実行しない。

#### 実画面確認と提示の条件（工程9）

| 対象 | 実際に表示して確認すること | 記録と提示条件 |
| --- | --- | --- |
| SCR-02進捗一覧 | 正常・停止・失敗・結果不明の件を表示し、保存・転記処理・Excel実反映・担当者確認・レビューの区別、失敗理由、承認済み文言を照合する。仕様外の表示と欠け・重なりを確認する。 | 対象版、端末・ブラウザ、表示条件、画面の証拠、差異と修正結果を記録する。不一致を修正して再確認してからユーザーへ提示する。確認不能なら未確認の理由を報告し、検証済みの実画面として提示しない。 |
| SCR-03件ごとの確認 | 元ログと該当Excelの参照、照合と人の判断の分離、失敗件の再実行、SCR-02への戻りを操作し、文言・項目・状態・遷移と欠け・重なりを確認する。 | 実操作と保存・失敗時の結果を記録する。承認済み設計と実際のExcel反映の確認を別の証拠として扱う。 |

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C01～C04 | 正常転記、異常件停止、結果不明、重複防止・再実行 | C01合格、C02一部合格、C03・C04未確認 | 2026-09-26の非公開アプリと既存フロー | D・Gの正常2件はExcel実内容まで一致。日付不一致Cのみ停止し正常Aは継続。結果不明・長文分割・同時起動防止・安全な再実行は未確認または未実装。詳細はT-011。 |

#### 評価・修正履歴

2026-09-25：GPT-6 Luna／max の実装担当が`outputs/flow-implementation/`にPower Apps (V2)→Dataverse→OneDrive→Excelの件別WDL定義を作成。単体検査12件とローカル自己試験は合格。統括が既存T-002保存済み定義と接続参照をCLIで照合し、同名0件を確認後、Dataverse Web APIへPOST。作成ID `ef881fbc-dcb8-f111-b377-7ced8d3141aa`、Power Automateのresource ID `ccbc5642-9cce-b1e9-07f5-8174f938bab4`。POST後の読戻しは定義文字列の完全一致・停止状態、PATCH後の読戻しは状態`statecode=1`／`statuscode=2`。`pac power-automate list-flow-actions`はトリガー1行と操作種別`CopyDriveFileByPath`、`GetItem`、`GetItems`、`ListRecords`、`PatchItem`、`UpdateOnlyRecord`を返した。架空件Aと添付1件をCLIで作成し、親ID・ファイル名・添付ありを読戻した。フロー起動は未実施。Chromeのブラウザ操作は、Chrome接続が一覧で確認できたがauto-reviewに2回拒否されたため未実施。Canvas Authoring MCPの安全な空フォルダー同期は`Nothing written`で、アプリYAML変更・コンパイルも未実施。これはT-009の完成条件の正式評価ではなく、不合格回数へ加算しない。

同日のGPT-6 Luna／max独立静的レビュー：既存T-002と照合したコネクター操作名・主要パラメーターに明白な誤りは見つからなかった。ただし、開始状態の読取と無条件更新の間に同時起動が入る競合、30,000文字超の分割未実装、BOM付きログの先頭行判定、成功時の`excelurl`未記録を指摘。特に同時起動時は成功と失敗の状態更新が競合し得る。短い架空件Aのクラウド実行も未確認であり、C01～C04やTR-01・TR-04の合格とは判定しない。修正と検査はアプリ結線・クラウド実行の結果とともにT-009で扱う。

ユーザーがChromeで新規フローの編集画面を開き、画像を共有。画面にはPower Apps (V2)、Compose CaseId、Get case、Condition Start Readyの先頭部分と、Dataverse・OneDrive for Business・Excel Online (Business)の3接続が「接続されています」と表示された。条件内のアクション、保存操作、起動、Excel実内容は画像から確認できない。Canvas Authoring MCPへ再接続は成功したが`list_data_sources`は0件で、編集セッションの状態確認には使えなかった。`pac canvas download --extract-to-directory outputs/canvas-current-20260925`は接続後にCLI内部の`System.InvalidOperationException`で終了し、保存済みアプリの読戻しに失敗。ユーザー指定のChrome操作は行っていない。次はPower Apps Studioの有効な編集セッションからMCPの状態を読み直し、フロー結線と架空件Aの実行経路を確認する。

その後、ユーザーが正確なPower Apps Studio編集URLを共有。Canvas Authoring MCPへ同じアプリ・環境で再接続し、データソース3件（T001検証ログ受付、架空ログ証跡件、架空ログ転記先）を確認した。`sync_canvas`で`outputs/canvas-live-current-20260925/`へ4 YAMLファイル・2画面を読戻し、保存済みの4画面版とは別の状態と判明。GPT-6 Luna／max担当が同期済みApp・Screen1・`_EditorState`を保持したまま、Screen2の承認済み2行差分を反映し、Screen3・Screen4を承認済み版とSHA-256一致で配置した。現在ローカルは6 YAMLファイル。`compile_canvas`はYAMLをPower Apps編集サービスへ送る対象・内容の明示が足りないとしてauto-reviewが実行前に拒否。迂回・再試行はせず、構文検査、編集セッションへの4画面反映、Studio保存、フロー結線はいずれも未実施。送信先と画面定義の範囲を示してユーザー確認中。

ユーザーが6 YAMLファイルのPower Apps編集サービスへの送信・構文検査・編集セッションへの反映を明示的に許可。GPT-6 Luna／max担当が同じ`compile_canvas`を再実行し、`Validation PASSED`（6ファイル）。新規`outputs/canvas-live-readback-20260925-02/`への`sync_canvas`は6ファイル・4画面を返し、App・Screen1・Screen2はローカルとバイト一致。Screen3の添付Items.Name／Value追加・Excelリンク高さ指定削除とScreen4の差し戻し理由Default空指定削除など、サーバー正規化差分を確認した。`_EditorState`の読戻しではScreen3・Screen4が順序に追加され、ローカル同ファイルは編集していない。Studio保存・アプリ実行・フロー結線は未実施で、ユーザーにStudioの保存操作を案内中。

続いてユーザー本人からPower Apps Studioで「保存できた」と回答があった。保存操作の実施はユーザー報告として記録し、CLIダウンロードによる開き直し・実画面の動作確認は未実施。次のStudio操作として既存フロー`架空ログ証跡_件別Excel転記`をPower Automate欄からアプリへ追加するよう案内した。追加後のMCP認識・処理開始ボタン結線・クラウド実行は未確認。

ユーザー共有のPower Apps Studio画像では、Power Automate欄の「アプリ内」に`架空ログ証跡_件別Excel転記`が表示された。Canvas MCPの`list_apis`も同名1件を返し、`describe_api`は`Run`（Boolean戻り値）と必須の`text: String — caseId`を返した。追加された編集セッションを新規`outputs/canvas-flow-bound-base-20260925/`へ`sync_canvas`し、6 YAMLファイルを読戻した。処理開始ボタンはまだ無効表示で、フロー呼出式とクラウド実行は未実施。GPT-6 Luna／max担当がScreen1とAppの最小結線を実装中。

GPT-6 Luna／max担当がScreen1の処理開始を、保存済み件IDを件ごとに確認し、Dataverseの現状態`保存済み`と有効な転記先を確認して`開始受付済み`へ更新後、`架空ログ証跡_件別Excel転記.Run(Text(caseId))`を1回だけ呼ぶ形へ変更。転記先なしは理由を残してフローを起動せず、開始返答不明は表示して再送を止める。App.StartScreenも`開始受付済み`を進行中と認識する。初回コンパイルはForAll内のSetで11エラー、次回は委任警告4件だった。担当がSetをループ外へ移し、9件の転記先をローカルの照会用コレクションへ取り込んだ後、6ファイルの`Validation PASSED`と新規ディレクトリへの読戻しでScreen1・Appの一致を確認した。独立したGPT-6 Luna／maxレビューでは、短い架空件Bの通常経路を阻む確実な静的欠陥は見つからなかったが、開始結果不明時に選択・取消が動く欠陥を指摘。担当が入力欄・添付・保存・選択・取消を開始中／結果不明中に止める修正をし、再コンパイル`Validation PASSED`、`outputs/canvas-flow-freeze-readback-20260925/`のScreen1と完全一致を確認。MCPのAPI一覧にもフロー1件が残る。Studio保存とクラウド実行は未確認で、ユーザーに今回の編集の保存を案内中。レビューで残った保存失敗の確定判定、開始不明時のDataverse照会、再起動後の未開始件復元、複数セッションの同時開始は後続の未実装・未確認として維持する。

保存状態の追加調査：ユーザー報告ではPower Apps Studioの保存ボタンが押せず、タブ更新時に未保存の可能性を知らせる警告が出る。公式Power Platform APIの読取ではアプリの`lastModifiedTime`は2026-09-25 22:18:19 JST。一方、最終のMCP読戻しファイルは同日22:52:57 JSTに作成されており、少なくとも最終の処理開始・操作制限差分が保存版へ入ったとは判定できない。APIの`documentUri`から得たアプリパッケージは公開版とみられ、`Src/Screen1.pa.yaml`のみを含むため、未公開の最新下書きの証拠には使わない。PAC `canvas list/download`は内部`InvalidOperationException`で失敗。Studioタブは更新せず、編集可能状態・自動保存設定を画面表示で確認する。**最終差分の永続保存・実機実行は未確認**。

その後ユーザーは連携済み内容が保存されていると報告した。公式APIの更新時刻は再照会でも22:18:19 JSTのまま。Canvas MCPを再接続して`outputs/canvas-after-user-save-20260925/`へ読戻すと4画面は存在するが、Screen1の`btnS01StartFlow`は無効・OnSelectなしの旧定義で、`list_apis`は0件、データソースは3件だった。GPT-6 Luna／max担当が別ディレクトリへの読戻しでも同じ結果を確認。承認済み6 YAMLの再`compile_canvas`はフロー名の`Run`が不明というエラーを含む2件で失敗し、再反映はされていない。現在のMCP編集状態でフロー接続を認識できないのが直近の停止点。Studioの「アプリ内」にフローが表示されるかユーザーへ確認中。ユーザー報告の保存表示とMCP読戻しの差異は未解決で、保存版への最終結線・クラウド実行は未確認。

23:16 JSTごろのユーザー画像では、フローは「フローの追加」の候補欄「すぐに」にあり、「アプリ内」にはなかった。候補をクリックした後の23:17ごろの画像では「アプリ内」に`架空ログ証跡_件別Excel転記`が表示された。直後にCanvas MCPの`list_apis`が0件から1件になり、欠けていたフロー接続が編集状態に入ったと確認した。GPT-6 Luna／max担当が承認済み6 YAMLを再`compile_canvas`し`Validation PASSED`。`outputs/canvas-after-reapply-20260925-2319/`へ読戻し、Screen1とAppが意図版にバイト一致、`list_apis`1件を確認した。Studioでの保存・公開は未実施で、ユーザーに保存アイコンの操作結果を確認中。実行とExcelの確認は未実施。

ユーザーがStudio右上の保存を実行して保存済みと報告。公式Power Platform APIの`lastModifiedTime`は2026-09-25 23:19:30 JSTへ進んだ。Canvas MCPを再接続し`outputs/canvas-after-studio-save-20260925/`へ読戻した6 YAMLのうちScreen1とAppは意図版とSHA-256一致、`list_apis`はフロー1件。これを4画面と処理開始結線の**保存確認**とする。公開版は更新しておらず、架空件Bのアプリ操作・フロー起動・Excel出力は未実施。`get_appchecker_errors`は重大エラー0、性能に関する中程度の警告6件（未使用変数、開始時の対応表収集と反復更新など）であり、実機合格の代替にはしない。

架空件Bの試験前確認：`samples/t006/normal-b.txt`は短い架空の正常ログ。SharePoint読み取りではT-006フォルダーに元の9ログとひな形1冊のみで、結果Excelはまだない。Dataverse APIを読み取り、架空環境B／架空サーバーBの既存件は0件。ユーザーのStudioプレビューは既存件AのためSCR-02から始まったが、SCR-02に新しい件へ進む常設操作がないと判明した。ユーザーはSCR-02右上に「新しい件を受付」を常時表示し、SCR-01へ遷移、既存件の状態・データを変えない変更を承認。画面仕様とCHECKSへ記録済み。GPT-6 Luna／max担当が`outputs/canvas-after-studio-save-20260925/`からScreen2のみ変更し`outputs/canvas-new-intake-button-stage-20260925-2324/`の6 YAMLを`compile_canvas`で`Validation PASSED`、`outputs/canvas-new-intake-button-readback-20260925-2324/`で全6ファイルのバイト一致、フローAPI1件を確認した。Studioでの追加変更の保存・実画面確認・Bの実行は未実施で、ユーザーの保存と画面表示の確認待ち。

追加ボタン後の保存状況：ユーザー報告ではStudioの保存アイコンを押せず、プレビューで進捗一覧が開いた。Power Platform APIの`lastModifiedTime`は再照会でも23:19:30 JSTのままで、23:24ごろのMCP追加差分の永続保存は確認できない。プレビュー内の「新しい件を受付」の表示有無を確認中。Canvas Studioの公式ショートカット`Ctrl+S`は確認したが、この状態での実行と結果は未実施。画面は更新せず、保存結果を確かめるまで架空件Bの入力・開始はしない。

Ctrl+S後の保存確認：ユーザーがStudioでCtrl+Sを押して保存したと報告。Power Platform APIの`lastModifiedTime`は2026-09-25 23:44:13 JSTへ進んだ。Canvas Authoring MCPを再接続し`outputs/canvas-after-ctrl-s-20260925/`へ6 YAMLを読戻し、`outputs/canvas-new-intake-button-stage-20260925-2324/`の6ファイルとSHA-256がすべて一致した。フローAPI一覧にも`架空ログ証跡_件別Excel転記`が1件残る。これを「新しい件を受付」ボタンを含む下書き保存の確認とする。Studioプレビューでのボタン表示・遷移、架空件Bの保存・開始・Excel出力は未実施。公開も未実施。

続くプレビュー確認：ユーザーがStudioの▶プレビューでSCR-02「進捗一覧」から「新しい件を受付」を押し、SCR-01「受付・対象確認」へ移れたと報告。遷移の実機確認は完了。既存件の不変性は保存先を照合しておらず未確認。架空件Bの添付・保存・開始・Excel出力は未実施。

架空件Bの受付試行：ユーザーは`normal-b.txt`を選んで「ログを選ぶ」を押しても反応がないと報告。件一覧への追加、Dataverse保存、処理開始は確認していない。GPT-6 Luna／maxによる保存後YAMLの静的確認では、このボタンは添付1件以上かつ入力待ち一覧が空の場合のみ動き、条件不成立の案内はない。また「新しい件を受付」は画面遷移だけで一時的な入力待ち一覧を初期化しない。添付欄にファイル名が表示されたかユーザー回答を待ち、条件のどれが外れたか絞る。未保存項目を失い得るため、原因未確定のまま一覧を消さない。

2026-09-26 00:10 JSTごろのユーザー画像で、最初の無反応時は添付欄に「添付されているものはありません」と出ていたことを確認。ユーザーが「ファイルを添付」で`normal-b.txt`を選び、改めて「ログを選ぶ」を押した後の画像では、左の入力待ち一覧と右の添付欄の両方に`normal-b.txt`が表示され、ボタンは無効表示になった。件の一時選択までは実機で確認。環境・サーバーはまだAの初期値、実行回は空欄、対象処理日は2001-12-31の表示であり、Bの4項目入力と保存は未実施。無反応の直近原因は添付前の操作で、残存キューの不具合とは判定しない。

同日続きのB保存確認：ユーザーが環境`架空環境B`、サーバー`架空サーバーB`、実行回`1`、対象処理日`2026-09-25`を入力し、「保存」を1回押して画面に「保存済み」と表示された。Dataverse CLIでBの件を読み取り、該当件1行、件名`normal-b.txt`、4項目、処理状態`保存済み`、Excel照合`未確認`を確認。Notes添付は`normal-b.txt`1件、166バイトで、親IDはBの件IDと一致し、本文のSHA-256はローカル`samples/t006/normal-b.txt`と一致。フローは同CLIで`statecode=1`／`statuscode=2`の有効状態、クラウド`clientdata`が`outputs/flow-implementation/workflow-create-body.json`と文字列一致することを確認した。CLIの最初の認証要求はサンドボックス内で応答せず中断し、Power Platform API用のPACトークンをDataverseへ使った試行は401だったため判定に使用していない。権限のあるDataverse CLI読取りで得た成功結果のみを証拠とする。処理開始・結果Excelは未実施。

2026-09-26のB開始・読戻し検証：ユーザーが保存済みBで「処理開始」を1回押し、進捗一覧に「処理中」を確認。Dataverse CLIの後続読取ではBの処理状態は`結果不明`、Excel照合は`読出不能`、理由は「Excelの反映内容を全文照合できませんでした」。OneDriveのT-006検証フォルダーにはB件ID名の結果Excelが1冊増え、SharePointプラグインで開いた`Evidence`表は1行、件ID・元ファイル名・4項目・処理日・全文ログが`samples/t006/normal-b.txt`と一致した。ChromeのPower Automate実行詳細では、この1回の実行全体は「成功」だが、`Read back evidence`（Excel Online (Business) GetItems）はHTTP 200で`body.value=[]`、応答ヘッダーに`x-ms-apihub-cached-response: true`があり、`Condition Readback Matches`のFalse経路が`Update case readback unknown`を実行した。**Excel実体とフローの直後の読戻し結果が食い違う**。空の読戻しとキャッシュの関連は疑われるが原因確定ではない。結果不明のBは再実行・上書きしない。C01全体は不合格／未完了として、読戻し方法の修正、別の架空件での再試験、Bの状態整合を残す。`pac power-automate list-flow-runs`は`null`、Power Platform APIのflowRuns一覧はHTTP 204だったため、この実行履歴の判定には使用していない。Chrome閲覧はユーザーに事前報告した読取範囲のみ。

同日の読戻し修正候補（GPT-6 Luna／max実装、同モデル・maxの独立レビュー）：`outputs/flow-implementation/build_flow_definition.py`で初回GetItemsが0行のときだけ10秒待ち、同じ件を読取り専用GetItemsで1回再照会する。初回・再照会とも1行の9項目が一致した場合だけ`転記済み`、空・複数・不一致・取得失敗は`結果不明`。再照会経路でExcel書込・OneDriveコピーは繰り返さない。テストはredを確認してから実装し、補強後の`test_flow_definition.py`13件、Python構文検査、定義ビルダー静的検査が合格。独立レビューで重大な論理欠陥は見つからず、生成JSONの一致を確認した。`$top`を2から3へ変えて要求形を変えたが、キャッシュ回避の実証ではない。**クラウドフローは未変更、修正版の実行は未実施**。Bの状態を成功に訂正したことにもならない。

同日のクラウド反映：ユーザーがDeveloper環境の既存フロー1件の更新と、別の架空件1件での試験を明示許可した。GPT-6 Luna／max担当がDataverse CLIで対象フローID・名前・有効状態を確認し、`clientdata`のみをPATCH（HTTP 204）。再読取でクラウド`clientdata`がローカル修正候補と文字列完全一致し、有効状態`statecode=1/statuscode=2`を維持した。新たなフロー・ファイル・件はこの段階では作っていない。上段の「クラウド未変更」は当時の候補作成時点の記録であり、現在は反映済み。修正版の実行結果は未確認。

修正版の新架空件試験前の画面不具合：今回の組合せ「架空環境B／架空サーバーB／実行回2／2026-09-25」はDataverse CLIの完全一致照会で0件。ChromeのStudioプレビューで進捗一覧から「新しい件を受付」を押すと、受付に既存`normal-b.txt / 保存済み`と添付が残り、4項目表示は旧A初期値、古い「処理を開始しました」表示と処理開始ボタン有効だった。Screen2のボタンは単なる`Navigate(Screen1, ...)`で、保存済みの一時キューが残る。GPT-6 Luna／maxテスト担当はCの入力・保存・開始をせず停止。既存B行にも変更なし。保存済み一時表示だけを新規受付時に初期化し、未保存・結果不明の入力を破棄しない最小修正が必要。Chrome利用は事前報告済み。

2026-09-26の新規受付修正と実機再検証：GPT-6 Luna／max担当が`outputs/canvas-new-intake-reset-stage-20260926/`のScreen1・2を修正。新規受付の指示があり、保存・開始要求が進行中または結果不明でなく、未保存キューがない場合だけ一時キュー・選択中件・古い開始表示を初期化し、Dataverse行には触れない。Canvas Authoring MCPのコンパイルは`Validation PASSED`、6 YAMLを別フォルダーへ読戻し、変更した2画面が送信版と一致。Studioで保存後、正式プレビューの進捗一覧から新規受付を開き、旧`normal-b.txt`・添付・入力待ち件が消え、保存・開始ボタンが無効の空画面を確認。GPT-6 Luna／max独立レビューは、処理中の旧件がDataverse上に残り新規受付できるS02-A4と整合し、永続件の消失経路はないと判定した。初回のChrome操作停止はStudio**編集モード**でボタンにAlt＋クリックが必要だったためで、F5の正式プレビューへ切り替えて解消した。

同日の新規架空件Cによる修正版試験：ユーザーが既存フロー1件の修正と、別の架空ログ1件だけの保存・開始・結果照合を許可。Chromeの正式プレビューで`samples/t006/normal-c-readback.txt`を1件添付し、環境B／サーバーB／実行回2／対象処理日2026-09-25を設定。保存は1回、処理開始は1回だけ実行した。開始前のDataverse読戻しは該当行1件（件ID`7eace997-2fb9-f111-b377-7ced8d3141aa`）、4項目一致、親件IDが一致する添付1件（178バイト）と状態`保存済み`を確認。開始後に進捗一覧へ移り、最初は`処理中`／Excel未確認。後続のCLI読戻しは同じ1行が`転記済み`／`全文一致`。SharePointプラグインの読取りで、T-006検証フォルダー内のC件ID名の結果Excelは1冊・Evidence表は1行のみで、9列の識別情報と6行のログ全文・末尾改行が元ファイルと一致。画面を新規受付経由で開き直すとCは`処理完了`／`反映確認済み`と表示した。旧Bの件ID・状態`結果不明`／`読出不能`・失敗理由・版数・ExcelアイテムIDと更新時刻は変わらず、再実行も上書きもしていない。Cの成功は短い正常1件の部分試験であり、複数環境・異常・長文・安全な再実行・担当者確認・レビューの合格には使わない。

#### 完了記録

- 変更内容：件別転記フロー1件をDeveloper環境に作成・有効化。架空件Aと添付1件を追加。アプリ側は未変更。
- 自動検証・ブラウザ・実機確認の結果：ローカル単体検査12件・自己試験合格、CLIで作成・有効化・定義読戻し成功。ブラウザ操作はauto-review拒否、実行とExcel実内容は未確認。
- 確認した対象版：フロー `ef881fbc-dcb8-f111-b377-7ced8d3141aa`、送信定義`outputs/flow-implementation/workflow-create-body.json`。
- ユーザーの受入結果：未確認。
- 残る課題と、その引き継ぎ先：Power Appsからの起動と画面結線、架空件A/Bのクラウド実行、Excel実内容、失敗・結果不明・再実行、長文をT-009・T-011で確認する。ブラウザ操作が必要ならauto-reviewの拒否理由を解消するか、ユーザーによる手動確認を使う。

### T-010：担当者確認・レビュー依頼・差し戻しを実装する

- 目的と今回の範囲：担当者役が転記内容の一致とログの処理結果への判断を別々に記録し、正常・異常・判断保留とコメントを残す。アプリ内でレビュー依頼、確認済み、理由付き差し戻し、修正後の再依頼を行う。
- 今回変更しないこと：別人の独立レビューや役割別アクセス制御を達成したと表示しない。メール・Teams通知を追加しない。
- 関連する要件・完成条件・画面仕様・技術上の規律：REQ-05～REQ-06／C05・C06／TR-02・TR-05。画面仕様は[件ごとの確認](ui/screens/件ごとの確認.md)と[レビュー](ui/screens/レビュー.md)（版 UI-20260924-01、追加承認を反映）。参考画像・承認済み基準画像・T-010対応テストは未作成。確認方法は`docs/CHECKS.md`のTR-05。
- 担当と先に必要な作業：実装担当 GPT-6 Luna／max、統括 `/root`。T-009で作成した完成版候補を引き継ぐ。C 1件の一巡はGPT-6 Luna／maxのテスト担当が各保存後のDataverse読戻しを行い、URL記録版フローの独立静的レビューも同モデルが担当した。
- 承認済みの操作条件：転記照合は「一致／不一致／確認保留」、処理結果判断は「正常／異常／判断保留」を別々に記録し、「異常」「判断保留」ではコメント必須、「正常」では任意。レビュー結果は「確認済み／差し戻し」で、差し戻し時は理由必須。Excel実内容の確認後に担当者の照合・判断／コメントが記録済みならレビュー依頼できる（照合不一致・異常／判断保留でも記録が揃えば依頼可）。差し戻し後は担当者3記録だけを修正・保存してから再依頼できる。同じアカウントで担当者役からレビュアー役へ切り替えて結果を記録し、保存成功後は担当者役へ戻す。確認済み後のレビュー結果は訂正できない。
- 未決定・ユーザーの判断待ち：レビュアーの確認基準の細部、コメント／差し戻し理由の文字数・形式、保存失敗時の正確な表示は画面仕様上未決定であり、本タスクで独断追加しない。
- 確認する操作・期待結果・対象端末：C05の照合・処理結果判断を分けた保存と依頼条件、C06の担当者役→レビュアー役→確認済み／理由付き差し戻し→担当者記録の保存後に再依頼を実画面で確かめる。差し戻し後もログ・4項目・既存Excelが変わらず、確認済み後の訂正ができないことを確認する。対象端末・ブラウザは`docs/CHECKS.md`の実機確認手順による。
- 関係する画面・データ・課金・資料：件別確認・レビュー・一覧、架空ログ・Excel・レビュー記録、画面仕様、CHECKS.md。
- 正式な不合格回数：1回（2026-09-26の初回評価）。停止上限は初回を含め3回。
- 次の行動：C 1件の承認済みレビュー一巡とURL記録版フローのローカル検査修正は完了。C05・C06の未確認分岐を含むT-011の全体確認へ進む。新規件でのURL記録版フロー実行は未確認として扱う。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C05・C06 | 担当者の判断と1アカウントでのレビュー循環 | 初回不合格、修正後のC 1件一巡は合格。全体は未確認 | `outputs/canvas-t010-attachment-display-final-stage-20260926/`とExcel URL記録版フロー | 元ログ表示と証跡Excelリンクを修正し、ダウンロードしたExcelの9列と元ログ80文字を照合。Cの担当者記録→依頼→差し戻し→修正→再依頼→確認済みを非公開Studioプレビューで操作。各書込後のDataverse読戻しと、確認済み後の画面操作無効を確認。異常判断・空コメント拒否・別人レビュー等は未確認。フローURL新定義の新規件実行も未確認。 |

#### 評価・修正履歴

2026-09-26にGPT-6 Luna／max担当が現行読戻し6 YAMLとREQ-05・REQ-06、TR-05、2画面仕様を読み合わせた。承認済み操作の主要式がCanvas読戻し版に存在することを確認。これは読み取りによる実装状況調査であり、後続の正式評価とは別の事前確認である。

2026-09-26の初回正式評価（不合格1回目）：ユーザー承認済みの既存C一巡について、CLIでC一行・4項目・`転記済み`／`全文一致`・annotation添付1件と結果Excel1冊の内容一致・担当者欄空・レビュー未依頼を事前確認した。ChromeプレビューのScreen3で元ログが「添付されているものはありません」、証跡Excelリンクが無効だったため、レビュー欄への書込前に停止。C05・C06は未達、実機一巡は未実施で、Cレビュー欄への書込はゼロ。当初のScreen3修正候補は別stageでcompile `Validation PASSED`、独立静的レビュー済みだったが、その時点ではStudioの「保存しています...」表示が残り永続保存・実表示は未確認だった。`cr6cb_excelurl`は空欄で、標準コネクタによる安全な保存方法も未確認だった。

同日の修正追試：添付の表示をフォーム内の読取専用Attachmentsコントロールへ移し、`Items = ThisItem.Attachments`とした最終候補`outputs/canvas-t010-attachment-display-final-stage-20260926/`の6 YAMLをCanvas Authoring MCPでコンパイルし`Validation PASSED`。Chromeの非公開StudioプレビューではCの元ログ欄に`normal-c-readback.txt`が表示され、診断表示を除いた版でも再確認した。プレビューを閉じたStudioのタイトルは「保存済み (公開取り消し済み)」。これで元ログへの画面上の参照は改善したが、ファイルを開いての本文確認は画面では未確認で、`cr6cb_excelurl`は依然空欄。証跡ExcelリンクとC05・C06の一巡は未確認であり、正式評価の不合格1回目は維持する。

同日、ユーザーが既存フロー1件のリンク記録処理と既存Cのリンク欄1列の更新を承認。GPT-6 Luna／max担当が`outputs/flow-implementation/build_flow_definition.py`に、Excel 9列・全文の読戻し一致後の成功分岐2つだけでURLを記録する候補を実装し、16件のローカルテストと静的検査に合格。通常の生成先では候補JSONの新規書込が`PermissionError`になったため、定義生成関数を用いてOS一時領域に更新bodyだけを置いた。Dataverse CLIで既存フローID・有効状態・現在定義がローカル基準と完全一致することを確認してから、`clientdata`だけを条件付きPATCHしHTTP 204。読戻しで修正版定義と文字列完全一致、有効状態`statecode=1/statuscode=2`を確認。フローは再実行しておらず、今後の新規成功件でURLが実際に保存される動作は未確認。

同じ承認範囲で、SharePointプラグインの`get_drive_item`から既存CのExcelアイテムID・ファイル名・直接URLを再確認。既存C行の1件・4項目・`転記済み`／`全文一致`・レビュー欄未記録・URL空欄を読戻してから、`cr6cb_excelurl`の1列だけをETag条件付きでPATCHした。後続読戻しでリンクが既存CのExcel直接URLと一致し、4項目・処理状態・Excel照合・レビュー欄は不変。SharePointのC ExcelアイテムID・eTag・サイズも更新前後で不変。Chrome非公開プレビューの進捗一覧からCを選び直すと「証跡Excel」リンクが有効で、そのクリックによりC件ID名のExcel 8256バイトがダウンロードされた。画面からの参照成立は確認したが、ダウンロード内容の再照合はテスト担当が実施中。新しいExcel・共有リンク・公開は作成していない。

同日、GPT-6 Luna／maxのテスト担当がダウンロード済みCのExcelを読取専用で検査し、Evidence表9列、C件ID・4項目・元ログ80文字の一致を確認。統括が事前報告済みのChrome非公開StudioプレビューでCの担当者記録`確認保留`／`判断保留`／コメントを保存し、レビュー依頼、理由付き差し戻し、担当者記録を`一致`／`正常`／新コメントに修正、再依頼、確認済みまで各1回操作。各書込直後、テスト担当がDataverse CLIでC行を読み戻し、予定した状態と件ID・4項目・処理状態・Excel照合・URLの不変を確認した。再依頼時は差し戻し理由と担当者記録を画面と保存先で確認。確認済み後の理由欄は`null`。統括が確認済み後のScreen3担当者入力・保存・依頼・再実行、およびScreen4役割切替・結果入力・記録の無効表示を確認。SharePoint再読取でC ExcelのアイテムID・eTag・サイズ・QuickXorHashは基準と一致。C 1件の最小一巡は合格。異常判断、空コメント拒否、古い依頼版、同時更新、別人・実権限は未確認。初回不合格の記録は履歴として残す。

同日、GPT-6 Luna／maxの独立静的レビューでURL候補生成コードのホスト検査、コピー先一致、読戻し条件・順序の検査不足を指摘。実装担当が対象2ファイルの検査と再現テストを修正し、全18件のローカルテストと`py_compile`に合格。既存のクラウド更新bodyの`clientdata`と修正後の既定生成候補は構造上完全一致し、新Validatorでもエラーなし。変更はローカルの検査コードとテストのみで、クラウドフローの再PATCH・再実行は行っていない。

#### 現時点の記録

- 変更内容：Screen3の元ログ参照を読取専用Attachmentsコントロールへ修正し、既存フローのExcel URL記録版定義とC行のExcel URL 1列を更新。Cの担当者確認・レビュー記録を上記一巡で保存した。
- 自動検証・ブラウザ・実機確認の結果：最終stageの6 YAMLはCanvas MCP compile `Validation PASSED`、Studioタイトルは「保存済み (公開取り消し済み)」。Chrome非公開プレビューでCの元ログ表示、Excelリンク、担当者記録から確認済みまでの一巡と確定後の操作無効を確認。各保存後のDataverse読戻しとExcel実内容の照合は合格。フローURL新定義を新規件で実行した結果は未確認。
- 確認した対象版：`outputs/canvas-t010-attachment-display-final-stage-20260926/`、既存フロー`ccbc5642-9cce-b1e9-07f5-8174f938bab4`、架空件C`7eace997-2fb9-f111-b377-7ced8d3141aa`。
- ユーザーの受入結果：C一巡の実施は承認済み。アプリ完成版の受入と公開は未確認・未実施。
- 残る課題と引き継ぎ先：C05/C06の未確認分岐、URL記録版フローの新規件実行、T-011全体確認、公開判断は後続に残す。ローカルURL検査3点の修正は完了。

### T-011：C01～C06・C08～C10を実機で検証し、修正する

- 目的と今回の範囲：正常・異常・保存失敗・Excel失敗と結果不明・重複・再実行・人の判断・レビュー・開始前取消・再起動復元を、C01～C06・C08～C10に従って確認する。承認範囲内の不具合を直して再確認する。
- 今回変更しないこと：条件を弱めない。未確認を合格にしない。公開しない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQUIREMENTS.md「確認用の完成条件案」／C01～C06・C08～C10。T-007で作るCHECKS.mdと画面仕様。
- 担当と先に必要な作業：テスト実行担当、独立レビュー担当、修正はGPT-6 Luna／max実装担当、統括。T-008～T-010。架空件Bの初回部分試験、Cの転記成功とレビュー最小一巡を引き継ぐ。
- 未決定・ユーザーの判断待ち：テスト端末・失敗再現方法はT-007で決める。仕様変更が必要な指摘はユーザー判断。
- 確認する操作・期待結果・対象端末：各C条件の操作を実行し、期待結果とExcelの実内容、画面の表示・操作、対象版・端末・証拠を条件別に残す。
- 関係する画面・データ・課金・資料：全画面・架空データ・証跡Excel、画面仕様、CHECKS.md、TASKS.md。
- 正式な不合格回数：未確認（停止上限は初回を含め3回）。
- 次の行動：C01の異なる環境の正常2件と、更新前フローでの日付不一致1件・正常1件の混在は実機確認済み。更新後フローでは無効日付・日付行欠落・サーバー識別情報欠落の個別停止・Excel未作成に加え、正常A/A・run102の単独回帰もExcel実体まで確認した。添付読取不能、更新後の異常件と正常件の混在などは未確認。C03はローカル候補の失敗経路ガードをテスト・独立レビュー済みだが、稼働中フローは未変更で実機失敗試験も未実施。C04の安全な再実行は未実装。次の候補は隔離した架空件1件の決定的なExcel書込失敗だが、範囲・復旧方法の整理と個別承認までは行わない。C08候補はCanvas MCP検査6ファイルで`Validation PASSED`、検査後の6ファイル再同期はテキスト一致（Screen1生ハッシュ差は行末差のみ）。Studio保存・実機と添付名／内容照合、同一件再保存、再起動後Unknown復元は未実施／未完了。Chrome保存は回答待ち・未承認扱い。その後C02残件も確認する。未確認を合格扱いせず、追加の実API試験は対象・操作・影響を明示して`docs/WORKFLOW.md`に従う。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C01 | 正常件の保存・転記・Excel実反映と件状態 | 合格（D・G 2件の同時開始） | 2026-09-26の非公開アプリと既存クラウドフロー | 環境D／サーバーDと環境G／サーバーGの正常ログ2件を同じ受付で保存し、「処理開始」1回で実行。各件`転記済み`／`全文一致`、件ID別Excel各1冊・Evidence表各9列1行、4項目とログ全文がそれぞれ一致し取り違えなし。画面も各件`処理完了`／`反映確認済み`を表示。旧Bの初版不合格は履歴に維持し再実行していない。 |
| C02 | 問題件だけ理由付きで停止し、独立する正常件は継続 | 異常3種と正常1件の単独回帰を確認。C02全体は未判定 | 2026-09-26の非公開Studioプレビューと、理由具体化版activeフロー | 更新前版での日付不一致C＋正常Aの混在は部分合格。更新後版では無効日付G、処理日行欠落D、サーバー識別情報欠落Hを個別に保存・開始し、各件が`停止`／`Excel未確認`と個別の期待理由になり、T-006フォルダーの件ID名Excel各0冊を確認。さらに正常A/A・run102の単独回帰で`転記済み`／`全文一致`、件ID名Excel1冊、Evidence表9列1行・4項目とログ全文一致を確認した。更新後版での異常＋正常混在、添付読取不能、長文等は未確認。Hの最終停止理由はCLIで確認し、画面では処理中の瞬間のみ確認。フロー履歴の個別アクションは未確認。 |
| C03～C04・C08～C09 | Excel失敗・重複と安全な再実行・保存失敗・取消 | C03ローカル静的検査合格・実機未確認、C04・C08～C09未完了 | C03ローカル候補と2026-09-26非公開実機版 | C03候補の再試行抑止・結果不明分岐を静的検査し、ローカル24件と独立再レビューに合格。稼働中フローは未変更で、C03実機失敗試験は未実施。C04独立監査では現行列・フローに安全な再実行の処理がなく未実装と確認。旧Bは結果Excelが存在する一方、件状態は`結果不明`なので再実行禁止。試行トークンと条件付き更新は方式候補に留まり、API適合・競合時動作は未確認。C08は行・関連添付件数・状態の読戻しを含むローカル候補と構造テストができ、独立レビューの重大分岐指摘も修正済み。Canvas MCP検査は6ファイルで`Validation PASSED`、検査後の再同期はテキスト一致（Screen1の生ハッシュ差は行末差のみ）。Studio保存・実機は未実施で、添付名／内容照合、安定IDの同一件再保存、再起動後の結果不明復元は未実装・未確認。 |
| C10 | 再起動時の保存済み件と未保存入力 | 進捗一覧を開く条件で一部合格。条件全体は未確認 | 2026-09-26の非公開Studioプレビュー | 既存の保存済みCと停止中Bを表示。架空ログFを未保存で選び、環境F・サーバーF・実行回1・日付を入力してからStudio編集タブを再読込。再プレビューは進捗一覧から開始し、受付に戻ると件一覧0件・添付なし・実行回と日付が空欄。進行中件がない場合の受付初期表示は未確認。 |
| C05・C06 | 担当者確認とレビュー | 既存C 1件の最小一巡は合格。条件全体は未確認 | 2026-09-26の非公開Studioプレビュー | CのExcel実内容を照合し、担当者記録、依頼、理由付き差し戻し、記録修正、再依頼、確認済みを各段階でDataverse読戻し。確定後の画面操作無効も確認。異常判断・空コメント拒否・別人レビュー等は未確認。詳細はT-010。 |

#### 評価・修正履歴

2026-09-26のB部分試験でC01の1回目を不合格として記録。Excel実体は正しいがフローの即時GetItemsが0行となり、件状態が`結果不明`。実行詳細・実Excel・Dataverse状態の証拠はT-009のB記録を参照。GPT-6 Luna／maxが修正候補を実装・テストし、同モデル・maxが独立レビューした。修正版のCは正常1件として合格。その後、異なる環境・サーバーのD・Gを1回の開始操作で処理し、C01を対象版で合格とした。初版のBの不合格は履歴として残し、Bは再実行していない。

同日のC10部分試験：ユーザーがDeveloper環境の非公開アプリでの再起動と架空F 1件の実機試験を承認。Chromeの新しいStudio編集タブでプレビューを開くと進捗一覧に保存済みC・停止中Bを表示した。受付へ移動して`samples/t006/judgment-hold.txt`を未保存で選び、架空環境F／架空サーバーF／実行回1／対象処理日2026-09-25を入力。「選択中の件を保存」は押さず、プレビューを閉じてStudioタブを再読込した。再プレビューは進捗一覧から始まり、受付へ移ると選択待ち0件・添付なし・実行回と日付が空欄だった。保存済み件の復元と未保存入力の破棄はこの条件で合格。進行中件がないとき受付から開く条件は未確認。Fのクラウド件行・添付・Excelはこの段階では未作成。

同日のF新規件試験：実行前に同じ4項目の件は0行、架空環境F／架空サーバーFの転記先対応は有効1件、ひな形Excelは1冊と読戻した。Chromeの非公開StudioプレビューでFを1回保存し、Dataverse件ID`0165ef02-5ab9-f111-b377-7ced8d3141aa`が1行・状態`保存済み`、annotation添付が1件で、元の`samples/t006/judgment-hold.txt`と262バイト・SHA-256が一致した。続けて「処理開始」を1回押し、一覧の`処理中`表示から、再表示後に`処理完了`／`反映確認済み`へ遷移した。Dataverseは`転記済み`／`全文一致`、Excel URLが記録された。T-006フォルダー内でF件ID名のExcelは1冊だけ（OneDrive item ID`01VTXCECHFAAGZGLRLSRFI3ND774ZHBWJ7`）。Evidence表の9列・対象行1行で件ID・ファイル名・4項目・分割情報・ログ全文が一致し、日付セル`2026-09-25 00:00:00`は対象日と一致した。保存先の直接URLと件表のURLは一致し、Screen3の「証跡Excel」リンクが有効。既存A・B・Cの再実行やExcel上書きはしていない。この1件と先行Cの成功から、同時に複数件を開始する条件、失敗・結果不明の分岐、異常判断、長文を合格としない。

同日のC02混在試験：GPT-6 Luna／max担当がDeveloper環境の既存件と転記先を照会し、`samples/t006/date-mismatch.txt`の環境C／サーバーC／実行回1／画面指定日2026-09-25は0件、環境Cの転記先は有効と確認。本文日付は2026-09-24で1日不一致。正常側の既存3サンプルは4項目が使用済みのため、`samples/t006/normal-a.txt`の実行回だけ2へ変更した`normal-a-run2.txt`をローカルに追加した。環境A／サーバーA／実行回2／2026-09-25も0件で転記先は有効、本文の日付・識別情報は画面値と一致。新サンプルのSHA-256は`5F0B9AC1B63545E2355D095EC98EE7A640FDEDA99426908C553135585C0A9D4D`。ユーザーが2件の保存・同時開始と最大Excel1冊を承認した。Chromeの非公開プレビューで2件を各1回保存し、Dataverseは各1行・添付各1件でファイル名・バイト長・SHA-256が元ログと一致した。正常Aの件ID`9f0b7d1e-5db9-f111-b377-7ced8d3141aa`、日付不一致Cの件ID`308ded3a-5db9-f111-b377-7ced8d3141aa`。処理開始ボタンを1回押した後、Aは`転記済み`／`全文一致`、Cは`停止`／Excel確認`未確認`。T-006フォルダーにはA件ID名のExcel1冊（item ID`01VTXCECB6YBHBA7QOCJEL2HJ5CO27M2GC`）、C件ID名のExcel0冊。AのEvidence表は9列・対象行1行で件ID・ログ名・4項目・分割情報・ログ全文が一致し、件表のURLと実ファイルのURLも一致した。進捗一覧はAの`処理完了`／`反映確認済み`、Cの`停止`／`未確認`を表示し、Aの件確認画面には有効なExcelリンクを表示した。Cの理由は「ログの日付・識別情報が件と一致しないか、1行上限を超えています。」で、日時不一致が実際の原因であることは元ログと画面値から確認できるが、画面文言だけでは一意に判別できない。この試験は日付不一致件の分離を確認した部分結果であり、C02の他の異常全種とC01の複数正常件を合格にしない。既存件を変更・再実行していない。

同日のC01二正常件試験：ユーザーがDeveloper環境の非公開アプリで新規正常ログ2件、添付2件、結果Excel最大2冊の実機試験を承認。GPT-6 Luna／max担当が重複0件と有効な転記先各1件を事前確認。`samples/t006/normal-d-run2.txt`（環境D／サーバーD／実行回2／2026-09-25）と`normal-g-run2.txt`（環境G／サーバーG／実行回2／同日）を同じ受付で各1回保存した。件IDはD=`d08f0611-5fb9-f111-b377-7ced8d3141aa`、G=`96709826-5fb9-f111-b377-7ced8d3141aa`。保存後は件各1行・添付各1件で、元ログと各136バイト・SHA-256一致。処理開始を1回押した後、Dataverse両件は`転記済み`／`全文一致`、画面は各件`処理完了`／`反映確認済み`。T-006フォルダーにはD件ID名Excelが1冊（item ID`01VTXCECCN33O7A22JZZEJOBWUNRW77S35`）、G件ID名Excelが1冊（item ID`01VTXCECBPCR6RPV3M2BBKUEPD6AANJO2B`）。Evidence表は各9列・1行で件ID、ファイル名、環境・サーバー、実行回、日付、分割情報、ログ全文がそれぞれ元ログと一致し、取り違えなし。生成した2つの元ログはどちらも詳細行が実際には`詳細: 架空環境。`であり、この同じ実内容との一致を確認した。既存A・B・C・Fは変更・再実行していない。C01の操作と期待結果をこの対象版で合格とした。

同日のC02理由具体化のローカル準備：GPT-6 Luna／max実装担当が入力異常の理由を分け、存在しない暦日の拒否と、添付本文の読取失敗・タイムアウト時にExcelコピー前で`停止`を記録する分岐を追加した。同モデル・maxの独立レビューで、両分岐が含まれ、読取失敗経路からExcelコピーへ進まない構造を確認。ローカルテスト21件と静的検査は成功したが、式の実評価は未確認。Excel URL記録・読戻し経路を組み合わせた候補は`outputs/flow-implementation/flow-definition.excelurl-c02-local-candidate.json`（SHA-256 `917d7ec981b5490da494d8f5b3f14b58cf6b2ad723870360634d96ab60bbfe1b`）。GPT-6 Luna／maxの独立レビュー後、稼働中フローの保存済みGET定義とオフライン比較し、C02判定に関係する19パス以外に差分がないことを確認した。実API更新は未実施だった。

同日のC02理由具体化版のクラウド反映（反映直後時点の記録）：ユーザー承認を得て、Developer環境の既存フロー`ef881fbc-dcb8-f111-b377-7ced8d3141aa`（`架空ログ証跡_件別Excel転記`）へDataverse CLIから条件付きPATCHを実行し、HTTP 204を確認。PATCH前に保存した稼働中定義とETagに対する差分は19パスで、C02のログ判定・停止理由に限定されていた。直後にGETで読み戻し、`prepare_workflow_patch.py --verify-readback`が候補と一致と判定し、フローがactiveであることを確認した。候補SHA-256は`917d7ec981b5490da494d8f5b3f14b58cf6b2ad723870360634d96ab60bbfe1b`。前後の定義スナップショットとPATCH準備物は`outputs/t011-20260926/`以下（gitignore対象）に保存。この段落は次項の異常3件および正常回帰を実施する前の状態記録である。当時はフロー未起動で、後続の各項に実行結果を記録する。

同日のC02更新版3異常ログ試験：ユーザー承認済みのDeveloper環境非公開アプリをChromeで操作し、1件ずつ保存・開始。開始直前のPAC FetchXMLで各4項目の重複は0件、有効転記先はG/G・D/D・H/H各1件。対象処理日は全件2026-09-25。
- G/G・実行回1の`samples/t006/invalid-date.txt`（本文日付2026-02-30）は件ID`757e9bb1-73b9-f111-b377-7ced8d3141aa`、添付1件・181バイトで保存。処理後は`停止`／Excel`未確認`、理由「ログの処理日は暦上存在しない日付です。」。画面でも停止と理由を確認。
- D/D・実行回1の`samples/t006/missing-date.txt`（冒頭の日付行なし）は件ID`6f972671-74b9-f111-b377-7ced8d3141aa`、添付1件・156バイトで保存。処理後は`停止`／Excel`未確認`、理由「ログの冒頭に処理日行（処理日: YYYY-MM-DD）がありません。」。画面でも停止と理由を確認。
- H/H・実行回1の`samples/t006/missing-identifier.txt`（サーバー行なし）は件ID`5a997206-75b9-f111-b377-7ced8d3141aa`、添付1件・129バイトで保存。処理後は`停止`／Excel`未確認`、CLI読戻しで理由「ログにサーバーの識別情報がありません。」を確認。画面では処理中の瞬間のみ確認し、最終理由の画面表示は未確認。
- SharePointプラグインの読取りでT-006検証フォルダーを各開始前・後に確認。既存の計16項目から増減なしで、各件ID名のExcelは0冊。Excel判定列`未確認`とフォルダーの0冊確認を区別して記録。フロー履歴の個別アクション結果は未確認。この3件の実行では旧B・normal-demo-run101・他の既存件を再実行せず、公開もしなかった。
- 同じプレビューで停止済みnormal-demo-run101のScreen3を開き、転記照合・判断・コメント欄と「確認を記録」ボタンがdisabledであることを確認。既存の担当者値は消去・変更していない。これは停止件の画面操作ゲート確認であり、担当者記録全条件の回帰確認ではない。
- 判定（3異常試験時点）：更新後フローで3種類の異常理由と当該件のExcel未作成を個別確認した段階では、C02全体は未判定で、正常経路回帰も未実施だった。後続の正常回帰結果は次項に記録する。添付本文読取不能と更新後フローでの異常件・独立正常件の混在は引き続き未確認。既存の日付不一致＋正常混在は更新前フローの結果であり、更新版の混在合格には転用しない。

同日のC02更新版正常単独回帰：C02更新版がactiveであることを確認した後、PAC FetchXMLでA/A・実行回102・2026-09-26の重複0件、有効な転記先1件を事前確認。Chromeの非公開Studioプレビューで`samples/t006/normal-demo-20260926-run102.txt`を1件添付し、保存1回・処理開始1回を実施した。Dataverse件IDは`25ba005c-76b9-f111-b377-7ced8d3141aa`、添付annotation IDは`26ba005c-76b9-f111-b377-7ced8d3141aa`、177バイト・1件。CLI読戻しで件状態`転記済み`／照合`全文一致`とExcel URLを確認。SharePointプラグインでT-006検証フォルダーが16項目から17項目へ増え、件ID名の結果ブック1冊（8265バイト）を確認した。ブックのEvidence表は9列・1行で、件ID、ログ名、A/A、実行回102、対象日、架空ログ全文が元ログ・入力値と一致した。Chromeプレビューでは開始直後に`処理中`と表示されたが、進捗一覧から受付へ移動して戻る再読込み後に`ログ保存：保存済み`／`転記処理：処理完了`／`Excel実反映：反映確認済み`へ更新された。担当者欄・レビューは未記録／未依頼。公開・削除は行わず、旧Bと`normal-demo-run101`は再実行していない。これは更新後フローの正常単独回帰の確認であり、異常件と正常件の混在、添付本文読取不能、長文などを含むC02全体の合格ではない。

同日の本人試行と調査：ユーザー提供の進捗一覧画像では`normal-demo-run101.txt`が架空環境A／架空サーバーA／実行回101／指定日2026-09-26で`停止`、`Excel実反映：未確認`、理由は「ログの日付・識別情報が件と一致しないか、1行上限を超えています。」。ローカル元ファイルの1行目は`処理日: 2026-09-25`で、指定日との不一致を確認。正常経路として案内した本人試行は不成立で、ログ／指定日の組合せを明示しなかった案内側の不備がある。これを本人の単独の操作ミスとは判定しない。停止件は再開始せず、画面の`転記照合：一致`と`処理結果の判断：正常`はExcel成功を示さない。ローカル画面定義では停止件でも担当者記録を保存できる条件になっており、承認済みSCR-03の照合前提との不整合を確認した。値がいつ保存されたか、Dataverse列の既定値かどうかは未確認。別候補の`normal-demo-20260926-run102.txt`は後続のC02更新版正常回帰で1回保存・処理され、Excel実体まで一致した（詳細は上記）。本人による受入は未実施であり、run101の停止件は再実行していない。

本人が1件の読取りを許可した後、CLI 2.12.2の`pac org fetch`でDeveloper環境の件表をログ名、次に環境A／サーバーA／実行回101で限定照会した。`pac auth list`は対象組織の認証プロファイルを表示し、Fetch要求も終了コード0だったが、件行は表示されなかった。照会XMLは`outputs/t011-20260926/normal-demo-run101-read.fetch.xml`。これは画像の件が存在しない証明ではなく、CLIの照会結果と画面の差として未解決。担当者欄の実保存値・由来、Excel生成の有無は未確認のまま扱う。新しい正常サンプルはローカル`parse_log`で4項目一致時に通過、日付不一致時に拒否されることを確認した。クラウドでの保存・転記成功とは区別する。

同じ1件のCLI読取りを再診断したところ、通常の実行ではPACの認証キャッシュlockfileへのアクセス拒否とDataverse接続失敗が出ていた。PACはこの失敗でも終了コード0を返したため、先の行なし表示を有効な検索結果と扱わない。認証キャッシュにアクセスできる実行環境で同じ1件・4条件のFetchXMLを再実行した。`top`指定はPAC内部の`page`指定と両立せずエラーになるため除去し、再実行で1行を取得。件ID`f15ba124-65b9-f111-b377-7ced8d3141aa`、指定日2026-09-26、状態`停止`、Excel`未確認`、担当者欄`一致`／`正常`、理由は画面表示と一致した。担当者値は実際に保存されているが、いつ・どの操作で保存されたかは未確認。Excelファイルの有無もこの件表照会では判定できない。CLIの終了コードだけでなく接続エラー不在と行内容を確認する必要がある。

停止件の担当者記録ゲート修正：GPT-6 Luna／max実装担当が、Canvas MCPで同期した現行6 YAMLのうち`outputs/canvas-live-readback-20260926-demo-audit/Screen3.pa.yaml`を変更。担当者入力3欄と「確認を記録」は、件状態`転記済み`かつExcel状態`全文一致`のときだけ有効にし、保存イベント自体でも同じ条件に加えて担当者役・レビュー状態を確認する。既存値は削除しない。別のGPT-6 Luna／max担当が独立レビューし、最初の保存イベントの役割・レビュー条件不足を指摘して修正した。ローカルYAML解析・条件検査成功後、Canvas Authoring MCPの6 YAML `compile_canvas`は`Validation PASSED`。同じMCPセッションへの再同期でScreen3のSHA-256はローカル修正版と一致。ユーザーがChromeでの保存を許可し、Studioの非公開編集画面で「すべての変更が保存されます」、保存済み時刻2026-09-26 14:29:23 JST、`btnCaseRecord`の`OnSelect`・`DisplayMode`に修正済み式を確認。公開・件データ更新・フロー起動はしていない。停止件でボタンが無効なのは画面で確認したが、保存後の別セッション再読込と正常件・差し戻し件の実機回帰は未確認。既存の`一致`／`正常`表示値の由来も未確認である。

C08の基準監査：コミット済み`outputs/canvas-live-readback-20260926-demo-audit/`を確認し、従来の`Form1.OnSuccess`が関連添付の保存先読戻しなしに保存済み扱いへ進むこと、状態Patch失敗でも保存済み表示となり得ることを記録した。この監査では添付の部分保存時の実イベントは未確認。以前のYAML文字列検索redテストは偽陽性と方式の決め打ちが指摘され撤回済みであり、C08の根拠にはしない。

C08ローカル候補：`outputs/canvas-c08-save-readback-candidate-20260926/`に件ごとの結果不明隔離、保存済み／入力不正の送信前拒否、Dataverse行・関連添付件数1・状態の読戻し後に保存済み表示する候補を作成した。構造テストはPASSし、GPT-6 Luna／maxの独立レビューで指摘された重大分岐と、実行回カードUpdateのIfError保護の最終小修正も確認済み。Canvas MCP検査はユーザー承認範囲の6ファイル・最大3回のうち1回実行し、`Validation PASSED`。検査前に現行同期6ファイルがコミット基準とSHA-256一致することを確認。検査後の6ファイル再同期では候補とテキスト一致し、Screen1の生ハッシュ差だけCRLF/LFによるものと確認した。Studio保存・公開・データ変更・実機試験は未実施。ChromeでのStudio保存は提案したが、ユーザーは進捗と所要時間の説明を求めたため、保存許可は得ていない。添付ファイル名・内容一致、安定IDでの同一件再保存、再起動後の結果不明復元は未実装・未確認のため、C08全体は未完了。次はStudio保存の明示確認後に画面・実機の検査範囲を整理する。

#### T-011：C03・C04の失敗経路ガード（ローカル、実機未確認）

- 対象：`outputs/flow-implementation/build_flow_definition.py`、`test_flow_definition.py`、`flow-definition.excelurl-c02-local-candidate.json`。Power Automateの失敗経路をローカルで検査し、稼働中フローの実機挙動とは分けて評価する。
- 候補の規律：`CopyDriveFileByPath`とExcel `PatchItem`の自動再試行は`retryPolicy=none`。コピー失敗／タイムアウトと、コピー後のExcel書込・読戻し失敗／タイムアウトは、成功扱いにせず`UpdateOnlyRecord`で`結果不明`と再実行保留理由を記録する構造を検査する。
- TDD・ローカル検査：自動再試行の追加、Excel書込の再試行設定欠落、コピー失敗ハンドラーからTimedOutを除外、コピー後失敗を`転記済み`へ変更、二つ目のコピー追加、転記先確認の失敗側へ処理一式を移動する変異を拒否するテストを実施。最後の枝移動は独立レビューで見つかった静的検証器のfalse negativeを修正した後、同等の変異を再実行し、4アクションそれぞれに成功側配置を要求するエラーが出ることを確認した。現行候補が検査を通るpositive controlも維持する。
- 検証結果：`test_flow_definition.py`全24件、`test_prepare_workflow_patch.py`の事前照合テスト4件、対象Pythonファイルの`py_compile`、文書・コードの`git diff --check`が成功。独立再レビューでも枝移動の変異が拒否されることとテストが枝配置理由を直接assertすることを確認した。これはローカルの静的候補検査の合格であり、実機C03の合格ではない。
- クラウド状態と未確認：稼働中フローの定義・アプリ・件データ・Excelは今回変更しておらず、フローも起動していない。実際のExcel副作用、Unknown状態のDataverse更新そのものが失敗した場合、サービス側の実効再試行、C03結果不明経路は未確認。C04の安全な再実行は未実装で、旧Bは結果不明のため再実行しない。
- C04独立監査：現行の件表列と稼働中フローには、安全に再実行するための試行識別・旧試行応答の拒否・条件付き状態更新がそろっておらず、安全な再実行は未実装。旧Bは結果Excelが存在するのに件状態が`結果不明`で、重複や上書きを避けるため再実行禁止とする。試行トークンと条件付き更新は方式候補だが、現行Dataverse APIでの条件付き書込・競合拒否・旧応答排除は未確認であり、採用済み仕様や実装として扱わない。次のゲートは現行列とAPIで候補が実現可能かを非破壊で調べること。書込試験が必要なら、操作範囲・副作用・復旧方法を定めて個別承認を得る。
- 次の実機候補：隔離した架空件1件で、決定的なExcel書込失敗を1回だけ再現する。失敗注入の仕方・副作用確認・復旧手順と操作範囲を先に定め、個別承認を得るまでは実施しない。C08候補の現況は直前のC08ローカル候補記録を参照し、このC03実機候補とは別件として扱う。

#### 完了記録

- 変更内容：未実施。
- 自動検証・ブラウザ・実機確認の結果：未実施。
- 確認した対象版：未設定。
- ユーザーの受入結果：未確認。
- 残る課題と、その引き継ぎ先：条件別結果をT-012へ渡す。

### T-012：架空データを相手に操作してもらい、受入を確認する

- 目的と今回の範囲：短い操作説明と架空の正常・異常サンプルを用意し、作者の端末で相手に操作してもらい、証跡Excelの内容、担当者とレビューの記録、作品で改善した範囲と人に残る確認を示す。
- 今回変更しないこと：相手端末への招待、一般公開、根拠のない効果数値を追加しない。独立レビュー・権限制御済みと主張しない。
- 関連する要件・完成条件・承認済み計画・画面仕様・参考画像：REQUIREMENTS.md「目的と作るもの」「今回は対象外」、R09・R10／C07、C01～C06・C08・C09の検証結果。画面設計は版 UI-20260924-01 で承認済み。受付の参考画像B-20260924は画面資料を参照。実演計画・基準画像は未作成。
- 担当と先に必要な作業：統括・実演準備担当、受入結果はユーザー。T-011。実担当は着手時に割り当てる。
- 未決定・ユーザーの判断待ち：実演の相手・日時・端末、ユーザー受入。公開・別端末共有は別判断。
- 確認する操作・期待結果・対象端末：相手が作者端末で正常・異常例を操作し、架空ログ全文と識別情報が残るExcel、確認結果、独立レビューと権限制御は未検証という限界を確かめられる。
- 関係する画面・データ・課金・資料：全画面、架空ログ・Excel、操作説明、T-011の結果、公開範囲の未決定事項。
- 正式な不合格回数：未確認（停止上限は初回を含め3回）。
- 次の行動：T-011の結果を受けて実演資料を準備し、ユーザー受入を確認する。

#### 完成条件ごとの現在の結果

| 条件ID | 確認内容 | 判定 | 対象版 | 証拠・未確認理由 |
| --- | --- | --- | --- | --- |
| C07 | 架空データの操作実演、短い説明、作品の限界 | 未確認 | 未設定 | 実演・ユーザー受入は未実施。 |
| C01～C06・C08～C10 | 実演へ引き継ぐ条件別結果 | 未確認 | 未設定 | T-011未実施。 |

#### 評価・修正履歴

未実施。評価した日時、対象版、条件別結果、証拠、指摘と修正、不合格回数への加算理由、残る問題を実施時に追記する。

#### 完了記録

- 変更内容：未実施。
- 自動検証・ブラウザ・実機確認の結果：未実施。
- 確認した対象版：未設定。
- ユーザーの受入結果：未確認。
- 残る課題と、その引き継ぎ先：公開と相手端末からの利用は別判断。

## 中断・再開メモ

### 現在の引き継ぎ（2026-09-26、Git整理時）

- **結論と段階：** 非公開の4画面アプリで正常経路は操作可能だが、完成・公開可能・全条件合格ではない。C01は正常D・Gの同時開始で合格。C02は更新後版で3種類の異常を個別理由付き停止・Excel未作成と確認し、正常A/A・run102の単独回帰もExcel実体まで一致した。更新後版での異常＋正常混在、添付読取不能、長文等は未確認。更新前版の日付不一致＋正常混在は部分確認に留める。C05・C06は既存Cの同一アカウントによる最小レビュー一巡を確認し、停止件Screen3の入力操作disabledも確認。C10は保存済み件復元・未保存入力破棄の一条件のみ確認。本人による最終受入は未実施。条件別の最新証拠はT-011の表と`docs/CHECKS.md`を参照する。
- **最優先の残件（重大）：** C03はローカル候補の失敗経路ガードと変異検査が合格したが、稼働中フローは未変更で実機失敗経路は未確認。C04の安全な再実行は未実装。C08・C09を含む保存／開始／Excel書込の失敗や返答不明、重複防止、安全な再実行の実装・確認が残る。C08では保存済み表示前の行・添付読戻しと、安定IDによる同一件再保存が未実装・未検証。同時開始防止と試行識別、実体の照会後に再実行可否を決める経路も未実証。画面の再実行ボタンは現時点で無効。旧BはExcel実体が正しく件状態だけ`結果不明`であり、重複を避けるため再実行せず保全する。
- **次の残件（重要）：** C02は更新版で確認した3種類の異常と正常単独回帰以外に、添付本文読取不能、更新後フローでの異常＋正常混在、長文ログ等を確認する。併せてC05・C06の異常／判断保留・空コメント拒否・古い版の更新拒否、C10の進行中件がない場合の初期画面を条件別に確認する。`normal-demo-run101.txt`は日付不一致で停止した既存件として再実行せず保全する。正常単独回帰は確認済みだが、クラウド実行範囲の受入はまだ途中。
- **今回のGit整理：** 正式資料、Canvas YAML、フロー定義・テスト、架空サンプル、参照される検証証拠を管理対象にする。ルートの`.gitignore`で認証設定、.NET／Python生成物、エディタ・一時ファイルを除外する。`outputs/`全体やYAML・Excel・画像は除外しない。Dataverseプラグインは過去のローカル堅牢化候補で、現行アプリへ登録・展開されていない。GitHub送信先は公開リポジトリ。認証秘密値の高確度一致は見つからなかったが、証拠に環境・フロー・件のIDがあるため、今後の追加時も送信内容を点検する。
- **Gitの引き継ぎ状態：** `main`の`cb579a1`を`origin/main`へプッシュ済み（2026-09-26）。直後の`git status --short --branch`は`## main...origin/main`で作業ツリーに未反映差分なし。Git整理の検査は`git diff --cached --check`成功、フローのローカル検査24件、モック操作検査、未展開プラグインのローカル検査16件成功。実APIの異常経路の合格を意味しない。再開時はGitの最新コミットと実物を改めて照合する。
- **次の24時間の最小の一歩：** T-011と`docs/CHECKS.md`を読み、C03候補と稼働中フローの差分を読み取りで照合する。続いて隔離した架空件1件の決定的なExcel書込失敗試験について、注入方法・副作用確認・復旧手順・操作範囲を定める。個別承認まではクラウド試験を実行しない。C04安全な再実行は未実装、旧Bは再実行しない。C08候補のCanvas MCP検査は6ファイルで`Validation PASSED`。Studio保存は未承認・未実施で、実機確認・添付名／内容照合・安定ID再保存・再起動後Unknown復元は未実施／未実装。ブラウザ／コンピュータユースを使う場合は直前に報告し、公開は別承認とする。
- **引き継ぎ用プロンプト：** 「`C:\dev\Powerapps_gas`の`docs/TASKS.md`『現在位置』『T-011』『中断・再開メモ』、`docs/REQUIREMENTS.md`の要件と技術上の規律、`docs/CHECKS.md`を読んでください。非公開Power Appsの正常D・GはC01合格です。C03はローカル候補の失敗経路ガード・変異検査が24件合格し独立再レビューも通過しましたが、稼働中フローは未変更で実機失敗は未確認です。C04の安全な再実行は未実装です。旧Bは結果不明のまま再実行しないでください。まずGitの最新状態と実物を読み取りで照合し、C03候補とライブ定義の差を確認してください。隔離した架空件の決定的なExcel書込失敗試験は、注入方法・副作用確認・復旧手順・範囲を定め、個別承認を得てから実施してください。C08候補のCanvas MCP検査は6ファイルで`Validation PASSED`。Studio保存は未承認・未実施です。実機確認・添付名／内容照合・安定ID再保存・再起動後Unknown復元は未実施／未実装です。ユーザーが指定した実装・実装計画レビューはGPT-6 Luna／maxサブエージェントを用い、起動時にモデル・推論強度を表示してください。CLI／MCPを優先し、ブラウザ／コンピュータユースは直前に報告してください。クラウド書込・実API試験・公開は`docs/WORKFLOW.md`に従い、未確認を合格扱いせず`docs/TASKS.md`へ証拠とともに記録してください。」

### 過去の引き継ぎ（2026-09-25、T-002レビュー後）

- 作業中のタスク：T-001の残件とT-003の方式検証。T-002は短い架空ログA・Bの件別Excel各1冊を実内容まで独立レビューして、方式検証として完了。完成アプリのC条件合格ではない。
- 作業場所・ブランチ：`C:\dev\Powerapps_gas`、`main`。資料更新、`log-evidence-app/`、`samples/`、`outputs/t002-20260925/`は未コミット。既存変更を保護する。
- 直近の検証：初回Aの実行履歴は「テストがキャンセルされました」。修正版ひな形はExcel Onlineの修復警告なし。A・Bの再試験は実行履歴で成功し、各Excelの本文全文・識別情報と別件混入なしを確認。Cの同一件再送は「宛先ファイルが既に存在しています」でコピー失敗、Excel行更新はスキップ。Cの既存Excelは1冊、内容・ID・ハッシュ不変。通常のコピーは上書き「いいえ」、Excel行更新の自動再試行は「なし」。結果不明時の安全な再実行と完成版の重複登録防止は未確認。
- 次の具体操作：T-001・T-003で通信／保存先の失敗と結果不明を安全に再現する方法を調べる。SharePointプラグインは承認済みのT-002検証で使用した。新たなブラウザ／コンピュータユースは事前報告と了承を受けた範囲だけで行う。

### 過去の引き継ぎ（2026-09-25 09:30 JST）

- 作業中のタスク：T-001。Developer環境の検証専用テーブルと非公開アプリで、架空ログA・Bの同時選択、件別4項目の保持・修正・取消、残件Bの保存行・添付1件・本文一致を確認済み。T-001全体は未完了。
- 作業場所・ブランチ・直近のコミット：`C:\dev\Powerapps_gas`、`main`、`85cb233`。未コミットの資料更新と`log-evidence-app/`、`samples/`を保持する。
- 直近の検証：Canvas Authoring MCPの`compile_canvas`は3ファイルで`Validation PASSED`。Studioは2026-09-25 09:29 JSTに保存済み・非公開。CLIのFetchXMLは権限のある実行でBの保存行`c1cc3ee4-75b8-f111-b377-7ced8d3141aa`と添付1件・本文100バイト一致を確認。Aの修正値の行は0件。入力不正の保存拒否では失敗文言・待ち件保持・行0件を確認。検証用の日付列は「日付のみ」書式＋「ユーザー ローカル」調整と閲覧確認。通常サンドボックスのPAC CLIは認証キャッシュのロックファイルアクセス拒否で失敗した。
- 次の具体操作：通信・Dataverse側の保存失敗と結果不明を安全に再現できる方法を調べ、T-001の残件を判定する。T-002の件別本文からExcelへの受渡しは別途検証する。完成版の日付列はT-004で決める。新たなブラウザ／コンピュータユースは事前に報告し、了承済みの範囲だけで行う。SharePointプラグインは現在使わない。

### 過去の中断メモ（2026-09-24）

- 中断日時と理由：2026-09-24 22:41 JST。今回はTASKS.mdの整理まで。T-001～T-012の実行は未着手。
- 作業中のタスク：なし。次はT-001（未着手）。
- 作業場所・ブランチ・直近のコミット：C:\dev\Powerapps_gas、main、f42227e（更新前の確認結果）。
- 未コミットの変更と、途中まで行った処理：docs/TASKS.mdの新規作成・整理。その他の未コミット変更は更新前のGit確認でなし。アプリ作業は未実施。
- 起動中のサーバー・実行中の処理：未確認。起動・停止は行っていない。
- 直近の検証結果と証拠：Microsoft公式資料の結果は本書冒頭とT-001の履歴。アプリの実動作・本人環境・費用は未確認。
- 停止理由・判断待ち：本依頼の作業範囲による停止。T-001の実行計画、候補保存場所と費用、外部操作の範囲は未確認。
- 次に行う具体的な一操作：T-001を始める依頼があれば、架空ログ2件・候補保存場所・検証操作・戻し方を示す実行計画を作る。

秘密値や個人情報は記録しない。Git等がまだ未設定の項目は、未設定と記す。

## 記録の維持ルール

- タスクごとに詳細欄を用意する。新しいタスクへ移るときは「現在位置」を更新し、以前のタスクの詳細・履歴は消さない。
- 不合格回数の0回は、正式な不合格がないと確認できた場合だけ記入する。前工程の要件整理の0回は保持し、新しいタスクの回数へ転用しない。
- AIの検証に合格しても、必要なユーザー確認が残る場合は「ユーザー受入待ち」とする。
- 完了済みのタスクを再開する場合は、理由を追記し、以前の評価履歴を保持する。
- 記録が長くなった場合は、過去の詳細を別ファイルへ移してもよい。その場合は、この一覧から参照できるようにする。
- 判定は合格／不合格／未確認／対象外。対象外には理由・判断者・確認日を残し、未実装・未実施を対象外にしない。
- 同じタスクで正式な不合格を累計し、初回を含め3回で停止する。質問待ち、途中の自己確認、単発のテスト失敗は正式評価と区別する。
- 開始・評価・修正・停止・完了時に、対象版、証拠、残る問題、次の一操作を追記する。過去の評価・修正履歴は上書きしない。
